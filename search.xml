<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>AES前端密码加密后端(java)解密的一些记录</title>
    <url>/2020/09/08/AES%E5%89%8D%E7%AB%AF%E5%AF%86%E7%A0%81%E5%8A%A0%E5%AF%86%E5%90%8E%E7%AB%AF-java-%E8%A7%A3%E5%AF%86%E7%9A%84%E4%B8%80%E4%BA%9B%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>本文主要讲前端传输时的密码加密及后端收到密文后解密的过程，避免明文传输密码，文中内容都比较浅显，加密原理部分需要查阅其它资料。至于前端加密是否有意义，见仁见智。</p>
<h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><p>前端用的<code>vue</code>，纯JS也可以，引入<code>crypto-js</code>库，后端用<code>java</code>。</p>
<h2 id="加密方法、MODE、PADDING的选择"><a href="#加密方法、MODE、PADDING的选择" class="headerlink" title="加密方法、MODE、PADDING的选择"></a>加密方法、MODE、PADDING的选择</h2><p>这几个参数之间相互有关联，且前后端加密、解密时的参数必须一致。</p>
<p>有的参数前端有A选项，但是后端没有对应的选项，反之也存在后端有B选项，但前端没有的，选择的时候只能选前后端都有的。</p>
<p>具体可以从下面两篇文档中看到：</p>
<ul>
<li><p><a href="https://cryptojs.gitbook.io/docs/#pbkdf2">前端cypto-js参考文档</a></p>
</li>
<li><p><a href="https://docs.oracle.com/javase/8/docs/technotes/guides/security/StandardNames.html">后端java参考文档</a></p>
</li>
</ul>
<p>三个参数前后端共有的选项分别是：</p>
<ul>
<li>加密方法：<code>AES</code>、<code>DES</code>、 <code>Triple DES</code>(java对应<code>DESede</code>)、<code>RC4</code></li>
<li>mode：<code>CBC</code>、<code>CFB</code>、<code>CTR</code>、<code>OFB</code>、<code>ECB</code></li>
<li>padding:<code>NoPadding</code>、<code>Iso10126</code>、<code>Pkcs7 </code>(java对应<code>PKCS5Padding</code>)</li>
</ul>
<span id="more"></span>
<p>选择的话,每一个方法、MODE、padding的实现都有点复杂，上面的java参考文档里有写具体的技术性文档链接，这里只说使用，不说原理。</p>
<ul>
<li>加密方法通常选<code>AES</code>，参考：<a href="https://csrc.nist.gov/publications/fips/fips197/fips-197.pdf">FIPS 197</a>。</li>
<li>mode: <code>crypto-js</code>默认是<code>CBC</code>，参考：<a href="https://csrc.nist.gov/publications/fips/fips81/fips81.htm">FIPS 81</a></li>
<li>padding：如果上面选的是<code>AES/CBC</code>,那么<code>padding</code>可以选<code>Iso10126</code>或者<code>Pkcs7</code>。<ul>
<li><font color="red">NoPadding不能用</font>，因为AES只支持128bit、192bit、256bit几种大小的分块(block)，取决于key的位数，除非要加密的密文正好是上面这个块的大小，否则就不能解密，这显然是不合理的。</li>
<li>前端的<code>Pkcs7</code>和java的<code>PKCS5Padding</code>是对应的，参考<a href="https://en.wikipedia.org/wiki/Padding_(cryptography)">wiki</a>上的解释,两者基本等价：<blockquote>
<p>PKCS#5 padding is identical to PKCS#7 padding, except that it has only been defined for block ciphers that use a 64-bit (8-byte) block size. In practice the two can be used interchangeably.</p>
</blockquote>
</li>
</ul>
</li>
</ul>
<h2 id="实现工具类"><a href="#实现工具类" class="headerlink" title="实现工具类"></a>实现工具类</h2><p>下面的例子采用<code>AES/CBC/Pkcs7</code>这几项，java端是<code>AES/CBC/PKCS5Padding</code>。</p>
<p>密钥<code>key</code>和初始向量<code>iv</code>都设置成固定值，前后端一致即可。</p>
<p><code>key</code>用于<code>AES</code>方法加密，<code>iv</code>用于<code>CBC</code>MODE，<code>iv</code>的大小要跟<code>block</code>一样，在这里就是128bit，跟<code>key</code>一样</p>
<p>前端引入<code>crypto-js</code>：<code>npm install crypto-js</code></p>
<h3 id="前端"><a href="#前端" class="headerlink" title="前端"></a>前端</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//aesutils.js</span></span><br><span class="line"><span class="keyword">import</span> CryptoJs <span class="keyword">from</span> <span class="string">&#x27;crypto-js&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//把key、iv设置成固定值，前后端的值要一致</span></span><br><span class="line"><span class="keyword">let</span> key = CryptoJs.enc.Utf8.parse(<span class="string">&quot;onlinemenukey123&quot;</span>);</span><br><span class="line"><span class="keyword">let</span> iv = CryptoJs.enc.Utf8.parse(<span class="string">&quot;onlinemenuiv1234&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param </span>word 原始文本</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@returns <span class="type">&#123;*&#125;</span> </span>Base64的文本</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@constructor</span></span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">Encrypt</span>(<span class="params">word</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> srcs = CryptoJs.enc.Utf8.parse(word);</span><br><span class="line">    <span class="keyword">var</span> encrypted = CryptoJs.AES.encrypt(srcs, key, &#123;</span><br><span class="line">        <span class="attr">iv</span>: iv,</span><br><span class="line">        <span class="attr">mode</span>: CryptoJs.mode.CBC,</span><br><span class="line">        <span class="attr">padding</span>: CryptoJs.pad.Pkcs7</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> CryptoJs.enc.Base64.stringify(encrypted.ciphertext);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="后端"><a href="#后端" class="headerlink" title="后端"></a>后端</h3><p>涉及到<code>String</code>和<code>byte[]</code>转换时，最好指定编码类型，可以避免</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.lyuww.bgmanager.Utils;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.apache.tomcat.util.codec.binary.Base64;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.crypto.Cipher;</span><br><span class="line"><span class="keyword">import</span> javax.crypto.spec.IvParameterSpec;</span><br><span class="line"><span class="keyword">import</span> javax.crypto.spec.SecretKeySpec;</span><br><span class="line"><span class="keyword">import</span> java.nio.charset.StandardCharsets;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AESUtils</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 解密前端发送的AES加密处理后的密码文本</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> text 前端发送的加密后的密文</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> key 跟前端设置的key一致,只能是128bit、192bit、256bit</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> iv 初始向量，跟前端设置的iv一致</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String key=<span class="string">&quot;onlinemenukey123&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String iv=<span class="string">&quot;onlinemenuiv1234&quot;</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">DecoderAES</span><span class="params">(String text)</span></span>&#123;</span><br><span class="line">        <span class="keyword">try</span>&#123;</span><br><span class="line">            <span class="keyword">byte</span>[] encrypted=<span class="keyword">new</span> Base64().decode(text);<span class="comment">//把base64编码的文本转换成byte数组</span></span><br><span class="line"></span><br><span class="line">            Cipher cipher=Cipher.getInstance(<span class="string">&quot;AES/CBC/PKCS5Padding &quot;</span>);</span><br><span class="line">            SecretKeySpec secretKey=<span class="keyword">new</span> SecretKeySpec(key.getBytes(StandardCharsets.UTF_8),<span class="string">&quot;AES&quot;</span>);</span><br><span class="line">            IvParameterSpec ivParameter=<span class="keyword">new</span> IvParameterSpec(iv.getBytes(StandardCharsets.UTF_8));</span><br><span class="line"></span><br><span class="line">            cipher.init(Cipher.DECRYPT_MODE,secretKey,ivParameter);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">byte</span>[] decrypted=cipher.doFinal(encrypted);</span><br><span class="line">            String oriringalString=<span class="keyword">new</span> String(decrypted,StandardCharsets.UTF_8);</span><br><span class="line">            <span class="keyword">return</span> oriringalString;</span><br><span class="line">        &#125;<span class="keyword">catch</span> (Exception e)&#123;</span><br><span class="line">            <span class="comment">//解密失败时的操作</span></span><br><span class="line">            <span class="keyword">return</span> e.toString();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><p>主要步骤：</p>
<p>前端</p>
<ul>
<li><p>引入工具类：</p>
<p><code>import &#123;Decrypt, Encrypt&#125; from &quot;@/utils/aesutils&quot;;</code></p>
</li>
<li><p>用<code>axios</code>等方法传输数据，比如密码，在传输前加工具类加密即可</p>
<p><code>Encrypt(this.$data.formData.password)</code></p>
</li>
</ul>
<p>后端</p>
<ul>
<li><p>引入工具类</p>
<p><code>import xxx.AESUtils;</code></p>
</li>
<li><p>用工具类解密还原密码，然后进行后续的数据库端密码操作即可</p>
<p><code>AESUtils.DecoderAES(user.getPassword()))</code></p>
</li>
</ul>
]]></content>
      <categories>
        <category>web</category>
        <category>前端</category>
        <category>后端</category>
      </categories>
  </entry>
  <entry>
    <title>JAVA泛型和C++模板的区别</title>
    <url>/2021/03/26/JAVA%E6%B3%9B%E5%9E%8B%E5%92%8CC++%E6%A8%A1%E6%9D%BF%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<p>主要介绍java泛型，再与C++模板作一个简单的比较。</p>
<span id="more"></span>

<h2 id="1-Java的泛型-Generic-Type"><a href="#1-Java的泛型-Generic-Type" class="headerlink" title="1 Java的泛型(Generic Type)"></a>1 Java的泛型(Generic Type)</h2><p>Java泛型基于类型擦除，使用了泛型的代码在编译后看不到“泛型语法”，泛型可以理解为语法糖，是写给编译器看的，虚拟机里没有泛型的概念。</p>
<h3 id="1-1-为什么要使用泛型"><a href="#1-1-为什么要使用泛型" class="headerlink" title="1.1 为什么要使用泛型"></a>1.1 为什么要使用泛型</h3><p><code>java</code>设计之初并没有泛型，比如，在<code>jdk1.5</code>以前，<code>ArrayList</code>操作的对象都是<code>Object</code>，使用时需要进行手动转换。</p>
<p>举个例子，我们构造一个数组，用来保存<code>String</code>类型的字符串，然后打印每一个字符串的长度：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ArrayList arr=<span class="keyword">new</span> ArrayList();</span><br><span class="line">        arr.add(<span class="string">&quot;1&quot;</span>);</span><br><span class="line">        arr.add(<span class="string">&quot;22&quot;</span>);</span><br><span class="line">        arr.add(<span class="string">&quot;333&quot;</span>);</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;arr.size();i++)&#123;</span><br><span class="line">            String str=(String)arr.get(i);<span class="comment">//对取出的元素执行转换，Object-&gt;String</span></span><br><span class="line">            System.out.println(<span class="string">&quot;第&quot;</span>+(i+<span class="number">1</span>)+<span class="string">&quot;个字符串的长度是：&quot;</span>+str.length());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">第1个字符串的长度是：1</span><br><span class="line">第2个字符串的长度是：2</span><br><span class="line">第3个字符串的长度是：3</span><br></pre></td></tr></table></figure>

<p>然后我们再往数组中添加一个元素，但是输入数据时少打了个双引号，原本想要输入字符串却输入了一个整数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ArrayList arr=<span class="keyword">new</span> ArrayList();</span><br><span class="line">        arr.add(<span class="string">&quot;1&quot;</span>);</span><br><span class="line">        arr.add(<span class="string">&quot;22&quot;</span>);</span><br><span class="line">        arr.add(<span class="string">&quot;333&quot;</span>);</span><br><span class="line">        arr.add(<span class="number">4444</span>);<span class="comment">//忘记了双引号，添加了一个整数</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;arr.size();i++)&#123;</span><br><span class="line">            String str=(String)arr.get(i);<span class="comment">//对取出的元素执行转换，Object-&gt;String</span></span><br><span class="line">            System.out.println(<span class="string">&quot;第&quot;</span>+(i+<span class="number">1</span>)+<span class="string">&quot;个字符串的长度是：&quot;</span>+str.length());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重新运行这段代码，输出结果：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">第<span class="number">1</span>个字符串的长度是：<span class="number">1</span></span><br><span class="line">第<span class="number">2</span>个字符串的长度是：<span class="number">2</span></span><br><span class="line">第<span class="number">3</span>个字符串的长度是：<span class="number">3</span></span><br><span class="line">Exception in thread <span class="string">&quot;main&quot;</span> java.lang.ClassCastException: <span class="class"><span class="keyword">class</span> <span class="title">java</span>.<span class="title">lang</span>.<span class="title">Integer</span> <span class="title">cannot</span> <span class="title">be</span> <span class="title">cast</span> <span class="title">to</span> <span class="title">class</span> <span class="title">java</span>.<span class="title">lang</span>.<span class="title">String</span> (<span class="title">java</span>.<span class="title">lang</span>.<span class="title">Integer</span> <span class="title">and</span> <span class="title">java</span>.<span class="title">lang</span>.<span class="title">String</span> <span class="title">are</span> <span class="title">in</span> <span class="title">module</span> <span class="title">java</span>.<span class="title">base</span> <span class="title">of</span> <span class="title">loader</span> &#x27;<span class="title">bootstrap</span>&#x27;)</span></span><br><span class="line"><span class="class">	<span class="title">at</span> <span class="title">Demo</span>.<span class="title">main</span>(<span class="title">Demo</span>.<span class="title">java</span>:11)</span></span><br></pre></td></tr></table></figure>

<p>从结果可以看到，在循环运行时，前面3次都正确打印出了结果，而到第4次执行到第11行-类型转换时出错了，因为我们在对应位置存储的是一个整数，而不是字符串，这是一个<code>运行时错误（RuntimeException)</code>。</p>
<p>通过上面的例子，我们可以看到“手动转换”来保证类型正确的弊端：</p>
<ul>
<li>每次取出数据都要手动转换，这一点在编译时可以检查</li>
<li>没法在编译时检查数据输入的合法性，一旦出错，就是<code>RuntimeException</code></li>
</ul>
<p>引入泛型以后，上面的例子就简单多了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ArrayList&lt;String&gt; arr=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        arr.add(<span class="string">&quot;1&quot;</span>);</span><br><span class="line">        arr.add(<span class="string">&quot;22&quot;</span>);</span><br><span class="line">        arr.add(<span class="string">&quot;333&quot;</span>);</span><br><span class="line"><span class="comment">//        arr.add(4444); //编译时会进行类型检查，报错：java: incompatible types: int cannot be converted to java.lang.String</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;arr.size();i++)&#123;</span><br><span class="line">            String str=arr.get(i);<span class="comment">//无需类型转换，直接就是String</span></span><br><span class="line">            System.out.println(<span class="string">&quot;第&quot;</span>+(i+<span class="number">1</span>)+<span class="string">&quot;个字符串的长度是：&quot;</span>+str.length());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>泛型让你的代码简单又可靠。</p>
<h3 id="1-2-泛型实现–类型擦除"><a href="#1-2-泛型实现–类型擦除" class="headerlink" title="1.2 泛型实现–类型擦除"></a>1.2 泛型实现–类型擦除</h3><p>泛型在编译的时候，具体的类型参数会被擦除，取而代之的是它的<strong>第一个</strong>“限定类型”，也就是其继承的类或者实现的接口，如果都没有的话，自然就是<code>Object</code>类了。编译器会在编译时检查你的代码，如果有错误的类型转换出现（如给泛型<code>List</code>添加一个错误类型的对象或者把从<code>List</code>取出的对象赋值给一个错误类型的变量），则会报错，无法通过编译。所以<strong>泛型实际上是把运行时的类型转换检查提前到了编译时，避免运行时异常</strong>。</p>
<p>所以，在判断泛型所属类型的时候，不管你传入参数的是什么类型，泛型始终都是同一个类型：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ArrayList&lt;String&gt; arr=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        ArrayList&lt;Integer&gt; arrInt=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        System.out.println(arr.getClass()==arrInt.getClass());<span class="comment">//true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS:虽然进行了“类型擦除”，<code>class</code>文件中仍然有关于被擦除类型的信息，被保存在<code>LocalVariableTypeTable</code>中。</p>
</blockquote>
<p>关于“限定类”，有几点要注意：</p>
<ul>
<li>可以是一个或者多个”限定类型”,但是最多只能有一个是类，其它的必须是接口，这跟类继承是一样的，但是用<code>&amp;</code>符号连接，因为逗号用来分隔不同的类型参数</li>
<li>如果有一个是类，那么类写在第一个位置，如<code>&lt;T extends ClassA&amp;InterfaceB&amp;InterfaceC&gt;</code></li>
<li>如果全部是接口，把最复杂的接口写在最前面可以提高效率。因为类型擦除是转换成第一个”限定类型”，如果第一个只是一个标记接口，那么后面用到其它接口的地方，还要再加入强制转换。</li>
</ul>
<h3 id="1-3-泛型的一些限制"><a href="#1-3-泛型的一些限制" class="headerlink" title="1.3 泛型的一些限制"></a>1.3 泛型的一些限制</h3><p>泛型有一些应用限制，其中一部分就是因为类型擦除。</p>
<ul>
<li>不能传入8大原始类型。<code>int,short,long,byte,boolean,double,float,char</code></li>
<li>泛型中的类型参数不能被实例化，但是可以通过反射，用<code>Class&lt;T&gt;</code>的<code>newInstance()</code>方法获得实例(<strong><code>JDK9</code>以后官方推荐用<code>Class&lt;T&gt;.getDeclaredConstructor().newInstance()</code></strong>)</li>
<li>泛型中的类型参数不能被声明为静态的</li>
<li>不能对带有类型参数的泛型表达式使用<code>instance of</code>，（大部分时候）也不能进行类型转换</li>
<li>不能创建泛型数组，<code>Object[] stringLists = new List&lt;String&gt;[];  // compiler error</code></li>
<li>泛型类不能继承异常类型，如<code>AException&lt;T&gt; extends Throwable//compile error</code>，也不能捕捉T，但是可以<code>throws T</code></li>
<li>不能重载一个仅仅是泛型参数类型不同的方法，因为类型擦除后，他们是一样的。</li>
</ul>
<h2 id="2-C-模板（template"><a href="#2-C-模板（template" class="headerlink" title="2 C++模板（template)"></a>2 C++模板（template)</h2><p>c++的模板就跟它的名字一样，定义的时候它是一个模板，后面使用时，编译器会把模板中的类型替换为具体的类型，生成一份“根据模板制作”的代码。c++有两种模板，类模板和函数模板，模板本身不是类或函数，可以理解创建类或函数的公式。</p>
<h3 id="2-1-模板语法"><a href="#2-1-模板语法" class="headerlink" title="2.1 模板语法"></a>2.1 模板语法</h3><p>以函数模板举例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//定义模板</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">compare</span><span class="params">(<span class="keyword">const</span> T &amp;v1,<span class="keyword">const</span> T &amp;v2)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(v1 &lt; v2) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">if</span>(v2 &lt; v1) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//使用模板</span></span><br><span class="line">cout&lt;&lt; <span class="built_in">compare</span>(<span class="number">0</span>,<span class="number">1</span>) &lt;&lt;endl;</span><br><span class="line"><span class="comment">//在上面的调用中，编译器会自动为函数模板推断类型为int，并生成下面的代码</span></span><br><span class="line"><span class="keyword">int</span> compare（<span class="keyword">const</span> <span class="keyword">int</span> &amp;v1,<span class="keyword">const</span> <span class="keyword">int</span> &amp;v2)&#123;</span><br><span class="line">    <span class="keyword">if</span>(v1 &lt; v2) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">if</span>(v2 &lt; v1) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="3-区别"><a href="#3-区别" class="headerlink" title="3 区别"></a>3 区别</h2><h3 id="3-1编译阶段的处理方式不同"><a href="#3-1编译阶段的处理方式不同" class="headerlink" title="3.1编译阶段的处理方式不同"></a>3.1编译阶段的处理方式不同</h3><p>java 在编译时直接把泛型中的类型参数擦除，并在需要地方插件强制类型转换。而c++在编译到模板定义的代码时，并没有任何操作，只在使用到这份模板代码时，根据实际类型，为这个类型生成一份特定的代码。这一点其实是本质区别，后面的几点区别都是因为这个。</p>
<h3 id="3-2可以接收的参数不同"><a href="#3-2可以接收的参数不同" class="headerlink" title="3.2可以接收的参数不同"></a>3.2可以接收的参数不同</h3><p>java 泛型不能接收8大原始类型，c++没有这个限制。java在类型擦除时，把类型“退化”成它的限定类型，而原始类型比较特殊，无法“退化”成<code>Object</code>。java可以接收通配符，c++不能。c++不能接收类型限定，虽然可以通过其它手段，如<code>c++20</code>的<code>concept</code>来实现类似的功能。</p>
<h3 id="3-3-效率不同"><a href="#3-3-效率不同" class="headerlink" title="3.3 效率不同"></a>3.3 效率不同</h3><p>java的类型擦除会增加很多类型转换的操作，效率低下。而c++模板生成一份特定类型的代码，这跟你自己手写一份和“模板”逻辑一样的代码没有什么区别。</p>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4 总结"></a>4 总结</h2><p>java选择这种看起来缺点很多的泛型实现方式，根本原因就是为了一个优点“兼容性”。java泛型和c++模板本质上还是差别挺大的，c++的模板像宏，java泛型像语法糖。</p>
]]></content>
      <categories>
        <category>编程语言特性</category>
      </categories>
  </entry>
  <entry>
    <title>Javascript中__proto__和prototype的区别</title>
    <url>/2020/07/28/Javascript%E4%B8%AD__proto__%E5%92%8Cprototype%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<p>prototype是函数特有的属性，一个对象的构造函数也是函数，js中没有类，只有构造函数。关于原型链的继承在这里不展开。</p>
<blockquote>
<p><code>__proto__</code>属性已经被标记为deprecated，可以用<code>Object.getPrototypeOf(obj)</code>的方式代替访问该属性，应该避免修改该属性，因为它会影响到所有跟这个属性有关的对象，性能非常低下，可以用<code>Object.create(targetproto)</code>的方式直接用你希望的原型创建一个新的对象。</p>
</blockquote>
<span id="more"></span>

<p><span style="color:red">本文以下内容仍以<code>__proto__</code>的方式表示该属性，主要为解释原理，在生产中应该避免使用</span></p>
<p><code>__proto__</code>是对象上的属性，而js中一切皆对象，访问<code>__proto__</code>属性等同于访问该对象的构造函数的<code>prototype</code>属性，对于一个普通的构造函数及通过其构造的对象，这一点不难理解，在console中测试：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Person构造函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.name=name;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> person1=<span class="keyword">new</span> Person();   <span class="comment">//构造一个person1对象</span></span><br><span class="line">person1.__proto__===Person.prototype    <span class="comment">//true,访问__proto__属性等同于访问该对象的构造函数的prototype属性</span></span><br><span class="line">person1.__proto__===person1.constructor.prototype   <span class="comment">//true,同上</span></span><br><span class="line"></span><br><span class="line">Person.prototype.age=<span class="number">18</span> <span class="comment">//给Person.prototype添加一个age属性</span></span><br><span class="line">person1.age <span class="comment">//18</span></span><br><span class="line"></span><br><span class="line">person1.__proto__.country=<span class="string">&quot;china&quot;</span>   <span class="comment">//添加一个country属性，两种方式相同，操作的是同一个原型</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> person2=<span class="keyword">new</span> Person;</span><br><span class="line">person2.country <span class="comment">//china</span></span><br><span class="line">person2.age <span class="comment">//18</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="Object和Function"><a href="#Object和Function" class="headerlink" title="Object和Function"></a>Object和Function</h2><p>相比于上面的普通对象和构造函数，<code>Object</code> 和 <code>Function</code>比较难理解，在浏览器点开console,输入<code>Object.prototype</code>，在输出中随便找点点击一个函数，如<code>toString()</code>，里面有个<code>__proto__</code>，再点进去，里面还有个<code>__proto__</code>，再点进去，里面的内容跟刚才Object.prototype的输入里一样的，如此循环下去。。。</p>
<p>想搞清楚这些，先明确几个概念：</p>
<ul>
<li>js中一切皆对象，person1是对象，其构造函数Person也是对象，Object，Function都是对象。<span style="color:red;font-weight:bold">所有对象的原型链尽头都是<code>Object.prototype</code></span>。对象的原型链：<code>person1 -&gt; Person.prototype -&gt; Object.protoytpe -&gt; null</code>，这其中的每一环，如<code>Person.prototype</code>也都是对象，所有对象不断的访问<code>__proto__</code>属性，都会返回自己的原型链上游，只有一个例外，<code>Object.prototype.__proto__===null</code></li>
<li>所有函数对象的构造函数都是<code>Function</code>,所有函数对象的原型都是<code>Function.prototype</code>。</li>
<li>一切构造函数是对象的同时，也是函数。<code>Object</code>和<code>Function</code>，既是对象又是构造函数。</li>
<li>原型<code>Function.prototype</code>也是对象，其原型链上游为<code>Object.prototype</code>。</li>
</ul>
<p>如构造函数<code>Person</code>，<code>Person.__proto__===Function.prototype</code>返回true,这里的Person可以理解为一个对象：一个从<code>Function.prototype</code>这个原型继承而来的对象。</p>
<p>在console中运行下面的代码来验证上述的原型之间的关系：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//构造Dog</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Dog</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"><span class="keyword">var</span> dog1=<span class="keyword">new</span> <span class="function"><span class="title">Dog</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//dog1是普通的对象，它的__proto__指向它的构造函数的原型，</span></span><br><span class="line">dog1.__proto__===Dog.prototype <span class="comment">//true</span></span><br><span class="line">Dog.__proto__===<span class="built_in">Function</span>.prototype  <span class="comment">//true 所有的函数对象的原型都是Function.prototype。</span></span><br><span class="line">Dog.prototype.__proto===<span class="built_in">Object</span>.prototype <span class="comment">//true Dog.prototype这个对象的原型就是Object.prototype</span></span><br><span class="line">dog1.__proto__.__proto__===<span class="built_in">Object</span>.prototype <span class="comment">//true 跟上面的表示等价</span></span><br><span class="line">dog1.__proto__.__proto__.__proto__  <span class="comment">//null Object.prototype的原型是null</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//下面的Object、Number、Dog、Function都是构造函数，所有的函数对象的原型都是Function.prototype。</span></span><br><span class="line"><span class="built_in">Object</span>.__proto__===<span class="built_in">Function</span>.prototype   <span class="comment">//true</span></span><br><span class="line"><span class="built_in">Number</span>.__proto__===<span class="built_in">Function</span>.prototype   <span class="comment">//true</span></span><br><span class="line">Dog.__proto__===<span class="built_in">Function</span>.prototype  <span class="comment">//true</span></span><br><span class="line"><span class="built_in">Function</span>.__proto__===<span class="built_in">Function</span>.prototype <span class="comment">//true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//原型`Function.prototype`也是对象，其原型链上游为Object.prototype。</span></span><br><span class="line"><span class="built_in">Function</span>.prototype.__proto__===<span class="built_in">Object</span>.prototype <span class="comment">//true</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.__proto__  <span class="comment">//null</span></span><br></pre></td></tr></table></figure>
<p>回到上面无限循环的例子，对应每一步解释：</p>
<ol>
<li>在console输入<code>Object.prototype</code>时，返回的是所有对象的原型链顶端的Object.prototype`对象，里面有一些方法</li>
<li>点击<code>toString()</code>方法，该方法本质是函数，所有的函数对象的原型都是<code>Function.prototype</code>，即<code>toString().__proto__===Function.prototype</code>，所以点开<code>__proto__</code>就是<code>Function.prototype</code>对象</li>
<li><code>Function.prototype</code>对象也是对象，它的原型就是<code>Object.prototype</code>，即<code>Function.prototype.__proto__===Object.prototype</code>，所以点开<code>__proto__</code>又回到了1,无限循环。</li>
</ol>
]]></content>
      <categories>
        <category>编程语言特性</category>
      </categories>
  </entry>
  <entry>
    <title>Java内部类和嵌套接口</title>
    <url>/2020/11/17/Java%E5%86%85%E9%83%A8%E7%B1%BB%E5%92%8C%E5%B5%8C%E5%A5%97%E6%8E%A5%E5%8F%A3/</url>
    <content><![CDATA[<p>主要介绍内部类，顺便提一下接口中的嵌套接口和嵌套类。</p>
<h2 id="1-内部类"><a href="#1-内部类" class="headerlink" title="1.内部类"></a><strong>1.内部类</strong></h2><p>内部类是定义在其它类中的类。这个定义很宽泛，因为内部类也可以定义在类中的方法里，可以用匿名内部类，还可以声明为public、private、protected、static，不同的情形下，内部类表现也不一致，分别举例说明。</p>
<span id="more"></span>
<h3 id="1-1-public-内部类"><a href="#1-1-public-内部类" class="headerlink" title="1.1 public 内部类"></a><strong>1.1 public 内部类</strong></h3><p>这是最普遍的情形，在一个类的内部定义另一个类，假设两个类分别叫<code>A</code>和<code>InnerA</code>，此时<code>InnerA</code>可以访问A中的属性，其它类也可以通过<code>A</code>的实例来实例化<code>InnerA</code>，因为<code>InnerA</code>是声明为<code>public</code>。为什么要通过<code>A</code>的实例呢？因为<code>InnerA</code>在<code>A</code>中跟其它属性、方法一样，属于“对象属性”,必须通过类的实例才能访问。</p>
<p><code>firstpackage</code></p>
<p>类<code>A</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> firstpackage;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">A</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String propA;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">A</span><span class="params">()</span></span>&#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">A</span><span class="params">(String s)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.propA=s;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        InnerA innerA=<span class="keyword">new</span> InnerA();</span><br><span class="line">        innerA.say();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">InnerA</span></span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;这是InnerA的say()方法，但是可以读取到外部类A的propA属性=====&gt;&quot;</span>+propA);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>secondpackage</code></p>
<p>类<code>TestB</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> secondpackage;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> firstpackage.A;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestB</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        A a=<span class="keyword">new</span> A(<span class="string">&quot;一条来自secondpackage的信息&quot;</span>);</span><br><span class="line">        A.InnerA innerA=a.<span class="function">new <span class="title">InnerA</span><span class="params">()</span></span>;</span><br><span class="line">        innerA.say();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<blockquote>
<p>输出：“这是InnerA的say()方法，但是可以读取到外部类A的propA属性=====&gt;一条来自secondpackage的信息”</p>
</blockquote>
<h3 id="1-2-protected、private内部类"><a href="#1-2-protected、private内部类" class="headerlink" title="1.2 protected、private内部类"></a><strong>1.2 protected、private内部类</strong></h3><p>这两个修饰符只能用在内部类上，普通类只能public或者不加修饰符（表示只能在同一个package内访问）。<br>跟属性、方法的修饰符一样，protected表示包内可以访问(也可以被子类访问，内部类基本不会这么用)，private表示只能在该外部类内部访问到这个内部类，在其它地方通过外部类也无法访问到这个内部类。</p>
<p>可以把上面的例子中的<code>TestB</code>类放到跟类<code>A</code>同一个package下来验证<code>protected</code>的行为或者把上面的例子中的<code>main</code>方法放到类<code>A</code>中来验证<code>private</code>的行为。</p>
<h3 id="1-3-方法中的内部类-local-局部-本地内部类"><a href="#1-3-方法中的内部类-local-局部-本地内部类" class="headerlink" title="1.3 方法中的内部类(local/局部/本地内部类)"></a><strong>1.3 方法中的内部类(local/局部/本地内部类)</strong></h3><p>如果内部类只在方法中用到一次，那么直接把类声明在这个方法中也是可以的。这里并不能用修饰符，因为方法作用域中的内容不能被外部访问，这比<code>private</code>的作用域还要严格。方法中的内部类还可以获取方法参数传递进来的变量，并保存为本地变量。</p>
<p>下面的例子中,通过<code>say(String s)</code>方法传递进来的字符串<code>s</code>，当方法结束后，变量<code>s</code>就消失了，但是<code>InnerA</code>在使用这个变量的时候保存在了本地，后续每一次事件触发时，都能正确的输出这个变量的值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> firstpackage;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionEvent;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionListener;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">A</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception</span>&#123;</span><br><span class="line">        A a=<span class="keyword">new</span> A(<span class="string">&quot;这是构造A的对象时传入的字符串&quot;</span>);</span><br><span class="line">        a.say(<span class="string">&quot;这是通过方法传递的字符串&quot;</span>);</span><br><span class="line">        <span class="comment">//这里只是为了让程序保持运行</span></span><br><span class="line">        Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">        System.exit(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">private</span> String propA;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">A</span><span class="params">()</span></span>&#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">A</span><span class="params">(String s)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.propA=s;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">say</span><span class="params">(String s)</span></span>&#123;</span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">InnerA</span> <span class="keyword">implements</span> <span class="title">ActionListener</span></span>&#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">actionPerformed</span><span class="params">(ActionEvent e)</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;这是内部类，但能获取外部类的属性========&gt;&quot;</span>+propA+<span class="string">&quot;\n还可以获取方法的参数并保存为本地变量========&gt;&quot;</span>+s);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        Timer timer=<span class="keyword">new</span> Timer(<span class="number">1000</span>,<span class="keyword">new</span> InnerA());</span><br><span class="line">        timer.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>输出：</p>
<p>这是内部类，但能获取外部类的属性========&gt;这是构造A的对象时传入的字符串<br>还可以获取方法的参数并保存为本地变量========&gt;这是通过方法传递的字符串</p>
<p>这是内部类，但能获取外部类的属性========&gt;这是构造A的对象时传入的字符串<br>还可以获取方法的参数并保存为本地变量========&gt;这是通过方法传递的字符串</p>
<p>这是内部类，但能获取外部类的属性========&gt;这是构造A的对象时传入的字符串<br>还可以获取方法的参数并保存为本地变量========&gt;这是通过方法传递的字符串</p>
<p>这是内部类，但能获取外部类的属性========&gt;这是构造A的对象时传入的字符串<br>还可以获取方法的参数并保存为本地变量========&gt;这是通过方法传递的字符串</p>
<p>这是内部类，但能获取外部类的属性========&gt;这是构造A的对象时传入的字符串<br>还可以获取方法的参数并保存为本地变量========&gt;这是通过方法传递的字符串</p>
</blockquote>
<h3 id="1-4-匿名内部类"><a href="#1-4-匿名内部类" class="headerlink" title="1.4 匿名内部类"></a><strong>1.4 匿名内部类</strong></h3><p>上面的例子，<code>InnerA</code>只是为了实现<code>ActionListener</code>接口，这种情况下，可以进一步用匿名内部类简化，连类名都不需要，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> firstpackage;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.swing.*;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionEvent;</span><br><span class="line"><span class="keyword">import</span> java.awt.event.ActionListener;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">A</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception</span>&#123;</span><br><span class="line">        A a=<span class="keyword">new</span> A(<span class="string">&quot;这是构造A的对象时传入的字符串&quot;</span>);</span><br><span class="line">        a.say(<span class="string">&quot;这是通过方法传递的字符串&quot;</span>);</span><br><span class="line">        <span class="comment">//这里只是为了让程序保持运行</span></span><br><span class="line">        Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">        System.exit(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">private</span> String propA;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">A</span><span class="params">()</span></span>&#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">A</span><span class="params">(String s)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.propA=s;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">say</span><span class="params">(String s)</span></span>&#123;</span><br><span class="line">        Timer timer=<span class="keyword">new</span> Timer(<span class="number">1000</span>, <span class="keyword">new</span> ActionListener() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">actionPerformed</span><span class="params">(ActionEvent e)</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;这是内部类，但能获取外部类的属性========&gt;&quot;</span>+propA+<span class="string">&quot;\n还可以获取方法的参数并保存为本地变量========&gt;&quot;</span>+s);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        timer.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>后来，<code>java</code>引入<code>lambda</code>表达式进一步简化了这种写法，上面的<code>say()</code>方法可以改为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">say</span><span class="params">(String s)</span></span>&#123;</span><br><span class="line">    Timer timer=<span class="keyword">new</span> Timer(<span class="number">1000</span>, (ActionEvent e)-&gt;&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;这是内部类，但能获取外部类的属性========&gt;&quot;</span>+propA+<span class="string">&quot;\n还可以获取方法的参数并保存为本地变量========&gt;&quot;</span>+s);</span><br><span class="line">    &#125;);</span><br><span class="line">    timer.start();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><span style="color:red"><strong>请注意：<code>lambda</code>表达式只能用于代替实现<code>FunctionalInterface</code>的内部类，就是内部只有一个方法的接口</strong></span></p>
<h3 id="1-5-static内部类"><a href="#1-5-static内部类" class="headerlink" title="1.5 static内部类"></a><strong>1.5 static内部类</strong></h3><p><code>static</code>内部类和别的内部类有几点区别：</p>
<ul>
<li>因为是static,所以使用该类时不需要外部类的实例</li>
<li>无法访问外部类的信息</li>
<li>可以声明<code>static</code>的变量和方法</li>
</ul>
<p><code>static</code>内部类的作用类似package，只是让内部类更深了一层，可以让代码的结构更清晰，减少重名问题等，所以<code>static</code>内部类跟普通的类很像，很多地方称之为”嵌套类”。</p>
<p>这里提一下<code>static</code>和其它内部类实例化的区别：跟类中的<code>static</code>属性一样，在外部类加载完成后，<code>static</code>内部类也加载完成了，所以如果要实例化<code>static</code>内部类，只要用<code>new Outer.Inner()</code>的表达方式就可以了。而普通的非<code>static</code>内部类，因为是定义在类中，属于”对象属性”级别，在外部类加载完成时，这些”对象属性”还不存在，等到外部类实例化时，内部类才作为这个外部类实例对象的”对象属性”加载进来，但此时只是有内部类完成，还没有实例化，需要使用外部类的对象来实例化内部类。</p>
<h2 id="2-接口中的嵌套接口和嵌套类"><a href="#2-接口中的嵌套接口和嵌套类" class="headerlink" title="2.接口中的嵌套接口和嵌套类"></a><strong>2.接口中的嵌套接口和嵌套类</strong></h2><p>接口中的类型成员，包括类和接口，都默认是<code>public staic</code>，也强制只能是<code>public static</code>。</p>
<p>首先，接口不能被实例化，所以，任何非<code>static</code>的内容都毫无意义。至于<code>public</code>，<a href="https://docs.oracle.com/javase/tutorial/java/IandI/interfaceDef.html">JavaSE规范</a>中规定，接口中的类型成员不是<code>public</code>就会在编译时报错。</p>
<p>关于这两个嵌套类型，举个例子，<code>spring boot</code>源码中有一个接口叫<code>DeferredImportSelector</code>，代码如下：</p>
<details>
<summary>显示代码</summary>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.context.annotation;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.core.type.AnnotationMetadata;</span><br><span class="line"><span class="keyword">import</span> org.springframework.lang.Nullable;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DeferredImportSelector</span> <span class="keyword">extends</span> <span class="title">ImportSelector</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Nullable</span></span><br><span class="line">	<span class="keyword">default</span> Class&lt;? extends Group&gt; getImportGroup() &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">interface</span> <span class="title">Group</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">(AnnotationMetadata metadata, DeferredImportSelector selector)</span></span>;</span><br><span class="line"></span><br><span class="line">		<span class="function">Iterable&lt;Entry&gt; <span class="title">selectImports</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">		<span class="class"><span class="keyword">class</span> <span class="title">Entry</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">private</span> <span class="keyword">final</span> AnnotationMetadata metadata;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">private</span> <span class="keyword">final</span> String importClassName;</span><br><span class="line"></span><br><span class="line">			<span class="function"><span class="keyword">public</span> <span class="title">Entry</span><span class="params">(AnnotationMetadata metadata, String importClassName)</span> </span>&#123;</span><br><span class="line">				<span class="keyword">this</span>.metadata = metadata;</span><br><span class="line">				<span class="keyword">this</span>.importClassName = importClassName;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="function"><span class="keyword">public</span> AnnotationMetadata <span class="title">getMetadata</span><span class="params">()</span> </span>&#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">this</span>.metadata;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="function"><span class="keyword">public</span> String <span class="title">getImportClassName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">this</span>.importClassName;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(<span class="meta">@Nullable</span> Object other)</span> </span>&#123;</span><br><span class="line">				<span class="keyword">if</span> (<span class="keyword">this</span> == other) &#123;</span><br><span class="line">					<span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">if</span> (other == <span class="keyword">null</span> || getClass() != other.getClass()) &#123;</span><br><span class="line">					<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				Entry entry = (Entry) other;</span><br><span class="line">				<span class="keyword">return</span> (<span class="keyword">this</span>.metadata.equals(entry.metadata) &amp;&amp; <span class="keyword">this</span>.importClassName.equals(entry.importClassName));</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">				<span class="keyword">return</span> (<span class="keyword">this</span>.metadata.hashCode() * <span class="number">31</span> + <span class="keyword">this</span>.importClassName.hashCode());</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="meta">@Override</span></span><br><span class="line">			<span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">this</span>.importClassName;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</details>

<p><code>DeferredImportSelector</code>接口中有一个抽象方法<code>getImportGroup</code>，返回的是实现<code>Group</code>接口的对象的Class，<code>Group</code>是一个嵌套接口。通过IDE查看实现或者使用<code>Group</code>的地方，都跟<code>DeferredImportSelector</code>有关，所以定义在其内部作为嵌套接口比较直观、合理。虽然两者经常搭配使用，但从语法上来说，实现外部接口和它的嵌套接口没有必然联系，可以只实现其中的一个。</p>
<p><code>Group</code>接口中还包含一个静态内部类<code>Entry</code>。<code>Group</code>接口中有个抽象方法<code>selectImports</code>返回的是<code>Iterable&lt;Entry&gt;</code>,<code>Entry</code>这个名称很普遍，放到内部类里可以避免重名，并且可以通过外部类的名称直观的看出这个类的作用。</p>
<h2 id="3-总结"><a href="#3-总结" class="headerlink" title="3 总结"></a>3 总结</h2><p><code>spring</code>的源码中就经常使用内部类、内部接口等，可以让源码可读性更好，让外部类和内部类直接联系起来。总的来说，内部类和嵌套类、嵌套接口是为了提高程序代码的内聚性而存在的，也是封装思想的一种体现。</p>
]]></content>
      <categories>
        <category>编程语言特性</category>
      </categories>
  </entry>
  <entry>
    <title>Java注解</title>
    <url>/2021/05/07/Java%E6%B3%A8%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="1-Java注解是什么"><a href="#1-Java注解是什么" class="headerlink" title="1 Java注解是什么?"></a>1 Java注解是什么?</h2><p>根据<a href="https://docs.oracle.com/javase/tutorial/java/annotations/">官方文档</a>说明，<code>Java注解</code>是元数据的一种形式，它不直接影响我们代码本身的操作，但是有以下几个用途：</p>
<ul>
<li>给编译器提供信息-编译器可以用注解来检测错误或者抑制编译时的warning信息</li>
<li>编译和部署代码时进一步加工—一些工具可以根据注解生成额外的代码或文件</li>
<li>运行时处理：一些注解可以在运行时被检测<span id="more"></span></li>
</ul>
<h2 id="2-注解的形式和定义"><a href="#2-注解的形式和定义" class="headerlink" title="2 注解的形式和定义"></a>2 注解的形式和定义</h2><p>注解是以<code>@</code>符号开头，后接注解名称，如果有参数的话，后面再用<code>（）</code>把参数写进去，参数都是<code>key=value</code>的形式传递，只有一个参数时默认是对应<code>value</code>这个<em>key</em>，可以不写。<br>如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//无参数</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mySuperMethod</span><span class="params">()</span> </span>&#123; ... &#125;</span><br><span class="line"><span class="comment">//一个参数</span></span><br><span class="line"><span class="meta">@SuppressWarnings(value = &quot;unchecked&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">myMethod</span><span class="params">()</span> </span>&#123; ... &#125;</span><br><span class="line"><span class="comment">//一个参数时可以省略key</span></span><br><span class="line"><span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">myMethod</span><span class="params">()</span> </span>&#123; ... &#125;</span><br></pre></td></tr></table></figure>
<p>如何定义一个注解：<br>注解本质上是<code>interface</code>的一种形式，只是在<code>interface</code>关键字前加了个<code>@</code>符号，如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@interface</span> ClassPreamble &#123;</span><br><span class="line">   <span class="function">String <span class="title">author</span><span class="params">()</span></span>;</span><br><span class="line">   <span class="function">String <span class="title">date</span><span class="params">()</span></span>;</span><br><span class="line">   <span class="function"><span class="keyword">int</span> <span class="title">currentRevision</span><span class="params">()</span> <span class="keyword">default</span> 1</span>;</span><br><span class="line">   <span class="function">String <span class="title">lastModified</span><span class="params">()</span> <span class="keyword">default</span> &quot;N/A&quot;</span>;</span><br><span class="line">   <span class="function">String <span class="title">lastModifiedBy</span><span class="params">()</span> <span class="keyword">default</span> &quot;N/A&quot;</span>;</span><br><span class="line">   <span class="comment">// Note use of array</span></span><br><span class="line">   String[] reviewers();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-Java-SE-API-预定义的注解"><a href="#3-Java-SE-API-预定义的注解" class="headerlink" title="3 Java SE API 预定义的注解"></a>3 Java SE API 预定义的注解</h2><p>预定义的注解有一些是给编译器使用的，另一些是给其它注解使用的(元注解）。</p>
<h3 id="3-1-编译器用的注解"><a href="#3-1-编译器用的注解" class="headerlink" title="3.1 编译器用的注解"></a>3.1 编译器用的注解</h3><p>这些注解的定义本身也有元注解，后面再讲。</p>
<h4 id="Deprecated："><a href="#Deprecated：" class="headerlink" title="@Deprecated："></a>@Deprecated：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(value=&#123;CONSTRUCTOR, FIELD, LOCAL_VARIABLE, METHOD, PACKAGE, MODULE, PARAMETER, TYPE&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Deprecated &#123;</span><br><span class="line">    <span class="function">String <span class="title">since</span><span class="params">()</span> <span class="keyword">default</span> &quot;&quot;</span>;</span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">forRemoval</span><span class="params">()</span> <span class="keyword">default</span> <span class="keyword">false</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>表示一个元素（类、方法或属性）被废弃。语言和框架等一直在更新，一些元素因为安全或者性能原因在新版本中被新的类或方法取代，此时旧的方法不能直接删除（为了保持兼容性），但又不建议使用，所以标记为<code>@Deprecated</code>，当编译器编译时，如果发现你的代码中使用了标记为<code>@Deprecated</code>的元素，就会生成warning</p>
<h4 id="Override："><a href="#Override：" class="headerlink" title="@Override："></a>@Override：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target(ElementType.METHOD)</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.SOURCE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Override &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>表示一个方法是重载父类的方法或者实现接口的方法。主要是避免方法名称拼写错误，如果出现了拼写错误，编译器会报错。现在<em>IDE</em>越来越智能，这类错误很少出现了。</p>
<h4 id="SuppressWarnings："><a href="#SuppressWarnings：" class="headerlink" title="@SuppressWarnings："></a>@SuppressWarnings：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target(&#123;TYPE, FIELD, METHOD, PARAMETER, CONSTRUCTOR, LOCAL_VARIABLE, MODULE&#125;)</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.SOURCE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> SuppressWarnings &#123;</span><br><span class="line">    String[] value();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>让编译器不要显示<code>warning</code>。比如一个方法调用了另一个标记为<code>deprecated</code>的方法，正常编译器会生成<code>warning</code>,用<code>@SuppressWarnings(&quot;deprecation&quot;)</code>可以不让其显示。<code>warning</code>有很多种,比如<code>deprecation</code>和<code>unchecked</code>，可以选择同时阻止多个<code>warning</code>：<code>@SuppressWarnings(&#123;&quot;unchecked&quot;, &quot;deprecation&quot;&#125;)</code></p>
<h4 id="SafeVarargs"><a href="#SafeVarargs" class="headerlink" title="@SafeVarargs"></a>@SafeVarargs</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(&#123;ElementType.CONSTRUCTOR, ElementType.METHOD&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> SafeVarargs &#123;&#125;</span><br></pre></td></tr></table></figure>
<p>1.7引入<br>作用对象：变长参数的【构造函数或者方法】，方法必须是<code>final</code>或者<code>static</code>，java9开始也可以是<code>private</code>,这个限制主要是为了不让方法被重载。如果参数是固定长度的，或者方法的修饰符不满足上面的要求，编译器都会报错。  </p>
<p>效果：（程序员）声明函数体本身不会对变长参数进行潜在的不安全操作,让编译器不生成<code>unchecked warning</code>。</p>
<p>什么是对变长参数的潜在不安全操作呢？简单说明一下：  </p>
<details>
<summary>查看详细内容</summary>

<p>当你的变长参数跟泛型有关（<code>non-reifiable</code>)的时候，就可能会出现类型转换的安全问题。用官方API的例子来解释：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SafeVarargs</span> <span class="comment">// Not actually safe!</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">m</span><span class="params">(List&lt;String&gt;... stringLists)</span> </span>&#123;</span><br><span class="line">  Object[] array = stringLists;</span><br><span class="line">  List&lt;Integer&gt; tmpList = Arrays.asList(<span class="number">42</span>);</span><br><span class="line">  array[<span class="number">0</span>] = tmpList; <span class="comment">// Semantically invalid, but compiles without warnings</span></span><br><span class="line">  String s = stringLists[<span class="number">0</span>].get(<span class="number">0</span>); <span class="comment">// Oh no, ClassCastException at runtime!</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是一个不正确的使用<code>@SafeVarargs</code>注解的例子，因为这段代码会抛出运行时异常。一行行分析：</p>
<p>首先，方法传入了一个变长参数，形式是<code>List&lt;String&gt;... stringLists</code>，变长参数在编译时会转化为<em>数组</em>，所以，这个参数在转化后就变成<code>List&lt;String&gt;[] stringLists</code>,Java不是不支持泛型数组吗？？？事实上，对于可变参数使用泛型的这种形式是支持的，所以它也会导致那些泛型数组会带来的问题。 </p>
<p><code>Object[] array = stringLists;</code><br>然后我们创建了一个<code>Object[]</code>数组变量<code>array</code>,并且初始化为<code>stringLists</code>，这里因为<em>类型擦除</em>，所以在编译后<code>stringLists</code>指向的类型是<code>List&lt;Object&gt;[]</code>。因为数组赋值是传递的<em>数组对象的引用</em>，所以<code>array</code>也是指向的<code>stringLists</code>的地址，也就是<code>List&lt;Object&gt;[]</code>，<strong>这里是伏笔</strong>。</p>
<p><code>List&lt;Integer&gt; tmpList = Arrays.asList(42);</code><br>这一行新建一个<code>List&lt;Integer&gt;</code>,里面只有一个元素42  </p>
<p><code>array[0] = tmpList;</code><br>把上面新建的这个<code>List</code>存到<code>array[0]</code>的位置。<strong>因为前面的类型擦除</strong>，所以这里是把一个<code>List&lt;Integer&gt;</code>对象放到<code>List&lt;Object&gt;[0]</code>里面，编译时没有问题，运行到这一步也没有问题。如果我们的参数<code>stringLists</code>不是泛型，而是普通的类型比如<code>String</code>,修改例子中的相应代码，那么这一步就会报运行时错误<code>ArrayStoreException</code>。</p>
<p><code>String s = stringLists[0].get(0);</code>右边读取的是<code>Integer</code>类型的42，想要转化成<code>String</code>失败，抛出运行时异常，在这一步抛出异常并不清晰，直到使用数组的元素时我们才发现存错了，如果代码复杂一点，会非常难以排查。</p>
</details>

<p>概括一下：实际上，”潜在的不安全操作”有很多种情况，大致都是由于数组和泛型的混用引起。<code>@SafeVarargs</code>可以在方法源头阻止编译器生成<code>unchecked warning</code>，否则的话需要在每个调用方使用<code>@SuppressWarnings</code>来抑制这些<code>warning</code>。使用<code>@SafeVarargs</code>时要注意几点:</p>
<ul>
<li>只有在你真的确定代码是安全的情况下才使用<code>@SafeVarargs</code>，不然会让你的异常更加排查，通常有两个准则需要遵守：<ul>
<li>不直接修改泛型数组变量，只读不改</li>
<li>不对外开放泛型数组变量的引用，本质也是同上</li>
</ul>
</li>
<li>可以考虑用<code>List</code>来替换可变参数，虽然代码会冗长一点，也更安全。</li>
</ul>
<h4 id="FunctionalInterface"><a href="#FunctionalInterface" class="headerlink" title="@FunctionalInterface"></a>@FunctionalInterface</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(ElementType.TYPE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> FunctionalInterface &#123;&#125;</span><br></pre></td></tr></table></figure>
<p>1.8引入<br>表明一个接口是一个函数接口（有且仅有一个抽象方法的接口），如果不满足这个定义，或者这个注解不是被用在接口上，编译器都会报错。<br>事实上，就算没有这个注解，只要接口满足函数接口的要求，编译器也会把它当成函数接口，所以这个注解提供了一个检测功能。</p>
<h4 id="Native"><a href="#Native" class="headerlink" title="@Native"></a>@Native</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Target(ElementType.FIELD)</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.SOURCE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Native &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>1.8引入<br>用在一个定义常量的字段上，表明这个字段的值可能引用<code>native code</code>(比如平台相关的底层库)。一些生成<code>native code</code>头文件的工具通过这个注解来判断是否需要生成相应的头文件，以及在头文件中应该包含哪些声明。这个注解用的少，通常跟<a href="https://en.wikipedia.org/wiki/Java_Native_Interface"><code>JNI</code></a>有关。</p>
<h3 id="3-2-作用在注解上的注解"><a href="#3-2-作用在注解上的注解" class="headerlink" title="3.2 作用在注解上的注解"></a>3.2 作用在注解上的注解</h3><p>下面的这些注解只用在其它注解定义时,被称为<em>元注解</em></p>
<h4 id="Retention"><a href="#Retention" class="headerlink" title="@Retention"></a>@Retention</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(ElementType.ANNOTATION_TYPE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Retention &#123;</span><br><span class="line">    <span class="function">RetentionPolicy <span class="title">value</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>表明注解在哪个阶段(含)之前持续存在,接收一个参数<code>value</code>，类型是<code>RetentionPolicy</code>,这是一个<code>Enum</code>类型，包含3个值，如果没有指定<code>value</code>的值，默认是<code>RetentionPolicy.CLASS</code>：</p>
<ul>
<li><code>RetentionPolicy.SOURCE</code>：表示注解只存在源代码中，在编译器使用后就抛弃，不写入<code>.class</code>文件。上面介绍过的<code>@Override</code>,<code>@SuppressWarnings</code>都是<code>SOURCE</code>级别</li>
<li><code>RetentionPolicy.CLASS</code>：表示注解存在于<code>.class</code>文件中，但虚拟机执行时会抛弃这些注解</li>
<li><code>RetentionPolicy.RUNTIME</code>：表示注解一直存在，包括虚拟机执行时。<code>@SafeVarargs</code>,<code>@FunctonalInterface</code>都是<code>RUNTIME</code>级别，主要是为了可以在运行时通过<em>反射</em>获取注解信息</li>
</ul>
<p><code>CLASS</code>和<code>RUNTIME</code>的区别：<br>两者都在字节码文件可见，区别是<code>CLASS</code>会生成一个<code>RuntimeInvisibleAnnotations</code>，而<code>RUNTIME</code>会生成<code>RuntimeVisibleAnnotations</code>，就是注解是否对虚拟机可见，不过这个行为可以被命令行参数覆盖。<br>通常都直接使用<code>RUNTIME</code>或者<code>SOURCE</code>，<code>CLASS</code>的适用场景并不多</p>
<h4 id="Documented"><a href="#Documented" class="headerlink" title="@Documented"></a>@Documented</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(ElementType.ANNOTATION_TYPE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Documented &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当在一个注解上标记<code>@Documented</code>时，表示所有使用这个注解的对象在生成<em>javadoc</em>时都会显示<strong>这个注解</strong>，默认情况<em>javadoc</em>生成时是不包含注解的。如果一个对象有多个注解，那么生成<em>javadoc</em>时是否显示这些注解是根据每一个注解自身是否使用了<code>@Documented</code>分别决定的。</p>
<h4 id="Target"><a href="#Target" class="headerlink" title="@Target"></a>@Target</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(ElementType.ANNOTATION_TYPE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Target &#123;</span><br><span class="line">    ElementType[] value();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>表示注解可以作用的对象。接收一个<code>ElementType[]</code>，<code>ElementType</code>是一个<code>Enum</code>类型，可以有如下取值（基于jdk11)：</p>
<ul>
<li><code>ElementType.TYPE</code>：所有<code>class,inteface,enum</code>类型的声明，<strong>其中<code>inteface</code>也包括了注解</strong></li>
<li><code>ElementType.FIELD</code>：字段的声明，包括<code>Enum</code>里面的常量</li>
<li><code>ElementType.METHOD</code>：方法的声明</li>
<li><code>ElementType.PARAMETER</code>：形参的声明</li>
<li><code>ElementType.CONSTRUCTOR</code>：构造函数的声明</li>
<li><code>ElementType.LOCAL_VARIABLE</code>：本地变量的声明</li>
<li><code>ElementType.ANNOTATION_TYPE</code>：注解的声明</li>
<li><code>ElementType.PACKAGE</code>：package的声明</li>
<li><code>ElementType.TYPE_PARAMETER</code>：1.8引入，类型参数的声明,如<code>public &lt;@MyAnnotation T&gt; T retT(T t)&#123; return t; &#125;</code></li>
<li><code>ElementType.TYPE_USE</code>：1.8引入，<code>TYPE</code>可以用的地方它都可以用。在1.8之前，注解只能用在“声明”的时候，有了<code>TYPE_USE</code>，可以在任何跟<code>Type</code>有关的地方使用它。官方提供了以下几个例子：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class实例创建时:</span><br><span class="line">    <span class="keyword">new</span> <span class="meta">@Interned</span> MyObject();</span><br><span class="line"></span><br><span class="line">类型转换时:</span><br><span class="line">    myString = (<span class="meta">@NonNull</span> String) str;</span><br><span class="line"></span><br><span class="line">implements语句里:</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">UnmodifiableList</span>&lt;<span class="title">T</span>&gt; <span class="keyword">implements</span></span></span><br><span class="line"><span class="class">        @<span class="title">Readonly</span> <span class="title">List</span>&lt;@<span class="title">Readonly</span> <span class="title">T</span>&gt; </span>&#123; ... &#125;</span><br><span class="line"></span><br><span class="line">Thrown exception的声明语句里:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">monitorTemperature</span><span class="params">()</span> <span class="keyword">throws</span></span></span><br><span class="line"><span class="function">        @Critical TemperatureException </span>&#123; ... &#125;</span><br></pre></td></tr></table></figure></li>
<li><code>ElementType.MODULE</code>：1.8引入，module的声明</li>
</ul>
<h4 id="Inherited"><a href="#Inherited" class="headerlink" title="@Inherited"></a>@Inherited</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(ElementType.ANNOTATION_TYPE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Inherited &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果一个注解被标记为<code>@Inherited</code>,那么使用了这个注解的类在被继承的时候，类上的注解也会一起被“继承”。这里注解的“继承”表现为：在子类上可以通过反射获取到自身没有但父类有的注解。<br>一个简单的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//定义一个注解，标记为@Inherited</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Target(ElementType.TYPE)</span></span><br><span class="line"><span class="meta">@Inherited</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyAnnotation &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">//测试类</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">      <span class="comment">//从子类获取注解信息</span></span><br><span class="line">        System.out.println(SubTest.class.isAnnotationPresent(MyAnnotation.class));<span class="comment">//true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//父类使用了注解</span></span><br><span class="line"><span class="meta">@MyAnnotation</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ParentTest</span></span>&#123;&#125;</span><br><span class="line"><span class="comment">//子类没有使用注解</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SubTest</span> <span class="keyword">extends</span> <span class="title">ParentTest</span></span>&#123;&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p><strong>注意：<code>@Inherited</code>只在<code>class</code>上生效，<code>interface</code>无效。</strong></p>
<h4 id="Repeatable"><a href="#Repeatable" class="headerlink" title="@Repeatable"></a>@Repeatable</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@Target(ElementType.ANNOTATION_TYPE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Repeatable &#123;</span><br><span class="line">    Class&lt;? extends Annotation&gt; value();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>1.8引入。<br>表示一个注解可以在同一个地方被使用多次。接收一个参数<code>Class&lt;? extends Annotation&gt; value()</code>,这是一个注解类型的<code>Class</code>，要解释这个，先从没有<code>@Repeatable</code>的版本说起。<br>在1.7版本，同一个注解不能在一个地方出现两次，为了实现类似功能，需要用另一个注解的数组参数把两个相同的注解“封装”起来。比如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Schedule注解</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Schedule &#123;</span><br><span class="line">    <span class="function">String <span class="title">dayOfMonth</span><span class="params">()</span> <span class="keyword">default</span> &quot;first&quot;</span>;</span><br><span class="line">    <span class="function">String <span class="title">dayOfWeek</span><span class="params">()</span> <span class="keyword">default</span> &quot;Mon&quot;</span>;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">hour</span><span class="params">()</span> <span class="keyword">default</span> 12</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//Schedules注解，接收一个参数@Schedule[]，注意这里声明变量时Schedule前不加@</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Schedules &#123;</span><br><span class="line">    Schedule[] value();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//调用@Schedules,间接调用多个@Schedule</span></span><br><span class="line"><span class="meta">@Schedules(value = &#123;@Schedule(dayOfMonth = &quot;last&quot;),@Schedule(dayOfWeek = &quot;Fri&quot;,hour =23)&#125;)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSth</span><span class="params">()</span></span>&#123;&#125;</span><br></pre></td></tr></table></figure>
<p>1.8以后上面的代码可以写成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Repeatable(Schedules.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Schedule &#123;</span><br><span class="line">    <span class="function">String <span class="title">dayOfMonth</span><span class="params">()</span> <span class="keyword">default</span> &quot;first&quot;</span>;</span><br><span class="line">    <span class="function">String <span class="title">dayOfWeek</span><span class="params">()</span> <span class="keyword">default</span> &quot;Mon&quot;</span>;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">hour</span><span class="params">()</span> <span class="keyword">default</span> 12</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Schedules &#123;</span><br><span class="line">    Schedule[] value();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Schedule(dayOfMonth = &quot;last&quot;)</span></span><br><span class="line"><span class="meta">@Schedule(dayOfWeek = &quot;Fri&quot;,hour =23)</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSth</span><span class="params">()</span></span>&#123;&#125;</span><br></pre></td></tr></table></figure>
<p>对比可以发现，依然需要3个步骤，不同的是<code>@Schedule</code>定义的时候多了一个<code>@Repeatable(Schedules.class)</code>注解，这一步是指明我们用了哪一个注解来包装我们的<code>@Schedule</code>，这里是<code>@Schedules</code>，所以传入<code>Schedules.class</code>，然后我们就可以多次使用<code>@Schedule</code>了，<strong>看上去repeatable</strong>。<br>实际上，这只是语法糖，通过编译后的class文件我们可以发现，两个版本都有如下<strong>相同</strong>内容：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">RuntimeInvisibleAnnotations:</span><br><span class="line">  <span class="number">0</span>: #<span class="number">17</span>(#<span class="number">18</span>=[@#<span class="number">19</span>(#<span class="number">20</span>=s#<span class="number">21</span>),@#<span class="number">19</span>(#<span class="number">22</span>=s#<span class="number">23</span>,#<span class="number">24</span>=I#<span class="number">25</span>)])</span><br><span class="line">    Schedules(</span><br><span class="line">      value=[<span class="meta">@Schedule(</span></span><br><span class="line"><span class="meta">        dayOfMonth=&quot;last&quot;</span></span><br><span class="line"><span class="meta">      )</span>,<span class="meta">@Schedule(</span></span><br><span class="line"><span class="meta">        dayOfWeek=&quot;Fri&quot;</span></span><br><span class="line"><span class="meta">        hour=23</span></span><br><span class="line"><span class="meta">      )</span>]</span><br><span class="line">    )</span><br></pre></td></tr></table></figure>
<p>这个形式跟我们在1.7版本没有引入<code>@Repeatable</code>之前是一样的</p>
<h2 id="4-关于自定义注解"><a href="#4-关于自定义注解" class="headerlink" title="4 关于自定义注解"></a>4 关于自定义注解</h2><p>前面介绍的JAVA原生支持的注解，直接使用就有效果。自定义注解不一样，你直接使用一个自定义注解的时候，编译器和虚拟机无法理解你想表达什么，你需要自己写”注解解析器”，根据注解信息执行相应操作，而注解信息则通过反射获取，所以自定义注解通常要标记为<code>@Retention(RetentionPolicy.RUNTIME)</code>。<code>Spring</code>等框架就大量使用了自定义注解。</p>
]]></content>
      <categories>
        <category>编程语言特性</category>
      </categories>
  </entry>
  <entry>
    <title>Vue中的大小写</title>
    <url>/2020/08/16/Vue%E4%B8%AD%E7%9A%84%E5%A4%A7%E5%B0%8F%E5%86%99/</url>
    <content><![CDATA[<p>命名方法主要分三种,效果如其名称:</p>
<p>camelCase</p>
<p>kebab-case</p>
<p>PascalCase</p>
<p>首先，DOM模板内部元素的属性名称是不区分大小写的，比如以下三种表示是等价的：</p>
<span id="more"></span>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;p style=&quot;color:red&quot;&gt;红色&lt;/p&gt;</span><br><span class="line">&lt;p sTYLe=&quot;color:red&quot;&gt;红色&lt;/p&gt;</span><br><span class="line">&lt;p STYLE=&quot;color:red&quot;&gt;红色&lt;/p&gt;</span><br></pre></td></tr></table></figure>
<p>但在vue.js中，是大小写敏感的，所以部分名称在vue.js中定义的时候采用camelCase,PascalCase方式命名，而在dom模板内使用的时候，可以采用相应的kebab-case名称，当然前提是命名要规范，比如”myComPoNent”肯定无法与”my-component”对应。</p>
<p style="color:red">使用字符串模板时，以下规则会有例外，用红字标出。</p>

<ul>
<li><p>组件名称</p>
<ul>
<li><p>在<span style="font-weight:bold">单文件组件</span>和<span style="font-weight:bold">字符串模板</span>中，组件名应该总是<strong>PascalCase</strong>,在DOM模板中总是<strong>kebab-case</strong>。<br>  使用kebab-case时，引用组件的时候只能用kebab-case。<br>  使用PascalCase时，引用组件的时候可以使用kebab-case或PascalCase，但只是引用，在DOM中（<span style="color:red">使用字符串模板时例外</span>)使用时只能用kebab-case。</p>
<p>  尽管全部采用kebab-case也可以，但是采用PascalCase有如下优势：</p>
<blockquote>
<ul>
<li>编辑器可以在模板里自动补全组件名，因为 PascalCase 同样适用于 JavaScript。</li>
<li>&lt;MyComponent&gt; 视觉上比 &lt;my-component&gt; 更能够和单个单词的 HTML 元素区别开来，因为前者的不同之处有两个大写字母，后者只有一个横线。</li>
<li>如果你在模板中使用任何非 Vue 的自定义元素，比如一个 Web Component，PascalCase 确保了你的 Vue 组件在视觉上仍然是易识别的。</li>
</ul>
</blockquote>
</li>
<li><p><span style="font-weight:bold">JS/JSX</span>中的组件名通常用PascalCase。但是如果你的应用只通过Vue.component定义全局组件，则建议使用kebab-case。官方原文解释：</p>
<blockquote>
<p>在 JavaScript 中，PascalCase 是类和构造函数 (本质上任何可以产生多份不同实例的东西) 的命名约定。Vue 组件也有多份实例，所以同样使用 PascalCase 是有意义的。额外的好处是，在 JSX (和模板) 里使用 PascalCase 使得代码的读者更容易分辨 Vue 组件和 HTML 元素。</p>
<p>然而，对于只通过 Vue.component 定义全局组件的应用来说，我们推荐 kebab-case 作为替代。原因是：</p>
<ul>
<li>全局组件很少被 JavaScript 引用，所以遵守 JavaScript 的命名约定意义不大。</li>
<li>这些应用往往包含许多 DOM 内的模板，这种情况下是必须使用 kebab-case 的。</li>
</ul>
</blockquote>
</li>
</ul>
</li>
<li><p>单文件组件的<span style="font-weight:bold;color:red">文件名</span></p>
<p>  要么全部用kebab-case,用么全部用PascalCase,保持统一</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">components/</span><br><span class="line">|- MyComponent.vue</span><br><span class="line"></span><br><span class="line">components/</span><br><span class="line">|- my-component.vue</span><br></pre></td></tr></table></figure></li>
<li><p>事件名称</p>
<p>  始终使用kebab-case。官方文档说明：</p>
<blockquote>
<p>不同于组件和 prop，事件名不存在任何自动化的大小写转换。而是触发的事件名需要完全匹配监听这个事件所用的名称。</p>
</blockquote>
</li>
<li><p>Prop名称（组件（component）中的props)）</p>
<p>  如果在props定义时使用了camelCase,则在html中使用该属性的时候，需要手动改成kebab-case。<span style="color:red">使用字符串模板时例外。</span></p>
<p>  官方风格指南建议在命名时采用camelCase,而在模板和JSX中使用kebab-case</p>
  <figure class="highlight js"><table><tr><td class="code"><pre><span class="line">Vue.component(<span class="string">&#x27;blog-post&#x27;</span>, &#123;</span><br><span class="line"><span class="comment">// 在 JavaScript 中是 camelCase 的</span></span><br><span class="line"><span class="attr">props</span>: [<span class="string">&#x27;postTitle&#x27;</span>],</span><br><span class="line"><span class="attr">template</span>: <span class="string">&#x27;&lt;h3&gt;&#123;&#123; postTitle &#125;&#125;&lt;/h3&gt;&#x27;</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&lt;!-- 在 HTML 中是 kebab-<span class="keyword">case</span> 的 --&gt;</span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">blog-post</span> <span class="attr">post-title</span>=<span class="string">&quot;hello!&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">blog-post</span>&gt;</span></span></span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>web</category>
        <category>前端</category>
      </categories>
  </entry>
  <entry>
    <title>Vue生命周期勾子</title>
    <url>/2020/08/16/Vue%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E5%8B%BE%E5%AD%90/</url>
    <content><![CDATA[<p>官方原图：<br><img src="https://cn.vuejs.org/images/lifecycle.png" alt="img"><br>上图中红色部分为常用的<strong>实例生命周期钩子</strong>，可以用在vue实例运行的各个阶段，按大概时间线：</p>
<span id="more"></span>

<p>先在例子中定义html元素：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;app&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span> <span class="attr">id</span>=<span class="string">&quot;myp&quot;</span>&gt;</span>hehe&#123;&#123;a&#125;&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p><span style="color:red;font-weight:bold">beforeCreate</span></p>
<p>在vue实例创建前调用，此时<strong>属性</strong>、<strong>方法</strong>都无法访问。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">    <span class="attr">el</span>: <span class="string">&quot;#app&quot;</span>,</span><br><span class="line">    <span class="attr">data</span>:&#123;</span><br><span class="line">        <span class="attr">a</span>:<span class="string">&quot;a&quot;</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">methods</span>:&#123;</span><br><span class="line">        <span class="function"><span class="title">hi</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="string">&quot;hi&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="function"><span class="title">beforeCreate</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">&quot;beforeCreate:a=&quot;</span>+<span class="built_in">this</span>.a);<span class="comment">//beforeCreate:a=undefined</span></span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">&quot;beforeCreate:hi()=&quot;</span>);<span class="comment">//beforeCreate:hi()=</span></span><br><span class="line">        <span class="built_in">this</span>.hi();<span class="comment">//报错</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p><span style="color:red;font-weight:bold">created</span></p>
<p>在vue实例创建后，此时实例内定义的<strong>属性</strong>和<strong>方法</strong>都可以访问到，但还没有进行渲染,无法访问<code>$el</code>属性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  <span class="attr">el</span>: <span class="string">&quot;#app&quot;</span>,</span><br><span class="line">  <span class="attr">data</span>:&#123;</span><br><span class="line">      <span class="attr">a</span>:<span class="string">&quot;aValue&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">methods</span>:&#123;</span><br><span class="line">      <span class="function"><span class="title">hi</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">          <span class="built_in">console</span>.log(<span class="string">&quot;hi&quot;</span>);</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">created</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&quot;created:a=&quot;</span>+<span class="built_in">this</span>.a);<span class="comment">//created:a=aValue</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&quot;created:hi()=&quot;</span>+<span class="built_in">this</span>.hi());<span class="comment">//created:hi()=</span></span><br><span class="line">      <span class="built_in">this</span>.hi();<span class="comment">//hi</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">document</span>.getElementById(<span class="string">&quot;myp&quot;</span>).innerText);<span class="comment">//hehe&#123;&#123;a&#125;&#125;</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">this</span>.$el);<span class="comment">//undefined</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p><span style="color:red;font-weight:bold">beforeMount</span></p>
<p>到这一步已经把<code>template</code>编译成了<code>render</code>，此时可以访问到<code>$el</code>的内容,但还没有挂载到页面(替换掉网页中的模板),</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  <span class="attr">el</span>: <span class="string">&quot;#app&quot;</span>,</span><br><span class="line">  <span class="attr">data</span>:&#123;</span><br><span class="line">      <span class="attr">a</span>:<span class="string">&quot;aValue&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">beforeMount</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">this</span>.$el);<span class="comment">//div#app，展开以后有很多属性</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">document</span>.getElementById(<span class="string">&quot;myp&quot;</span>).innerText);<span class="comment">//hehe&#123;&#123;a&#125;&#125;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p><span style="color:red;font-weight:bold">mounted</span></p>
<p>此时渲染好的内容已经挂载到网页上，所有创建工作完成</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  <span class="attr">el</span>: <span class="string">&quot;#app&quot;</span>,</span><br><span class="line">  <span class="attr">data</span>:&#123;</span><br><span class="line">      <span class="attr">a</span>:<span class="string">&quot;aValue&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">mounted</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">document</span>.getElementById(<span class="string">&quot;myp&quot;</span>).innerText);<span class="comment">//heheaValue</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p><span style="color:red;font-weight:bold">beforeUpdate</span></p>
<p>这个状态对应<strong>数据已经变更，但页面没有重新渲染之前</strong>,可以在虚拟dom中访问更新之前的dom元素值，但此时变量已经为新值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">    <span class="attr">el</span>: <span class="string">&quot;#app&quot;</span>,</span><br><span class="line">    <span class="attr">data</span>:&#123;</span><br><span class="line">        <span class="attr">a</span>:<span class="string">&quot;aValue&quot;</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="function"><span class="title">beforeUpdate</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="comment">//在浏览器vue devtool中修改a的值为newvalue</span></span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">&quot;beforeUpdate:a=&quot;</span>+<span class="built_in">this</span>.a);<span class="comment">//beforeUpdate:a=newvalue</span></span><br><span class="line">        <span class="comment">//beforeUpdate:a-dom=heheaValue</span></span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">&quot;beforeUpdate:a-dom=&quot;</span>+<span class="built_in">document</span>.getElementById(<span class="string">&quot;myp&quot;</span>).innerText);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p><span style="color:red;font-weight:bold">updated</span></p>
<p>此时更新后虚拟dom已经重新渲染,官方文档建议不要在此期间更改状态，用<strong>计算属性</strong>或<strong>watcher</strong>取代。</p>
<p>如下面的例子，在update()函数内部加入<code>this.$data.a=this.$data.a+&quot;a&quot;</code>,会导致页面无限循环渲染。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  <span class="attr">el</span>: <span class="string">&quot;#app&quot;</span>,</span><br><span class="line">  <span class="attr">data</span>:&#123;</span><br><span class="line">      <span class="attr">a</span>:<span class="string">&quot;aValue&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">updated</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">      <span class="comment">//在浏览器vue devtool中修改a的值为newvalue</span></span><br><span class="line">      <span class="comment">//bupdated:a-dom=hehenewvalue</span></span><br><span class="line">      <span class="comment">//this.$data.a=this.$data.a+&quot;a&quot;//加入此行会无限循环渲染</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&quot;updated:a-dom=&quot;</span>+<span class="built_in">document</span>.getElementById(<span class="string">&quot;myp&quot;</span>).innerText);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p><span style="color:red;font-weight:bold">beforeDestroy</span>和<span style="color:red;font-weight:bold">destroyed</span></p>
<p>beforeDestroyed这个状态下可以访问vue实例，所有跟销毁有关的操作应该在这里完成。</p>
<p>destroyed这个状态下仍然可以访问虚拟dom，但实例已经完全销毁，可以看源码<code>*/src/core/instance/lifecyle.js</code>得知，触发<code>$destroy</code>时，先执行<code>beforeDestroy</code>勾子，再执行具体销毁过程，最后调用<code>destroyed</code>勾子，也就是说，在destroyed勾子里对实例进行任何操作都没有意义了。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>web</category>
        <category>前端</category>
      </categories>
  </entry>
  <entry>
    <title>Windows用Docker部署Redis集群</title>
    <url>/2021/04/14/Windows%E7%94%A8Docker%E9%83%A8%E7%BD%B2Redis%E9%9B%86%E7%BE%A4/</url>
    <content><![CDATA[<h2 id="1-预备知识"><a href="#1-预备知识" class="headerlink" title="1 预备知识"></a>1 预备知识</h2><p>除了知道<code>Docker</code>和<code>Redis cluster</code>的基本概念和常规部署流程外，还需要知道<code>Docker</code>里的几个概念：</p>
<span id="more"></span>

<ul>
<li><a href="https://docs.docker.com/storage/volumes/">Volume</a>：独立于<code>container</code>的一个数据保存区域，可以被<code>container</code>共享，也可以用来把本地的配置文件映射过去供<code>container</code>使用。</li>
<li><a href="https://docs.docker.com/network/bridge/">Network</a>：<code>Docker</code>内部的”网络”，让不同的<code>container</code>可以互相访问，这是<code>redis</code>官方建议的在<code>docker</code>里做<code>cluster</code>时的方式。我们这里主要用<code>bridge</code>模式。</li>
</ul>
<h2 id="2-基本步骤"><a href="#2-基本步骤" class="headerlink" title="2 基本步骤"></a>2 基本步骤</h2><h3 id="2-1-获取redis镜像"><a href="#2-1-获取redis镜像" class="headerlink" title="2.1 获取redis镜像"></a>2.1 获取redis镜像</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker pull redis</span><br></pre></td></tr></table></figure>

<h3 id="2-2-在本机创建配置文件"><a href="#2-2-在本机创建配置文件" class="headerlink" title="2.2 在本机创建配置文件"></a>2.2 在本机创建配置文件</h3><p>在本地的一个文件夹内创建一个<code>redis-cluster.conf</code>配置文件，里面仅包含简单的集群配置信息：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">port 6379</span><br><span class="line">cluster-enabled yes</span><br><span class="line">cluster-config-file nodes.conf</span><br><span class="line">cluster-node-timeout 5000</span><br><span class="line">appendonly yes</span><br></pre></td></tr></table></figure>

<h3 id="2-3-创建一个Docker-Network"><a href="#2-3-创建一个Docker-Network" class="headerlink" title="2.3 创建一个Docker Network"></a>2.3 创建一个<code>Docker Network</code></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker network create my-net</span><br></pre></td></tr></table></figure>

<h3 id="2-4-用本地配置文件启动多个redis节点"><a href="#2-4-用本地配置文件启动多个redis节点" class="headerlink" title="2.4 用本地配置文件启动多个redis节点"></a>2.4 用本地配置文件启动多个redis节点</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker run -d --name redis-1 --network my-net -v C:\Users\xxx\Desktop\conf\redis-cluster.conf:/usr/local/etc/redis/redis.conf redis redis-server /usr/local/etc/redis/redis.conf</span><br></pre></td></tr></table></figure>

<p>上面这条命令里的几个参数：</p>
<ul>
<li>docker run：运行一个<code>container</code></li>
<li>-d:后台运行</li>
<li><code>--name</code>：自定义的container的名称</li>
<li><code>--network</code>:容器加入的网络</li>
<li>-v:volume映射,“本机地址：docker地址”的格式，”C:\Users\xxx\Desktop\conf\redis-cluster.conf”是刚才存放的配置文件地址（加文件名），”/usr/local/etc/redis/redis.conf”是我们的<code>volume</code>挂载的地址,这里我们把配置文件的名称也改掉了，本机是<code>redis-cluster.conf</code>，<code>volume</code>里是<code>redis.conf</code></li>
<li>redis：我们的<code>image</code>名称，默认用最新版</li>
<li>redis-server /usr/local/etc/redis/redis.conf:这跟我们常规启动<code>redis</code>服务是一样的，用我们刚才映射过去的配置文件启动。</li>
</ul>
<p>把上面的”redis-1”换成”redis-2”…”redis-6”，启动6个节点，注意这里配置文件不用改，端口也全是6379,因为<code>network</code>会给我们每一个节点分配不同的IP。</p>
<p>完成后查看一下容器：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker ps</span><br></pre></td></tr></table></figure>

<details>
<summary>查看输出</summary>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONTAINER ID   IMAGE     COMMAND                  CREATED          STATUS          PORTS      NAMES</span><br><span class="line"></span><br><span class="line">cd6d8811ce44   redis     &quot;docker-entrypoint.s…&quot;   2 seconds ago    Up 1 second     6379/tcp   redis-6</span><br><span class="line"></span><br><span class="line">f67c48b6ea9d   redis     &quot;docker-entrypoint.s…&quot;   8 seconds ago    Up 7 seconds    6379/tcp   redis-5</span><br><span class="line"></span><br><span class="line">4fe4a89469da   redis     &quot;docker-entrypoint.s…&quot;   13 seconds ago   Up 12 seconds   6379/tcp   redis-4</span><br><span class="line"></span><br><span class="line">950537cd501e   redis     &quot;docker-entrypoint.s…&quot;   21 seconds ago   Up 20 seconds   6379/tcp   redis-3</span><br><span class="line"></span><br><span class="line">4d09c04e87b1   redis     &quot;docker-entrypoint.s…&quot;   34 seconds ago   Up 32 seconds   6379/tcp   redis-2</span><br><span class="line"></span><br><span class="line">d15c0933bc05   redis     &quot;docker-entrypoint.s…&quot;   12 minutes ago   Up 12 minutes   6379/tcp   redis-1</span><br></pre></td></tr></table></figure>
</details>


<p>再查看一下network:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker network inspect my-net</span><br></pre></td></tr></table></figure>

<p>可以看到我们的6个<code>container</code>都在里面，每个分配了不同的ID</p>
<details>
<summary>查看输出</summary> 

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">       &quot;Name&quot;: &quot;my-net&quot;,</span><br><span class="line">       &quot;Id&quot;: &quot;8fa94b07701cad47c4440298b2908fbadd0591b44999df2013a7e1eb4b7b541b&quot;,</span><br><span class="line">       &quot;Created&quot;: &quot;2021-04-21T05:46:18.4468198Z&quot;,</span><br><span class="line">       &quot;Scope&quot;: &quot;local&quot;,</span><br><span class="line">       &quot;Driver&quot;: &quot;bridge&quot;,</span><br><span class="line">       &quot;EnableIPv6&quot;: false,</span><br><span class="line">       &quot;IPAM&quot;: &#123;</span><br><span class="line">           &quot;Driver&quot;: &quot;default&quot;,</span><br><span class="line">           &quot;Options&quot;: &#123;&#125;,</span><br><span class="line">           &quot;Config&quot;: [</span><br><span class="line">               &#123;</span><br><span class="line">                   &quot;Subnet&quot;: &quot;172.18.0.0/16&quot;,</span><br><span class="line">                   &quot;Gateway&quot;: &quot;172.18.0.1&quot;</span><br><span class="line">               &#125;</span><br><span class="line">           ]</span><br><span class="line">       &#125;,</span><br><span class="line">       &quot;Internal&quot;: false,</span><br><span class="line">       &quot;Attachable&quot;: false,</span><br><span class="line">       &quot;Ingress&quot;: false,</span><br><span class="line">       &quot;ConfigFrom&quot;: &#123;</span><br><span class="line">           &quot;Network&quot;: &quot;&quot;</span><br><span class="line">       &#125;,</span><br><span class="line">       &quot;ConfigOnly&quot;: false,</span><br><span class="line">       &quot;Containers&quot;: &#123;</span><br><span class="line">           &quot;4d09c04e87b1b99ebe7827181ce89f8bddd054b047ae24a4179d37ff0f98d271&quot;: &#123;</span><br><span class="line">               &quot;Name&quot;: &quot;redis-2&quot;,</span><br><span class="line">               &quot;EndpointID&quot;: &quot;46faad0fc995299d176f941dd97e06b5a35f16a5a2b4b5d66f3ccf624363cebb&quot;,</span><br><span class="line">               &quot;MacAddress&quot;: &quot;02:42:ac:12:00:03&quot;,</span><br><span class="line">               &quot;IPv4Address&quot;: &quot;172.18.0.3/16&quot;,</span><br><span class="line">               &quot;IPv6Address&quot;: &quot;&quot;</span><br><span class="line">           &#125;,</span><br><span class="line">           &quot;4fe4a89469da15e772d8a1437698b5832bc95310ebc59a18fb4860ec93c86595&quot;: &#123;</span><br><span class="line">               &quot;Name&quot;: &quot;redis-4&quot;,</span><br><span class="line">               &quot;EndpointID&quot;: &quot;c2a84c3c7e8db91f4f694f10b390c1a78f702f2b65e71eeecbc51dc2288e28be&quot;,</span><br><span class="line">               &quot;MacAddress&quot;: &quot;02:42:ac:12:00:05&quot;,</span><br><span class="line">               &quot;IPv4Address&quot;: &quot;172.18.0.5/16&quot;,</span><br><span class="line">               &quot;IPv6Address&quot;: &quot;&quot;</span><br><span class="line">           &#125;,</span><br><span class="line">           &quot;950537cd501eb2731dbd69890fd30c933f3246d2d85fca6cc51818b0c3fc5b31&quot;: &#123;</span><br><span class="line">               &quot;Name&quot;: &quot;redis-3&quot;,</span><br><span class="line">               &quot;EndpointID&quot;: &quot;1bfe934156ff36b05a4c44c3695b02cef2a50f6a5fd3a75f52c03e3c900cbb7c&quot;,</span><br><span class="line">               &quot;MacAddress&quot;: &quot;02:42:ac:12:00:04&quot;,</span><br><span class="line">               &quot;IPv4Address&quot;: &quot;172.18.0.4/16&quot;,</span><br><span class="line">               &quot;IPv6Address&quot;: &quot;&quot;</span><br><span class="line">           &#125;,</span><br><span class="line">           &quot;cd6d8811ce443f98a504e8466d2cd321f8129eae06b03b7667e4c145a308a880&quot;: &#123;</span><br><span class="line">               &quot;Name&quot;: &quot;redis-6&quot;,</span><br><span class="line">               &quot;EndpointID&quot;: &quot;d0b9d092e6c38433d760f16781bd3cd2100b8f9c1b7f37f5c499e172fe0845d5&quot;,</span><br><span class="line">               &quot;MacAddress&quot;: &quot;02:42:ac:12:00:07&quot;,</span><br><span class="line">               &quot;IPv4Address&quot;: &quot;172.18.0.7/16&quot;,</span><br><span class="line">               &quot;IPv6Address&quot;: &quot;&quot;</span><br><span class="line">           &#125;,</span><br><span class="line">           &quot;d15c0933bc05450410f4c1923e70493f1fd79b200c16341be0b5cda4c599bbfa&quot;: &#123;</span><br><span class="line">               &quot;Name&quot;: &quot;redis-1&quot;,</span><br><span class="line">               &quot;EndpointID&quot;: &quot;7262111112b513c1d4c3e090fc4ae7b01bbc6adf0df337eb38fa311b04d92b45&quot;,</span><br><span class="line">               &quot;MacAddress&quot;: &quot;02:42:ac:12:00:02&quot;,</span><br><span class="line">               &quot;IPv4Address&quot;: &quot;172.18.0.2/16&quot;,</span><br><span class="line">               &quot;IPv6Address&quot;: &quot;&quot;</span><br><span class="line">           &#125;,</span><br><span class="line">           &quot;f67c48b6ea9d8fb31ba8b1ec96ad6ea8cc1c246747608420814e79f59abfd56c&quot;: &#123;</span><br><span class="line">               &quot;Name&quot;: &quot;redis-5&quot;,</span><br><span class="line">               &quot;EndpointID&quot;: &quot;a86deeae79f267f21e55e7ee06da7d12a627617e348398125c6303a12439cbdc&quot;,</span><br><span class="line">               &quot;MacAddress&quot;: &quot;02:42:ac:12:00:06&quot;,</span><br><span class="line">               &quot;IPv4Address&quot;: &quot;172.18.0.6/16&quot;,</span><br><span class="line">               &quot;IPv6Address&quot;: &quot;&quot;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;,</span><br><span class="line">       &quot;Options&quot;: &#123;&#125;,</span><br><span class="line">       &quot;Labels&quot;: &#123;&#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
</details>

<p>上面可以看到我们每一个<code>container</code>的IP，也可以用下面这条命令单独获取某个<code>container</code>的IP,比如获取redis-1这个<code>container</code>的IP:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker inspect --format=&#x27;&#123;&#123;range .NetworkSettings.Networks&#125;&#125;&#123;&#123;.IPAddress&#125;&#125;&#123;&#123;end&#125;&#125;&#x27; $INSTANCE_ID</span><br></pre></td></tr></table></figure>

<p>2.5 启动cluster</p>
<p>根据上面获取的IP，我们先进入redis1这个container</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker exec -it redis-1 /bin/bash</span><br></pre></td></tr></table></figure>

<p>然后跟常规一样运行cluster</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">redis-cli --cluster create 172.18.0.2:6379 172.18.0.3:6379 172.18.0.4:6379 172.18.0.5:6379 172.18.0.6:6379 172.18.0.7:6379 --cluster-replicas 1</span><br></pre></td></tr></table></figure>

<p>redis会自动分配slot，输入yes完成cluster。</p>
<p>完成以后，我们以cluster模式启动（还是在redis-1这个container里面）:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">redis -c -p 6379</span><br></pre></td></tr></table></figure>

<p>测试下面4条语句可以观察到cluster自动在节点间跳转</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">set foo bar</span><br><span class="line">set hello world</span><br><span class="line">get foo</span><br><span class="line">get bar</span><br></pre></td></tr></table></figure>

<h2 id="3-在项目中使用"><a href="#3-在项目中使用" class="headerlink" title="3 在项目中使用"></a>3 在项目中使用</h2><p>因为我们的<code>network</code>对于主机来说并没有开放，所以在本地运行的项目并不能直接访问到这些IP。但是我们可以把项目打包部署到这个<code>network</code>里面，就可以直接使用这个<code>cluster</code>了。</p>
]]></content>
      <categories>
        <category>工具使用</category>
      </categories>
  </entry>
  <entry>
    <title>Windows用PowerShell连接VPS(以GoogleCloud为例）</title>
    <url>/2020/07/28/Windows%E7%94%A8PowerShell%E8%BF%9E%E6%8E%A5VPS(%E4%BB%A5GoogleCloud%E4%B8%BA%E4%BE%8B%EF%BC%89/</url>
    <content><![CDATA[<blockquote>
<p>Powershell已经内置OpenSSH,用起来比Xshell还方便，并且用root账号或开启密码登陆都存在安全隐患，所以这里只介绍普通用户+ssh的登陆方式。</p>
</blockquote>
<p>基本步骤：</p>
<span id="more"></span>

<h2 id="1-本机生成key-如果已经有key，可以跳过"><a href="#1-本机生成key-如果已经有key，可以跳过" class="headerlink" title="1.本机生成key(如果已经有key，可以跳过)"></a>1.本机生成key(如果已经有key，可以跳过)</h2><p>打开<code>powershell</code>,输入:</p>
<p><code>ssh-keygen</code></p>
<p>一路enter,生成的秘钥默认位置在<code>~\.ssh\</code>下，即<code>C:\Users\xxx\.ssh</code>，xxx为你的windows用户名。</p>
<h2 id="2-单个服务器设置"><a href="#2-单个服务器设置" class="headerlink" title="2.单个服务器设置"></a>2.单个服务器设置</h2><p>这种方法是对单个服务器写入pub-key，更适合授权给别人访问的时候使用，对方把pub-key给你，你写到自己的服务器里面，对方就可以连接你的服务器了。</p>
<h3 id="2-1复制公钥到服务器"><a href="#2-1复制公钥到服务器" class="headerlink" title="2.1复制公钥到服务器"></a>2.1复制公钥到服务器</h3><ul>
<li><p>打开本地<code>~\.ssh\</code>目录下的.pub公钥文件，复制内容。</p>
</li>
<li><p>打开GCP控制面板，进入虚拟机实例管理页面，点击实例右边的连接下方的“SSH”，选择在浏览器窗口中打开。</p>
</li>
<li><p><strong>请记住打开的shell这里显示的用户名</strong></p>
</li>
<li><p>进入用户目录下的<code>.ssh</code>文件夹：<code>cd ~/.ssh/</code></p>
</li>
<li><p>编辑<code>authorized_keys</code>文件,</p>
<p><code>vi authorized_keys</code></p>
<p>添加刚才复制的公钥，文件中可以用#开头添加注释，注意不要把公钥给注释了。保存退出。</p>
</li>
</ul>
<blockquote>
<p>注意：如果没有<code>.ssh</code>文件夹，或者是通过root账号新增的用户，需要手动新增一个文件夹，并设置文件夹权限为<code>700</code>（<code>chmod 700 .ssh</code>)，新增<code>authorized_keys</code>文件并设置权限为600或700（<code>chmod 600 authorized_keys</code>）（如果不新增key，连接时只要对该文件有读权限就可以，也就是400），权限不对的话后面连接会被服务器拒绝，每次都要输入密码。这里权限设置可以理解为只有用户自己才能操作这个文件夹，即使同组内用户也不能访问，更别说其它一般用户了，否则就有安全问题。</p>
</blockquote>
<h3 id="2-2重启sshd服务"><a href="#2-2重启sshd服务" class="headerlink" title="2.2重启sshd服务"></a>2.2重启sshd服务</h3><p>  <code>sudo service sshd restart</code></p>
<p>  到这里先保留网页端，后面也许还用的到。</p>
<h3 id="2-3建立连接"><a href="#2-3建立连接" class="headerlink" title="2.3建立连接"></a>2.3建立连接</h3><p>完成后就可以在powershell输入</p>
<p><code>ssh username@myServerIP</code></p>
<p>直接连接主机了，这里的<code>username</code>就是上面gcp打开的shell中显示的用户名。</p>
<p><strong>注意：如果服务器的IP以前已经连接过，记得到本机<code>~\.ssh\known_hosts</code>(跟pub-key同目录)文件中删除该IP对应的那一行，不然的话会因为跟之前的记录冲突而导致无法建立连接。</strong></p>
<p>如果连不上，可以返回刚才的网页端重启服务器</p>
<p><code>sudo reboot</code></p>
<h2 id="3-全部实例通用连接设置-推荐"><a href="#3-全部实例通用连接设置-推荐" class="headerlink" title="3.全部实例通用连接设置(推荐)"></a>3.全部实例通用连接设置(推荐)</h2><p>这种方法通过把pub-key写到gcp面板的设置里面，以后你的每个实例在创建的时候都会自动写入这个pub-key，就可以直接用<code>ssh username@hostname</code>的方式连接了，适合所有实例都是自己一个人用的情况。</p>
<p>主要步骤：</p>
<ul>
<li><p>在gcp面板上的搜索栏直接搜索<code>metadata</code>，中文应该是<code>元数据</code>，点进去之后再选择<code>ssh keys</code>选项卡。</p>
</li>
<li><p>打开本机<code>~\.ssh\</code>目录下的.pub公钥文件，复制内容，添加到上面的<code>ssh keys</code>里面，保存。</p>
</li>
<li><p>添加以后会自动生成一个用户名，比如<code>username</code>，然后在本机就可以用<code>ssh username@hostname</code>的方式连接了。</p>
</li>
</ul>
<p><strong>注意：如果服务器的IP以前已经连接过，记得到本机<code>~\.ssh\known_hosts</code>(跟pub-key同目录)文件中删除该IP对应的那一行，不然的话会因为跟之前的记录冲突而导致无法建立连接。</strong></p>
<h2 id="4-其它"><a href="#4-其它" class="headerlink" title="4.其它"></a>4.其它</h2><ul>
<li>google cloud默认是没有设置root密码的，连上以后可以<code>sudo passwd</code>第一时间设置密码，增加安全性。</li>
<li>如果账号下的实例都是自己用，推荐使用第二种方法，设置一次以后无论创建多少次实例，都不用再设置key。</li>
<li>还可以在powershell中配置别名等，进一步简化登陆步骤。</li>
<li>第一种方法对任意服务器都是通用的，而第二种则需要厂商的支持。</li>
</ul>
]]></content>
      <categories>
        <category>工具使用</category>
      </categories>
  </entry>
  <entry>
    <title>elasticsearch分布式搜索引擎</title>
    <url>/2021/04/27/elasticsearch%E5%88%86%E5%B8%83%E5%BC%8F%E6%90%9C%E7%B4%A2%E5%BC%95%E6%93%8E/</url>
    <content><![CDATA[<h2 id="1-elasticsearch是什么"><a href="#1-elasticsearch是什么" class="headerlink" title="1 elasticsearch是什么"></a>1 elasticsearch是什么</h2><p>es是一个分布式的搜索和分析数据的引擎。es是基于<code>Apache Lucene</code>开发的，<code>Apache Lucene</code>库提供了很多API，但是偏底层，没有进行封装的话在项目中直接使用是很不方便的。</p>
<p>es本身可以存储和读取数据，那么跟数据库有什么不同呢？</br></p>
<span id="more"></span>
<p>这里有个概念叫反向索引(也叫倒排索引)，英文是<a href="https://en.wikipedia.org/wiki/Inverted_index"><strong>Inverted Index</strong></a>，<strong>全文搜索</strong>和搜索引擎技术都依赖于这种数据结构。常规的数据库采用的是与之对应的<code>Forward Index</code>。</br><br>比如，常见的技术书籍，通常在最后几页会有一个索引表，可以让你快速定位到某个名词对应的内容在第几页，这就是反向索引。而整本书的编排就像正向索引，你可以快速翻到某一页知道上面是什么内容。这两种索引技术是互补的，一本书如果根据索引表来编写，就显得没有逻辑，同样，缺少了索引表，作为工具书查询的时候效率也很低下。</br><br>再举个例子，搜索引擎的运作也依赖反向索引，当你输入一个关键字进行搜索时，它不可能把整个网络世界的所有网页都搜一遍，从技术上来说这也不现实。一般来说，搜索引擎会利用爬虫不断的收集网络中的信息，给其中出现的关键字添加记录，指明这些关键字分别在哪些网页中出现了，当你搜索这些关键字的时候，对应的网页记录就显示出来了。当然，搜索引擎具体的工作方式还有很多影响因素，可以参考<code>Goolge</code>的科普专题：<a href="https://www.google.com/search/howsearchworks/">How Google Search works</a>。</br><br>通过比较可以发现，数据库更偏向于存储数据，而反向索引则侧重于检索和分析数据，它的一系列操作就是为你的搜索作提前准备的，就像搜索引擎在你浏览网页之前，已经用爬虫分析了很多网页，看里面有哪些关键词，并进行分类记录，这都需要消耗资源。</p>
<p>es就像一个小型的搜索引擎，为你的数据建立索引，然后提供搜索和分析数据的功能。</p>
<p>web开发中，在以下场景可以使用es：</p>
<ul>
<li>你的网站或者APP需要添加搜索功能</li>
<li>存储和分析日志等</li>
<li>使用机器学习自动对数据进行实时分析和建模</li>
</ul>
<h2 id="2-运行es"><a href="#2-运行es" class="headerlink" title="2 运行es"></a>2 运行es</h2><p>es跟<em>redis</em>类似，本身需要先运行一个server端，然后用client去连接server。  </p>
<h3 id="2-1-运行server端"><a href="#2-1-运行server端" class="headerlink" title="2.1 运行server端"></a>2.1 运行server端</h3><p>详见<a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/install-elasticsearch.html">官方文档</a>，启动server端比较简单，在windows上两种常见的运行方式：</p>
<ul>
<li>下载es压缩包，解压以后直接在本地运行</li>
<li>在docker里面运行</li>
</ul>
<p>server端通常开放<code>9200</code>端口。</p>
<h3 id="2-2-运行client"><a href="#2-2-运行client" class="headerlink" title="2.2 运行client"></a>2.2 运行client</h3><p>client是相对于server的称呼，实际上我们通过API就可以直接跟server端通信了。<br>比如，在<a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/getting-started.html#add-data">官方入门教程</a>中，从step3开始的那些操作，都可以用api测试软件来直接测试，不需要<a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/getting-started.html#run-elasticsearch"><em>安装并运行Kibana</em></a>这一步。<br>举个例子，step3里面的添加数据，在<em>Kibana</em>中运行代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">POST logs-my_app-default/_doc</span><br><span class="line">&#123;</span><br><span class="line">  &quot;@timestamp&quot;: &quot;2099-05-06T16:21:15.000Z&quot;,</span><br><span class="line">  &quot;event&quot;: &#123;</span><br><span class="line">    &quot;original&quot;: &quot;192.0.2.42 - - [06/May/2099:16:21:15 +0000] \&quot;GET /images/bg.jpg HTTP/1.0\&quot; 200 24736&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么，可以用api测试软件（如chrome应用商店里的<a href="https://chrome.google.com/webstore/detail/talend-api-tester-free-ed/aejoelaoggembcahagimdiliamlcdmfm?hl=zh-CN">Talend API Tester - Free Edition</a>）发送<code>post</code>请求到<code>http://localhost:9200/logs-my_app-default/_doc?</code>并携带<code>&#123;&#125;</code>的内容。如下图所示：</p>
<details>
<summary>查看图片</summary>

<p><a href="https://ozryan.github.io/images/2021-05-03-elasticsearch-apitester.png" title="点击查看大图"><img src="/images/2021-05-03-elasticsearch-apitester.png" alt="image"></a></p>
</details>

<p><strong>教程中GET方法携带了json内容，而常规的GET方法不能携带内容，这里只要把GET改成POST即可。</strong></p>
<h2 id="3-在java项目中使用es"><a href="#3-在java项目中使用es" class="headerlink" title="3 在java项目中使用es"></a>3 在java项目中使用es</h2><p>主要介绍当下流行的两种方式。</p>
<h3 id="3-1-Java-High-Level-REST-Client"><a href="#3-1-Java-High-Level-REST-Client" class="headerlink" title="3.1 Java High Level REST Client"></a>3.1 Java High Level REST Client</h3><p>官方提供的方式，通过引入<em>Maven</em>依赖直接使用。</p>
<h4 id="第一步：引入依赖"><a href="#第一步：引入依赖" class="headerlink" title="第一步：引入依赖"></a>第一步：引入依赖</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;org.elasticsearch.client&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;elasticsearch-rest-high-level-client&lt;/artifactId&gt;</span><br><span class="line">    &lt;version&gt;7.12.1&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>

<p>官方还有一个<code>Java Low Level REST Client</code>，这个更接近原版的es操作，<code>Java High Level REST Client</code>是在其基础上进一步封装。</p>
<blockquote>
<p>关于Jest：jest是<code>Java HTTP Rest client for ElasticSearch</code>，是java端的<code>RESTful</code>风格的client，但现在很少用了。从<a href="https://github.com/elastic/elasticsearch">es源码</a>和<a href="https://github.com/searchbox-io/Jest">jest源码</a>的历史分析，es最早在2010年发布，而jest在2012年发布，但es直到2016发布的5.0版本之前，官方都没有提供<code>RESTful</code>风格的java client，而是一直用的<a href="https://www.elastic.co/guide/en/elasticsearch/client/java-api/5.6/client.html">TranspostClient</a>。5.0版本的es提供了<code>Java Rest Client</code>，而2017年的6.0进一步提供了<code>Java High Level REST Client</code>，并将原来的<code>Java Rest Client</code>更名为<code>Java Low Level REST Client</code>，至此以后，jest逐渐淡出历史舞台，最后一次<em>release</em>已经是2018年，所以本文不再介绍jest的使用。</p>
</blockquote>
<h4 id="第二步：初始化client"><a href="#第二步：初始化client" class="headerlink" title="第二步：初始化client"></a>第二步：初始化<code>client</code></h4><p><code>RestHighLevelClient</code>的初始化要依赖于<code>REST low-level client builder</code>,因为后者在运行时有一些连接池和线程资源，所以在使用后需要手动关闭<code>RestHighLevelClient</code>来间接释放这些资源。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">RestHighLevelClient client = <span class="keyword">new</span> RestHighLevelClient(</span><br><span class="line">        RestClient.builder(</span><br><span class="line">                <span class="keyword">new</span> HttpHost(<span class="string">&quot;localhost&quot;</span>, <span class="number">9200</span>, <span class="string">&quot;http&quot;</span>)</span><br><span class="line">        )</span><br><span class="line">);</span><br><span class="line">client.close();</span><br></pre></td></tr></table></figure>

<h4 id="第三步：创建对应功能的Request"><a href="#第三步：创建对应功能的Request" class="headerlink" title="第三步：创建对应功能的Request"></a>第三步：创建对应功能的<code>Request</code></h4><p>整个<code>RestHighLevelClient</code>的使用方式就是，在上面<a href="#%E7%AC%AC%E4%BA%8C%E6%AD%A5%EF%BC%9A%E5%88%9D%E5%A7%8B%E5%8C%96%60client%60">第二步</a>的初始化后，根据不同的需求去构造对应的<code>xxxRequest</code>，然后用<code>client</code>去执行这个<code>xxxRequest</code>,并返回<code>xxxResponse</code>。<br>同步执行方式为<code>client.xxx(xxxRequest,options)</code>，异步执行方式只需在xxx后加<code>Aysnc</code>标识，如<code>client.xxxAsync(xxxRequest,options)</code>，但异步执行不能马上获取结果，需要借助回调函数处理。<br>比如，要构建<code>Index</code>:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//示例数据</span></span><br><span class="line">String jsonString = <span class="string">&quot;&#123;&quot;</span> +</span><br><span class="line">        <span class="string">&quot;\&quot;user\&quot;:\&quot;kimchy\&quot;,&quot;</span> +</span><br><span class="line">        <span class="string">&quot;\&quot;postDate\&quot;:\&quot;2013-01-30\&quot;,&quot;</span> +</span><br><span class="line">        <span class="string">&quot;\&quot;message\&quot;:\&quot;trying out Elasticsearch\&quot;&quot;</span> +</span><br><span class="line">        <span class="string">&quot;&#125;&quot;</span>;</span><br><span class="line"><span class="comment">//构建IndexRequest</span></span><br><span class="line">IndexRequest request = <span class="keyword">new</span> IndexRequest(<span class="string">&quot;posts&quot;</span>).id(<span class="string">&quot;1&quot;</span>).source(jsonString,XContentType.JSON);</span><br><span class="line"><span class="comment">//执行index</span></span><br><span class="line">IndexResponse response = client.index(request, RequestOptions.DEFAULT);</span><br><span class="line"><span class="comment">//异步执行</span></span><br><span class="line"><span class="comment">//IndexResponse response = client.indexAsync(request, RequestOptions.DEFAULT);</span></span><br><span class="line"><span class="comment">// listener = new ActionListener&lt;IndexResponse&gt;() &#123;</span></span><br><span class="line"><span class="comment">//     @Override</span></span><br><span class="line"><span class="comment">//     public void onResponse(IndexResponse indexResponse) &#123;</span></span><br><span class="line">        <span class="comment">//在这里操作IndexResponse</span></span><br><span class="line"><span class="comment">//     &#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//     @Override</span></span><br><span class="line"><span class="comment">//     public void onFailure(Exception e) &#123;</span></span><br><span class="line">        </span><br><span class="line"><span class="comment">//     &#125;</span></span><br><span class="line"><span class="comment">// &#125;;</span></span><br></pre></td></tr></table></figure>
<p>或者要执行<code>search</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//构建SearchRequest</span></span><br><span class="line">SearchRequest searchRequest=<span class="keyword">new</span> SearchRequest();</span><br><span class="line">SearchSourceBuilder searchSourceBuilder=<span class="keyword">new</span> SearchSourceBuilder();</span><br><span class="line">searchSourceBuilder.query(QueryBuilders.matchAllQuery());</span><br><span class="line">searchRequest.source(searchSourceBuilder);</span><br><span class="line"><span class="comment">//执行search</span></span><br><span class="line">SearchResponse searchResponse=client.search(searchRequest,RequestOptions.DEFAULT);</span><br></pre></td></tr></table></figure>
<p>更多API使用参见<a href="https://www.elastic.co/guide/en/elasticsearch/client/java-rest/current/java-rest-high.html">官方文档</a></p>
<h3 id="3-2-spring-data-elasticsearch"><a href="#3-2-spring-data-elasticsearch" class="headerlink" title="3.2 spring-data-elasticsearch"></a>3.2 spring-data-elasticsearch</h3><p><code>spring-data-elasticsearch</code>是<a href="https://spring.io/projects/spring-data">spring-data</a>这个大项目中的一员，它的主要目的是方便在<em>spring</em>项目中使用<em>elasticsearch</em>,是对<a href="#3.1-Java-High-Level-REST-Client">上面</a>介绍的<code>Java High Level REST Client</code>的封装。因为封装也需要时间，所以<code>spring-data-elasticsearch</code>使用的不一定是最新版的elasticsearch，具体的版本依赖参见<a href="https://docs.spring.io/spring-data/elasticsearch/docs/4.2.0/reference/html/#preface.requirements">官方说明</a>。  </p>
<p>下面的例子假设是一个<code>springboot</code>项目：</p>
<h4 id="第一步：生成springboot项目"><a href="#第一步：生成springboot项目" class="headerlink" title="第一步：生成springboot项目"></a>第一步：生成springboot项目</h4><p>先用<a href="https://www.jetbrains.com/idea/">Idea</a>快速生成一个springboot项目，选上web功能  </p>
<h4 id="第二步：引入依赖"><a href="#第二步：引入依赖" class="headerlink" title="第二步：引入依赖"></a>第二步：引入依赖</h4><p>在pom.xml文件中添加：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">        &lt;groupId&gt;org.springframework.data&lt;/groupId&gt;</span><br><span class="line">        &lt;artifactId&gt;spring-data-elasticsearch&lt;/artifactId&gt;</span><br><span class="line">        &lt;version&gt;<span class="number">4.2</span><span class="number">.0</span>&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br><span class="line">&lt;!-- 如果第一步没有勾选web功能，则加上下面这个依赖 --&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">        &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">        &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>
<p>这里不引入<em>web</em>依赖的话，后面的例子运行会报错，因为要用到<code>org.springframework.http</code>这个<em>package</em>里的<code>HttpHeaders</code>等类，这个<em>package</em>属于<em>web</em>依赖。</p>
<h4 id="第三步：写配置类"><a href="#第三步：写配置类" class="headerlink" title="第三步：写配置类"></a>第三步：写配置类</h4><p>这一步主要是为了注册<code>RestHighLevelClient</code>这个Bean</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.esdemo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.elasticsearch.client.RestHighLevelClient;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.elasticsearch.client.ClientConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.elasticsearch.client.RestClients;</span><br><span class="line"><span class="keyword">import</span> org.springframework.data.elasticsearch.config.AbstractElasticsearchConfiguration;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EsConfig</span> <span class="keyword">extends</span> <span class="title">AbstractElasticsearchConfiguration</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestHighLevelClient <span class="title">elasticsearchClient</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> ClientConfiguration clientConfiguration=ClientConfiguration.builder()</span><br><span class="line">                .connectedTo(<span class="string">&quot;localhost:9200&quot;</span>)</span><br><span class="line">                .build();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> RestClients.create(clientConfiguration).rest();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="第四步：测试"><a href="#第四步：测试" class="headerlink" title="第四步：测试"></a>第四步：测试</h4><p>在<code>test</code>文件夹下，写一个<code>Index</code>测试一下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.esdemo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.elasticsearch.action.index.IndexRequest;</span><br><span class="line"><span class="keyword">import</span> org.elasticsearch.action.index.IndexResponse;</span><br><span class="line"><span class="keyword">import</span> org.elasticsearch.action.support.WriteRequest;</span><br><span class="line"><span class="keyword">import</span> org.elasticsearch.client.RequestOptions;</span><br><span class="line"><span class="keyword">import</span> org.elasticsearch.client.RestHighLevelClient;</span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.test.context.SpringBootTest;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.util.Collections;</span><br><span class="line"></span><br><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EsdemoApplicationTests</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    RestHighLevelClient highLevelClient;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">contextLoads</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        IndexRequest request=<span class="keyword">new</span> IndexRequest(<span class="string">&quot;spring-data&quot;</span>)</span><br><span class="line">                .id(<span class="string">&quot;1&quot;</span>)</span><br><span class="line">                .source(Collections.singletonMap(<span class="string">&quot;feature&quot;</span>,<span class="string">&quot;high-level-rest-client&quot;</span>))</span><br><span class="line">                .setRefreshPolicy(WriteRequest.RefreshPolicy.IMMEDIATE);</span><br><span class="line"></span><br><span class="line">        IndexResponse response=highLevelClient.index(request, RequestOptions.DEFAULT);</span><br><span class="line">        System.out.println(response);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>输出：（执行次数不同结果会有一点差异）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">IndexResponse[index=spring-data,type=_doc,id=1,version=5,result=updated,seqNo=4,primaryTerm=1,shards=&#123;&quot;total&quot;:2,&quot;successful&quot;:1,&quot;failed&quot;:0&#125;]</span><br></pre></td></tr></table></figure>
<p>最终我们的项目文件结构如下图：</p>
<details>
<summary>查看图片</summary>

<p><a href="https://ozryan.github.io/images/2021-05-07-elasticsearch-spring-data-elasticsearch-demo-project-structure.png" title="点击查看大图"><img src="/images/2021-05-07-elasticsearch-spring-data-elasticsearch-demo-project-structure.png" alt="image"></a></p>
</details>

<p><code>spring-data-elasticsearch</code>的内容比较多，这里只是一个很简单的例子，详见<a href="https://docs.spring.io/spring-data/elasticsearch/docs/current/reference/html/#reference">官方文档</a></p>
]]></content>
      <categories>
        <category>web</category>
        <category>后端</category>
      </categories>
  </entry>
  <entry>
    <title>spring boot用java bean validation进行数据验证</title>
    <url>/2020/11/13/spring-boot%E7%94%A8java-bean-validation%E8%BF%9B%E8%A1%8C%E6%95%B0%E6%8D%AE%E9%AA%8C%E8%AF%81/</url>
    <content><![CDATA[<blockquote>
<p>概述：以一个web项目中的用户注册服务为例子，介绍使用java bean validation进行数据验证的过程。</p>
</blockquote>
<span id="more"></span>

<h2 id="1-引入依赖"><a href="#1-引入依赖" class="headerlink" title="1.引入依赖"></a><strong>1.引入依赖</strong></h2><p>从spring boot 2.3开始，<code>spring-boot-starter-validation</code>依赖独立出来了，2.3之前的版本，validation整合在<code>spring-boot-starter-web</code>中，具体原因可以参考这个<a href="https://github.com/spring-projects/spring-boot/issues/19550">issue</a>，所以请根据当前使用的spring boot版本选择相应依赖：</p>
<ul>
<li>spring boot 2.3+:  <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-validation<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
<li>spring boot 2.2及更早版本:<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="2-用注解对字段进行约束"><a href="#2-用注解对字段进行约束" class="headerlink" title="2.用注解对字段进行约束"></a><strong>2.用注解对字段进行约束</strong></h2><p>注解可以自定义，大部分时候内置的就够用。内置的注解可以参考最新的<a href="https://beanvalidation.org/2.0-jsr380/spec/#builtinconstraints">jsr380</a>,需要jdk8+支持，更早的jdk版本可以参考jsr303,jsr349等。</p>
<p>以<code>User</code>类为例,用户名和密码不能为空，用户名长度4-20位：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> uid;<span class="comment">//用户ID</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotEmpty(message = &quot;用户名不能为空&quot;)</span></span><br><span class="line">    <span class="meta">@Length(min=4,max=20,message = &quot;用户名长度不符合要求,4-20位&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String username;<span class="comment">//用户名</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@NotEmpty(message = &quot;密码不能为空&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String password;<span class="comment">//用户密码</span></span><br></pre></td></tr></table></figure>
<p><em>注意:如果<code>User</code>类中有其它封装的对象，要在这个对象上添加<code>@Valid</code>注解来开启嵌套验证，才能让这个嵌套对象里面的<code>@NotEmpty</code>等内部注解生效，这个特性支持层层嵌套。</em></p>
<h2 id="3-在接收参数时使用-Valid启用验证"><a href="#3-在接收参数时使用-Valid启用验证" class="headerlink" title="3.在接收参数时使用@Valid启用验证"></a><strong>3.在接收参数时使用@Valid启用验证</strong></h2><p>比如在<code>Controller</code>层接收前端发送的用户名和密码，用<code>User</code>类去接收<code>RequestBody</code>的内容，添加<code>@Valid</code>注解开启验证功能。这里也可以用<code>@Validated</code>，两者的区别是:</p>
<ul>
<li><code>@Valid</code>是java自带的，<code>@Validated</code>是spring提供的，后者额外支持了一个<code>group</code>特性</li>
<li><code>@Valid</code>可以用在方法、方法参数、成员属性(支持嵌套验证)</li>
<li><code>@Validatted</code>可以用在类、方法、方法参数，因为不能用在成员属性上，所以不支持嵌套验证，但可以配合<code>@Valid</code>使用实现。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserController</span> </span>&#123;</span><br><span class="line">    UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">UserController</span><span class="params">(UserService userService)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.userService = userService;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostMapping(&quot;/register&quot;)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ResponseBean <span class="title">submitRegister</span><span class="params">(<span class="meta">@RequestBody</span> <span class="meta">@Valid</span> User user)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        userService.addUser(userDO);</span><br><span class="line">        <span class="keyword">return</span> ResponseBean.ok(<span class="string">&quot;注册成功&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="4-处理异常"><a href="#4-处理异常" class="headerlink" title="4.处理异常"></a><strong>4.处理异常</strong></h2><p>如果数据验证没有问题，那么程序照常运行。如果验证不通过，则会抛出异常<code>MethodArgumentNotValidException</code>，可以在<code>@ControllerAdvice</code>中进行全局捕捉异常。<code>e.getBindingResult().getFieldError().getDefaultMessage()</code>会输出定义约束时的<code>message</code>信息，如上面的“用户名不能为空”。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GlobalExceptionAdvice</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ExceptionHandler(MethodArgumentNotValidException.class)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ResponseBean <span class="title">handleArgumentInvalid</span><span class="params">(MethodArgumentNotValidException e)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> ResponseBean.error(<span class="number">4001</span>,<span class="string">&quot;数据格式不符合要求:&quot;</span>+e.getBindingResult().getFieldError().getDefaultMessage());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-补充说明"><a href="#5-补充说明" class="headerlink" title="5.补充说明"></a><strong>5.补充说明</strong></h2><p>上面的例子用一个<code>User</code>类来接收前端的数据，用<code>@Valid</code>来开启验证，返回的是<code>MethodArgumentNodValidException</code>，有时候，我们只对前端发送的一个参数进行直接验证，比如前端发送了一个用户名来查询一些信息，这时候可以直接对这个参数进行验证，但需要在类上添加<code>@Validated</code>注解开启验证，如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span></span><br><span class="line"><span class="meta">@Validated</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserController</span> </span>&#123;</span><br><span class="line">    UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">UserController</span><span class="params">(UserService userService)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.userService = userService;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/address/&#123;id&#125;&quot;)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ResponseBean <span class="title">getAddress</span><span class="params">(<span class="meta">@PathVariable(&quot;username&quot;)</span> <span class="meta">@NotEmpty</span> <span class="keyword">int</span> id)</span></span>&#123;</span><br><span class="line">        String address=userService.getBirthdayById(id);</span><br><span class="line">        <span class="keyword">return</span> ResponseBean.ok(address);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/address&quot;)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ResponseBean <span class="title">getAddress2</span><span class="params">(<span class="meta">@Param(&quot;username&quot;)</span> <span class="meta">@NotEmpty</span> <span class="keyword">int</span> id)</span></span>&#123;</span><br><span class="line">        String address=userService.getBirthdayById(id);</span><br><span class="line">        <span class="keyword">return</span> ResponseBean.ok(address);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>此时如果验证失败，则抛出<code>ConstraintViolationException</code>,需要进行相应的异常处理。</p>
]]></content>
      <categories>
        <category>web</category>
        <category>后端</category>
      </categories>
  </entry>
  <entry>
    <title>vue+element-ui 不借助vuex实现弹窗登录</title>
    <url>/2020/09/01/vue-element-ui-%E5%AE%9E%E7%8E%B0%E5%BC%B9%E7%AA%97%E7%99%BB%E5%BD%95/</url>
    <content><![CDATA[<blockquote>
<p>本文主要介绍不使用vuex的情况下，通过父子组件传值通信等方法达到弹窗登录的目的。在生产环境中，使用vuex会非常的方便，通过store去维护一个”isLoginVisible”变量即可。</p>
</blockquote>
<h2 id="主要思路"><a href="#主要思路" class="headerlink" title="主要思路"></a>主要思路</h2><p>首先，在同一个页面文件中实现弹窗功能一般没什么问题，定义一个状态变量控制组件的显示就行了。这里主要介绍弹窗作为一个单独的单文件组件时的情况，涉及父子组件通信及属性绑定的问题。</p>
<p>实现：</p>
<span id="more"></span>
<ul>
<li>弹窗子组件采用<code>element-ui</code>的<code>&lt;el-dialog&gt;</code>元素，该元素有一个<code>visible</code>属性,定义一个<code>isShow</code>的状态prop来控制该弹窗是否显示。</li>
<li>子组件中不直接修改这个<code>isShow</code>的prop，而是通过<code>this.$emit(&#39;update:isShow&#39;,false)</code>这样的方式，用事件通知父组件自己想要修改<code>isShow</code>的值</li>
<li>父组件里面定义一个<code>showDialogOrNot</code>之类的<code>data</code>字段来维护弹窗是否显示,父组件中修改弹窗状态时，直接修改<code>showDialogOrNot</code>的值即可。</li>
<li>父组件在引用子组件时，对<code>isShow</code>属性作双向绑定：<code>&lt;child-component :is-show.sync=&quot;showDialogOrNot&quot;&gt;&lt;/child-component&gt;</code>。</li>
</ul>
<h2 id="代码示意"><a href="#代码示意" class="headerlink" title="代码示意"></a>代码示意</h2><h3 id="父组件"><a href="#父组件" class="headerlink" title="父组件"></a>父组件</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Parent.vue</span></span><br><span class="line">&lt;template&gt;</span><br><span class="line">  <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">round</span> <span class="attr">id</span>=<span class="string">&quot;sign-in&quot;</span> @<span class="attr">click</span>=<span class="string">&quot;openLogin&quot;</span>&gt;</span>登录<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">Login</span> <span class="attr">:is-show.sync</span>=<span class="string">&quot;isLoginVisible&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">Login</span>&gt;</span></span></span><br><span class="line"><span class="xml">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"></span></span></span><br><span class="line"><span class="javascript"><span class="xml"><span class="keyword">import</span> Login <span class="keyword">from</span> <span class="string">&quot;@/components/Login&quot;</span>;</span></span></span><br><span class="line"><span class="javascript"><span class="xml"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">name</span>: <span class="string">&quot;Header&quot;</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">components</span>: &#123;Login&#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">props</span>:[],</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="function"><span class="title">data</span>(<span class="params"></span>)</span>&#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="keyword">return</span>&#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="attr">isLoginVisible</span>:<span class="literal">false</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">methods</span>:&#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">openLogin</span>(<span class="params"></span>)</span>&#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.isLoginVisible=<span class="literal">true</span>;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">&#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">style</span> <span class="attr">scoped</span>&gt;</span><span class="css"></span></span></span><br><span class="line"><span class="css"><span class="xml"><span class="selector-id">#name</span> &#123;</span></span></span><br><span class="line"><span class="css"><span class="xml">  <span class="attribute">font-size</span>: <span class="number">30px</span>;</span></span></span><br><span class="line"><span class="css"><span class="xml">  <span class="attribute">font-weight</span>: bold;</span></span></span><br><span class="line"><span class="css"><span class="xml">&#125;</span></span></span><br><span class="line"><span class="css"><span class="xml"><span class="selector-id">#register</span>&#123;</span></span></span><br><span class="line"><span class="css"><span class="xml">  <span class="attribute">padding</span>: <span class="number">5px</span>;</span></span></span><br><span class="line"><span class="css"><span class="xml">&#125;</span></span></span><br><span class="line"><span class="css"><span class="xml"><span class="selector-id">#register</span>&#123;</span></span></span><br><span class="line"><span class="css"><span class="xml">  <span class="attribute">padding</span>: <span class="number">5px</span>;</span></span></span><br><span class="line"><span class="css"><span class="xml">&#125;</span></span></span><br><span class="line"><span class="css"><span class="xml"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span></span><br></pre></td></tr></table></figure>

<h3 id="子组件"><a href="#子组件" class="headerlink" title="子组件"></a>子组件</h3><p>一个不好的例子：</p>
<h4 id="子组件（弹窗组件-—有缺陷的版本"><a href="#子组件（弹窗组件-—有缺陷的版本" class="headerlink" title="子组件（弹窗组件)—有缺陷的版本"></a>子组件（弹窗组件)—<strong>有缺陷的版本</strong></h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Login.vue</span></span><br><span class="line">&lt;template&gt;</span><br><span class="line">  <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;<span class="name">el-dialog</span> <span class="attr">v-bind</span>=<span class="string">&quot;$attrs&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">v-on</span>=<span class="string">&quot;$listeners&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               @<span class="attr">open</span>=<span class="string">&quot;onOpen&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               @<span class="attr">close</span>=<span class="string">&quot;onClose&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">title</span>=<span class="string">&quot;登录&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">:visible</span>=<span class="string">&quot;isShow&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">:close-on-click-modal</span>=<span class="string">&quot;false&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">:show-close</span>=<span class="string">&quot;false&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">el-form</span> <span class="attr">ref</span>=<span class="string">&quot;elForm&quot;</span> <span class="attr">:model</span>=<span class="string">&quot;formData&quot;</span> <span class="attr">:rules</span>=<span class="string">&quot;rules&quot;</span> <span class="attr">size</span>=<span class="string">&quot;medium&quot;</span> <span class="attr">label-width</span>=<span class="string">&quot;100px&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">el-form-item</span> <span class="attr">label</span>=<span class="string">&quot;用户名&quot;</span> <span class="attr">prop</span>=<span class="string">&quot;username&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">          <span class="tag">&lt;<span class="name">el-input</span> <span class="attr">v-model</span>=<span class="string">&quot;formData.username&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">                    <span class="attr">show-word-limit</span> <span class="attr">clearable</span> <span class="attr">prefix-icon</span>=<span class="string">&#x27;el-icon-user-solid&#x27;</span> <span class="attr">:style</span>=<span class="string">&quot;&#123;width: &#x27;100%&#x27;&#125;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">el-input</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">el-form-item</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">el-form-item</span> <span class="attr">label</span>=<span class="string">&quot;密码&quot;</span> <span class="attr">prop</span>=<span class="string">&quot;password&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">          <span class="tag">&lt;<span class="name">el-input</span> <span class="attr">v-model</span>=<span class="string">&quot;formData.password&quot;</span> <span class="attr">placeholder</span>=<span class="string">&quot;请输入密码&quot;</span> <span class="attr">clearable</span> <span class="attr">show-password</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">                    <span class="attr">:style</span>=<span class="string">&quot;&#123;width: &#x27;100%&#x27;&#125;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">el-input</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">el-form-item</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;/<span class="name">el-form</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">slot</span>=<span class="string">&quot;footer&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;<span class="name">el-button</span> @<span class="attr">click</span>=<span class="string">&quot;close&quot;</span>&gt;</span>取消<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> @<span class="attr">click</span>=<span class="string">&quot;handleConfirm&quot;</span>&gt;</span>确定<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">el-dialog</span>&gt;</span></span></span><br><span class="line"><span class="xml">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"></span></span></span><br><span class="line"><span class="javascript"><span class="xml"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">inheritAttrs</span>: <span class="literal">false</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">components</span>: &#123;&#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">props</span>: &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="attr">isShow</span>:<span class="built_in">Boolean</span></span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="function"><span class="title">data</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="keyword">return</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="attr">formData</span>: &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="attr">username</span>: <span class="literal">undefined</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="attr">password</span>: <span class="string">&quot;&quot;</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">methods</span>: &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">onClose</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.$refs[<span class="string">&#x27;elForm&#x27;</span>].resetFields();</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">close</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.$emit(<span class="string">&#x27;update:isShow&#x27;</span>, <span class="literal">false</span>);</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">handleConfirm</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.$refs[<span class="string">&#x27;elForm&#x27;</span>].validate(<span class="function"><span class="params">valid</span> =&gt;</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="keyword">if</span> (!valid) <span class="keyword">return</span></span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="built_in">this</span>.close()</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      &#125;)</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">&#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml"></span></span></span><br><span class="line"><span class="javascript"><span class="xml"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span></span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span></span><br><span class="line"><span class="xml"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
<p>刚开始的时候，直接用一个子组件中的<code>prop</code>来通信，这样可以实现如下的功能：在父组件中点击登录时，弹窗出现，在弹窗中点击取消时，弹窗消失，好像也没什么问题。</p>
<p>但是，如果把上面的<code>:show-close</code>改成<code>true</code>，也就是显示弹窗右上角的关闭按钮，会发现点击这个按钮是无效的。通过查看<code>&lt;el-dialog&gt;</code>的实现代码，发现点击这个按钮的时候是触发一个<code>handleClose</code>方法，这个方法的相关实现如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">handleClose</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="built_in">this</span>.beforeClose === <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">        <span class="built_in">this</span>.beforeClose(<span class="built_in">this</span>.hide);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.hide();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="title">hide</span>(<span class="params">cancel</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (cancel !== <span class="literal">false</span>) &#123;</span><br><span class="line">        <span class="built_in">this</span>.$emit(<span class="string">&#x27;update:visible&#x27;</span>, <span class="literal">false</span>);</span><br><span class="line">        <span class="built_in">this</span>.$emit(<span class="string">&#x27;close&#x27;</span>);</span><br><span class="line">        <span class="built_in">this</span>.closed = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到：</p>
<p>如果没有定义<code>beforeClose</code>的话，<code>handleClose</code>会直接调用<code>hide()</code>方法，而<code>hide</code>中会触发一个事件，试图更新<code>visible</code>的值，但是之前的代码并没有给<code>visible</code>设置<code>.sync</code>，也就是没有双向绑定，导致<code>visible</code>的值只能由父组件来设置，子组件传递的这个事件并不会被父组件接收，而<code>&lt;el-dialog&gt;</code>就是通过<code>visible</code>的值来控制组件是否显示的。如果给<code>visible</code>加上<code>.sync</code>，会出现另一个问题，<code>Avoid mutating a prop directly...</code>，尝试在本地修改<code>props</code>的值，这是官方不建议的，会导致逻辑混乱。</p>
<p>关于<code>visible</code>的变化情况大致如下：</p>
<ol>
<li>在父组件中点击<code>登录</code>时，触发<code>openLogin</code>方法，通过<code>isLoginVisible</code>来设置<code>isShow</code>为<code>true</code>，而<code>isShow</code>将值传给<code>visible</code>，此时<code>visilbe</code>为<code>true</code>。</li>
<li>点击关闭按钮，子组件(el-dialog)传递事件给父组件(login)，尝试修改<code>isVisible</code>的值为false,但是父组件未处理该事件，因为<code>:isVisible</code>没有用<code>.sync</code>实现双向绑定，此时<code>visible</code>仍然为<code>true</code>，也就是点击关闭按钮没有反应。</li>
</ol>
<p>这个问题可以通过在子组件中增加一个变量来解决。</p>
<hr>
<h4 id="子组件（弹窗组件-—修改后的版本"><a href="#子组件（弹窗组件-—修改后的版本" class="headerlink" title="子组件（弹窗组件)—修改后的版本"></a>子组件（弹窗组件)—<strong>修改后的版本</strong></h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Login.vue</span></span><br><span class="line">&lt;template&gt;</span><br><span class="line">  <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;<span class="name">el-dialog</span> <span class="attr">v-bind</span>=<span class="string">&quot;$attrs&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">v-on</span>=<span class="string">&quot;$listeners&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               @<span class="attr">open</span>=<span class="string">&quot;onOpen&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               @<span class="attr">close</span>=<span class="string">&quot;onClose&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">title</span>=<span class="string">&quot;登录&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">:visible.sync</span>=<span class="string">&quot;isVisible&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">:close-on-click-modal</span>=<span class="string">&quot;false&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">               <span class="attr">:show-close</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">el-form</span> <span class="attr">ref</span>=<span class="string">&quot;elForm&quot;</span> <span class="attr">:model</span>=<span class="string">&quot;formData&quot;</span> <span class="attr">:rules</span>=<span class="string">&quot;rules&quot;</span> <span class="attr">size</span>=<span class="string">&quot;medium&quot;</span> <span class="attr">label-width</span>=<span class="string">&quot;100px&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">el-form-item</span> <span class="attr">label</span>=<span class="string">&quot;用户名&quot;</span> <span class="attr">prop</span>=<span class="string">&quot;username&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">          <span class="tag">&lt;<span class="name">el-input</span> <span class="attr">v-model</span>=<span class="string">&quot;formData.username&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">                    <span class="attr">show-word-limit</span> <span class="attr">clearable</span> <span class="attr">prefix-icon</span>=<span class="string">&#x27;el-icon-user-solid&#x27;</span> <span class="attr">:style</span>=<span class="string">&quot;&#123;width: &#x27;100%&#x27;&#125;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">el-input</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">el-form-item</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">el-form-item</span> <span class="attr">label</span>=<span class="string">&quot;密码&quot;</span> <span class="attr">prop</span>=<span class="string">&quot;password&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">          <span class="tag">&lt;<span class="name">el-input</span> <span class="attr">v-model</span>=<span class="string">&quot;formData.password&quot;</span> <span class="attr">placeholder</span>=<span class="string">&quot;请输入密码&quot;</span> <span class="attr">clearable</span> <span class="attr">show-password</span></span></span></span><br><span class="line"><span class="tag"><span class="xml">                    <span class="attr">:style</span>=<span class="string">&quot;&#123;width: &#x27;100%&#x27;&#125;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">el-input</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">el-form-item</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;/<span class="name">el-form</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">slot</span>=<span class="string">&quot;footer&quot;</span>&gt;</span></span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;<span class="name">el-button</span> @<span class="attr">click</span>=<span class="string">&quot;close&quot;</span>&gt;</span>取消<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> @<span class="attr">click</span>=<span class="string">&quot;handleConfirm&quot;</span>&gt;</span>确定<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">el-dialog</span>&gt;</span></span></span><br><span class="line"><span class="xml">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"></span></span></span><br><span class="line"><span class="javascript"><span class="xml"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">inheritAttrs</span>: <span class="literal">false</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">components</span>: &#123;&#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">props</span>: &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="attr">isShow</span>:<span class="built_in">Boolean</span></span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="function"><span class="title">data</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="keyword">return</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="attr">formData</span>: &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="attr">username</span>: <span class="literal">undefined</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="attr">password</span>: <span class="string">&quot;&quot;</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="attr">isVisible</span>:<span class="literal">false</span>,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">watch</span>:&#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="attr">isVisible</span>:<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.$emit(<span class="string">&#x27;update:isShow&#x27;</span>,<span class="built_in">this</span>.isVisible)</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="attr">isShow</span>:<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.isVisible=<span class="built_in">this</span>.isShow;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  <span class="attr">methods</span>: &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">onOpen</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">onClose</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.$refs[<span class="string">&#x27;elForm&#x27;</span>].resetFields();</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">close</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.$emit(<span class="string">&#x27;update:isShow&#x27;</span>,<span class="literal">false</span>);</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    <span class="function"><span class="title">handleConfirm</span>(<span class="params"></span>)</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      <span class="built_in">this</span>.$refs[<span class="string">&#x27;elForm&#x27;</span>].validate(<span class="function"><span class="params">valid</span> =&gt;</span> &#123;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="keyword">if</span> (!valid) <span class="keyword">return</span></span></span></span><br><span class="line"><span class="javascript"><span class="xml">        <span class="built_in">this</span>.close()</span></span></span><br><span class="line"><span class="javascript"><span class="xml">      &#125;)</span></span></span><br><span class="line"><span class="javascript"><span class="xml">    &#125;,</span></span></span><br><span class="line"><span class="javascript"><span class="xml">  &#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml">&#125;</span></span></span><br><span class="line"><span class="javascript"><span class="xml"></span></span></span><br><span class="line"><span class="javascript"><span class="xml"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span></span><br><span class="line"><span class="xml"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span></span><br><span class="line"><span class="xml"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
<ul>
<li>修改后的子组件代码比之前多了一个<code>isVisible</code>变量，把设置<code>:visible.sync=isVisible</code>，<strong>避免</strong>了子组件中直接修改<code>props</code>的情况。</li>
<li>同时在<code>watch</code>中增加了对<code>isShow</code>和<code>isVisible</code>的监控：<ul>
<li>如果<code>isShow</code>变化了，说明是父组件传过来的，则在子组件中把新值赋给<code>isVisible</code>，进而修改<code>visible</code>的值，控制组件显示。</li>
<li>如果<code>isVisible</code>变化了，说明是子组件中自己发生状态变化（如上面的”点击右上角的关闭按钮”这个事件）,则通过传递一个事件给父组件，通知父组件自己想要修改<code>isShow</code>的值，目标值就是变化后的<code>isVisible</code>，父组件收到事件后，通过修改子组件的<code>isShow</code>属性来控制显示。</li>
</ul>
</li>
</ul>
<h2 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h2><p>vue中的props，如果要定义类型啥的，则使用的是对象形式，用<code>&#123;&#125;</code>包裹,且属性名不用引号也可以，如果只是列出属性不加约束，则使用字符串数组的模式，此时用<code>[]</code>包裹，且属性名必须加引号。</p>
]]></content>
      <categories>
        <category>web</category>
        <category>前端</category>
      </categories>
  </entry>
  <entry>
    <title>后端处理数据格式验证及统一处理异常</title>
    <url>/2020/11/02/%E5%90%8E%E7%AB%AF%E5%A4%84%E7%90%86%E6%95%B0%E6%8D%AE%E6%A0%BC%E5%BC%8F%E9%AA%8C%E8%AF%81%E5%8F%8A%E7%BB%9F%E4%B8%80%E5%A4%84%E7%90%86%E5%BC%82%E5%B8%B8/</url>
    <content><![CDATA[<p>对于前后端分离的项目，后端对于前端发送的数据进行验证是必要的，但这不表示前端的验证就没必要，比如一些数据的格式问题可以在前端完成验证，快速返回结果给用户，提升用户体验。</p>
<p>这里以一个简单的用户注册服务举例，代码部分主要展示逻辑和框架。</p>
<p>主要思路：前端发送用户注册提交的表单信息，后端接收后在service层进行格式验证，如果格式不符合要求，则抛出相应异常，最后统一在controller层进行异常的拦截处理，返回特定的json信息。</p>
<span id="more"></span>

<h2 id="1-接收前端数据"><a href="#1-接收前端数据" class="headerlink" title="1.接收前端数据"></a>1.接收前端数据</h2><p>controller层：</p>
<ul>
<li><code>@ResponseBody</code>注解把前端发送的信息保存在<code>UserDO</code>类中，因为项目结构简单，这里直接拿DO类来接收了。</li>
<li>返回的<code>ResponseBean</code>是一个封装类，用于统一返回给前端。</li>
<li>调用的<code>service</code>层的<code>userService.addUser</code>实际上是会抛出异常的，但这里不需要用<code>try-catch</code>去捕获，到最后我们再统一拦截处理。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserController</span> </span>&#123;</span><br><span class="line">    UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">UserController</span><span class="params">(UserService userService)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.userService = userService;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理用户注册</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> userDO 直接用UserDO类接收前端注册信息，角色信息通过后端生成，默认都是user</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 如果用户信息格式正确，返回成功的ResponseBean，如果格式不正确，返回包含错误信息的ResponseBean</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@PostMapping(&quot;/register&quot;)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ResponseBean <span class="title">submitRegister</span><span class="params">(<span class="meta">@RequestBody</span> UserDO userDO)</span> </span>&#123;</span><br><span class="line">        userService.addUser(userDO);</span><br><span class="line">        <span class="keyword">return</span> ResponseBean.ok(<span class="string">&quot;注册成功&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="2-Service层格式验证"><a href="#2-Service层格式验证" class="headerlink" title="2.Service层格式验证"></a>2.Service层格式验证</h2><p>service层：</p>
<ul>
<li><code>DataValidateUtils</code>是自定义的数据格式验证类，比如手机号码格式是否正确等，具体根据需求实现即可。</li>
<li><code>InvalidParameterException</code>是java自带的<code>Runtime</code>异常子类，可以根据需要自己写异常类。自己实现的话通常也是继承<code>Runtime</code>异常，先写一个项目级异常类，再用继承方式扩展开各种细分的子类。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserService</span> <span class="keyword">implements</span> <span class="title">UserDetailsService</span></span>&#123;</span><br><span class="line">    UserMapper userMapper;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">UserService</span><span class="params">(UserMapper userMapper)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.userMapper = userMapper;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 添加User，需要做数据格式验证</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> userDO</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">addUser</span><span class="params">(UserDO userDO)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(!DataValidateUtils.ValidateUserDO(userDO)) <span class="keyword">throw</span> <span class="keyword">new</span> InvalidParameterException(<span class="string">&quot;前端发送的用户信息不符合格式&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> userMapper.addUser(userDO);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-统一处理controller层异常"><a href="#3-统一处理controller层异常" class="headerlink" title="3.统一处理controller层异常"></a>3.统一处理controller层异常</h2><p>异常处理：</p>
<ul>
<li><code>@RestControllerAdvice</code>是spring提供的功能，实现了类似<code>aop</code>切面的功能。这个注解相当于<code>@ControllerAdvice</code>+<code>@ResponseBody</code>。</li>
<li><code>@ExceptionHandler(InvalidParameterException.class)</code>标注于方法上，用来对应特定的异常类，返回特定的结果。</li>
<li>这里的<code>ResponseBean</code>和前面处理成功时返回的是同一个类，只是在这里用<code>error</code>返回，表示服务端虽然接收到了前端的数据，但是数据有问题，把问题描述返回给前端，让前端自行处理。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GlobalExceptionAdvice</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ExceptionHandler(InvalidParameterException.class)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ResponseBean <span class="title">invalidParameter</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> ResponseBean.error(<span class="string">&quot;数据不合法&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4.总结"></a>4.总结</h2><p>文中用了一个很简单的例子来演示前后端分离项目后端如何进行数据验证，及验证失败时如何处理。这种处理方式有几个优点：</p>
<ul>
<li>返回结果的格式统一，不管是否验证成功，都是用同一个<code>ResponseBean</code>来表示结果，实际上如果验证失败时，<code>error</code>表示还可以添加更详细的错误代码，前后端用统一错误代码来表示对应的错误。</li>
<li>对原代码改动较小，<code>controller</code>层没有改变。</li>
<li>集中处理异常。相比于散落各处的<code>try-catch</code>语句，<code>@RestControllerAdvice</code>提供了一个统一处理异常的功能，代码可读性更好，便于维护。</li>
</ul>
<p>最后，<code>@RestControllerAdvice</code>是一个很方便的注解，如果没有这个注解，使用aop虽然也可以实现类似的功能，但要麻烦很多，先定义一个针对全局<code>controller</code>的<code>PointCut</code>，然后在<code>around</code>里面用<code>try-catch</code>包裹<code>xx.proceed()</code>，最后在<code>catch</code>那里按照特定的顺序捕获各种特定异常。。。只能感慨spring帮我们做了很多。</p>
]]></content>
      <categories>
        <category>web</category>
        <category>后端</category>
      </categories>
  </entry>
  <entry>
    <title>在Windows中安装使用Redis的几种方法</title>
    <url>/2020/07/28/%E5%9C%A8Windows%E4%B8%AD%E5%AE%89%E8%A3%85%E4%BD%BF%E7%94%A8Redis%E7%9A%84%E5%87%A0%E7%A7%8D%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<p>redis主要是运行在linux下的，官网没有提供windows版本，可以通过以下几种方法在windows系统下使用redis，部分方法只适用于win10。</p>
<span id="more"></span>

<h2 id="1-下载windows专用版"><a href="#1-下载windows专用版" class="headerlink" title="1.下载windows专用版"></a>1.下载windows专用版</h2><p>github上有windows专用版</p>
<p>下载链接:<a href="https://github.com/dmajkic/redis/downloads">https://github.com/dmajkic/redis/downloads</a></p>
<p>基本步骤：</p>
<ul>
<li>下载软件，解压</li>
<li>根据系统进入32bit或64bit目录</li>
<li>双击打开redis-server.exe，运行redis服务</li>
<li>双击打开redis-cli.exe，运行命令窗口开始使用</li>
</ul>
<p>这种直接运行的方式只能使用默认配置，配置文件在当前目录下:redis.conf，想要运行多个server的话，复制一份配置文件，修改端口，用cmd在当前窗口打开，<code>redis-server.exe redis2.conf</code></p>
<p>优点：操作简单，下载解压就能用了<br>缺点：版本落后，可能导致学习时部分功能无法使用</p>
<h2 id="2-安装wsl-windows-subsystem-for-linux-–适用于win10"><a href="#2-安装wsl-windows-subsystem-for-linux-–适用于win10" class="headerlink" title="2.安装wsl(windows subsystem for linux)–适用于win10"></a>2.安装wsl(windows subsystem for linux)–适用于win10</h2><p>基本步骤：</p>
<ul>
<li><p>打开windows系统中的wsl功能。可以通过面板打开或管理员模式下的powershell输入<code>Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Windows-Subsystem-Linux</code>，然后重启。</p>
</li>
<li><p>安装ubuntu。在微软应用商店中搜索ubuntu，就可以下载安装对应的系统。目前免费的还有其它系统如debian等，centos只有收费版，本文以ubuntu为例。</p>
</li>
<li><p>apt源改为国内的，不然网速太慢。</p>
<p><code>sudo vim /etc/apt/sources.list</code></p>
<p>  替换为以下内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-updates main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-backports main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-security main restricted universe multiverse</span><br></pre></td></tr></table></figure></li>
<li><p>更新软件源列表 </p>
<p>  <code>sudo apt-get update</code></p>
</li>
<li><p>安装redis-server</p>
<p><code>sudo apt install redis-server</code></p>
<p>配置文件位置：<code>/etc/redis/redis.conf</code></p>
</li>
</ul>
<p>后面就跟在linux中一样了,也可以开启ssh，22端口，使用xshell来连接，假装自己有一台服务器。。。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">配置文件路径：</span><br><span class="line">/etc/ssh/sshd_config</span><br><span class="line">修改3项：</span><br><span class="line">Port 22</span><br><span class="line">PermitRootLogin yes</span><br><span class="line">PasswordAuthentication yes</span><br></pre></td></tr></table></figure>

<p>优点：可以使用新版软件，基本类似linux下的体验。</p>
<p>缺点：跟其它的虚拟机平台冲突，跟大部分安卓模拟器冲突</p>
<h2 id="3-安装虚拟机"><a href="#3-安装虚拟机" class="headerlink" title="3.安装虚拟机"></a>3.安装虚拟机</h2><p>跟上面一种差不多，不展开了。<br>优点：兼容性更好，比wsl稳定<br>缺点：安装较麻烦，使用也不够方便，除非重度用户把虚拟机常驻后台，不然的话每次使用还要打开虚拟机软件，再开启系统，系统开销也大。</p>
<h2 id="4-docker–适用于win10"><a href="#4-docker–适用于win10" class="headerlink" title="4.docker–适用于win10"></a>4.docker–适用于win10</h2><p>当然是使用window下的docker,基本步骤：</p>
<ul>
<li><p>安装docker</p>
<ul>
<li>先开启windows的hyper-v功能。跟上面开启wsl差不多，用管理员模式的powershell的话输入<code>Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V -All</code>,重启。</li>
<li>官网下载windows版docker,一路next安装。<a href="https://www.docker.com/get-started">https://www.docker.com/get-started</a></li>
<li>修改下载源提速。在右下角找到docker图标右击，设置，找到registry-mirrors所在的地方（不同的版本可能不一样，v19.03.8是在“Docker Engine”这里），找个国内的源输进去，如<code>https://registry.docker-cn.com</code>，其实最好是在阿里云上申请一个镜像加速的地址，教程网上有，速度比官方提供的中国区的源要快很多。</li>
</ul>
</li>
<li><p>获取docker image</p>
<p><code>docker pull redis:latest</code></p>
<p>查看镜像<code>docker images</code></p>
</li>
<li><p>创建并运行redis容器</p>
<p><code>docker run -itd --name myredis -p 6379:6379 redis</code><br>查看：<code>docker container ls -all</code></p>
<p>参数：</p>
<ul>
<li>i：以交互模式运行容器，通常跟t一起使用</li>
<li>t:为容器重新分配一个伪输入终端，通常与i一起使用</li>
<li>d:后台运行容器</li>
<li>p:指定端口映射，格式为：<code>本机端口：容器端口</code>，这样，外部就可以用本机IP:本机端口的方式访问到redis</li>
<li>name:容器名称</li>
<li>redis:镜像名称,获取镜像时用redis:latest，运行容器时用redis也可以，毕竟默认就是latest</li>
</ul>
</li>
<li><p>在redis容器执行</p>
<p><code>docker exec -it myredis /bin/bash</code><br>在打开的bash中测试<br><code>redis-cli</code></p>
</li>
</ul>
<p>docker相比于传统的虚拟机技术更加轻便，当然如果只是为了装redis的话这个优势不明显。</p>
<h2 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h2><ul>
<li>推荐使用docker的方法，使用起来方便，且系统开销也小。</li>
<li>如果只是临时用一下，可以选第一种方法。</li>
<li>如果在使用redis的同时，还想使用linux的大部分功能，那可以选择wsl，安装比传统的虚拟机那一套更便捷，稳定性其实也还行。</li>
<li>如果之前已经安装好了虚拟机，或者反感开启hype-v带来的副作用，就选用传统的虚拟机方式，毕竟都是工具，用起来舒服才是最重要的。</li>
</ul>
]]></content>
      <categories>
        <category>工具使用</category>
      </categories>
  </entry>
  <entry>
    <title>JVM之class文件及类加载机制和双亲委派模型</title>
    <url>/2021/06/02/JVM%E4%B9%8Bclass%E6%96%87%E4%BB%B6%E5%8F%8A%E7%B1%BB%E5%8A%A0%E8%BD%BD%E6%9C%BA%E5%88%B6%E5%92%8C%E5%8F%8C%E4%BA%B2%E5%A7%94%E6%B4%BE%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<p>本文先简单介绍虚拟机规范约定的<code>class</code>文件格式，并从一个示例<code>class</code>文件复原代码基本内容，最后描述虚拟机类加载机制以及介绍一下双亲委派模型。Java版本为11。</p>
<span id="more"></span>

<h2 id="1-class文件格式"><a href="#1-class文件格式" class="headerlink" title="1 class文件格式"></a>1 class文件格式</h2><p><code>class</code>文件是字节码文件，文件内容基本单位就是一个字节，文件中不同的项根据约定的格式用一个或多个字节表示。<br>下面是<code>class</code>文件的基本结构：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ClassFile &#123;</span><br><span class="line">    u4             magic;</span><br><span class="line">    u2             minor_version;</span><br><span class="line">    u2             major_version;</span><br><span class="line">    u2             constant_pool_count;</span><br><span class="line">    cp_info        constant_pool[constant_pool_count-1];</span><br><span class="line">    u2             access_flags;</span><br><span class="line">    u2             this_class;</span><br><span class="line">    u2             super_class;</span><br><span class="line">    u2             interfaces_count;</span><br><span class="line">    u2             interfaces[interfaces_count];</span><br><span class="line">    u2             fields_count;</span><br><span class="line">    field_info     fields[fields_count];</span><br><span class="line">    u2             methods_count;</span><br><span class="line">    method_info    methods[methods_count];</span><br><span class="line">    u2             attributes_count;</span><br><span class="line">    attribute_info attributes[attributes_count];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在分别介绍上面的每一个条目之前，先说明几点：</p>
<ul>
<li>“u4”,”u2”分别表示4个字节和2个字节</li>
<li>每一项都是<strong>严格按照顺序</strong>排列的。</li>
<li>cp_info,field_info,method_info,attribute_info,这几个没有标明长度，因为他们对应的条目是复杂结构，其长度根据不同的条目会变化（但是可以从条目信息中获取相应的长度），整个<code>class</code>文件类似一个目录结构</li>
</ul>
<p>下面按顺序解释每一个条目：</p>
<h3 id="magic"><a href="#magic" class="headerlink" title="magic"></a>magic</h3><p>4个字节。其值固定是<code>0xCAFEBABE</code>，这是一个16进制的值，名字起源可参考<a href="https://en.wikipedia.org/wiki/Java_class_file">wiki</a></p>
<h3 id="minor-version"><a href="#minor-version" class="headerlink" title="minor_version"></a>minor_version</h3><p>2个字节。<code>class</code>文件次版本号，现在版本大都为0，以前</p>
<h3 id="major-version"><a href="#major-version" class="headerlink" title="major_version"></a>major_version</h3><p>2个字节。<code>class</code>文件主版本号，比如java11对应的55，虚拟机都是<em>backward compatibility</em>,所以可以支持旧版的<code>class</code>文件，对应的版本支持如下：<br>Java SE | class file format version range<br>———|———-<br> 1.0.2     | 45.0 ≤ v ≤ 45.3<br> 1.1     | 45.0 ≤ v ≤ 45.65535<br> 1.2     | 45.0 ≤ v ≤ 46.0<br> 1.3     |45.0 ≤ v ≤ 47.0<br>1.4         |45.0 ≤ v ≤ 48.0<br>5.0      |45.0 ≤ v ≤ 49.0<br>6         |45.0 ≤ v ≤ 50.0<br>7         |45.0 ≤ v ≤ 51.0<br>8         |45.0 ≤ v ≤ 52.0<br>9         |45.0 ≤ v ≤ 53.0<br>10         |45.0 ≤ v ≤ 54.0<br>11         |45.0 ≤ v ≤ 55.0 </p>
<h3 id="constant-pool-count"><a href="#constant-pool-count" class="headerlink" title="constant_pool_count"></a>constant_pool_count</h3><p>2个字节。常量池条目的数量，值是“实际条目数量+1”，一个说明是因为计算的时候把<code>constant_pool_count</code>本身也算在内了，其下标是0。</p>
<h3 id="constant-pool-constant-pool-count-1"><a href="#constant-pool-constant-pool-count-1" class="headerlink" title="constant_pool[constant_pool_count-1]"></a>constant_pool[constant_pool_count-1]</h3><p>常量池，对应的条目下标编号是从1开始，直到<code>constant_pool_count-1</code>,每一个条目的基本结构如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cp_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u1 info[];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>tag</code>表是这个条目是什么类型的，<code>info[]</code>表示后面的具体信息。<br>比如，<code>tag</code>是1表示<code>CONSTANT_Utf8</code>,对应的结构如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>注意，这里的tag和上面tag是同一个，也就是说，length和bytes[length]替换了<code>info[]</code>的位置。</strong><br>全部的常量池类型参考<a href="https://docs.oracle.com/javase/specs/jvms/se11/html/jvms-4.html#jvms-4.4">虚拟机规范</a></p>
<h3 id="access-flags"><a href="#access-flags" class="headerlink" title="access_flags"></a>access_flags</h3><p>2个字节。类型的一些信息，可以是几个值的组合，每个FLAG信息如下：</p>
<table>
<thead>
<tr>
<th>Flag Name</th>
<th>Value</th>
<th>Interpretation</th>
</tr>
</thead>
<tbody><tr>
<td>ACC_PUBLIC</td>
<td>0x0001</td>
<td>Declared public; may be accessed from outside its package.</td>
</tr>
<tr>
<td>ACC_FINAL</td>
<td>0x0010</td>
<td>Declared final; no subclasses allowed.</td>
</tr>
<tr>
<td>ACC_SUPER</td>
<td>0x0020</td>
<td>Treat superclass methods specially when invoked by the invokespecial instruction.</td>
</tr>
<tr>
<td>ACC_INTERFACE</td>
<td>0x0200</td>
<td>Is an interface, not a class.</td>
</tr>
<tr>
<td>ACC_ABSTRACT</td>
<td>0x0400</td>
<td>Declared abstract; must not be instantiated.</td>
</tr>
<tr>
<td>ACC_SYNTHETIC</td>
<td>0x1000</td>
<td>Declared synthetic; not present in the source code.</td>
</tr>
<tr>
<td>ACC_ANNOTATION</td>
<td>0x2000</td>
<td>Declared as an annotation type.</td>
</tr>
<tr>
<td>ACC_ENUM</td>
<td>0x4000</td>
<td>Declared as an enum type.</td>
</tr>
<tr>
<td>ACC_MODULE</td>
<td>0x8000</td>
<td>Is a module, not a class or interface.</td>
</tr>
</tbody></table>
<blockquote>
<p>上面的value用的是16进制表示，转化成2进制可以发现这些值都是1个16位的二进制数在不同的位取1，其它15位都是0，这种方法可以非常方便的用二进制操作来判断对应的FLAG是否存在，比如我们的FLAGS值是：<code>0010 0010 0000 0000</code>，用<code>ACC_INTERFACE</code>的值<code>0000 0010 0000 0000</code>去跟FLAGS的值做与运算，返回不是0的值，就表示<code>ACC_INTERFACE</code>这个FLAG存在。不同的FLAG对应的值就是<a href="https://en.wikipedia.org/wiki/Mask_(computing)">位掩码(mask)</a>。</p>
</blockquote>
<h3 id="this-class"><a href="#this-class" class="headerlink" title="this_class"></a>this_class</h3><p>2个字节。指向常量池中一个条目，被指向的条目必须是一个<code>CONSTANT_Class_info</code>类型。</p>
<h3 id="super-class"><a href="#super-class" class="headerlink" title="super_class"></a>super_class</h3><p>2个字节。通常是一个非0值指向常量池中一个条目，被指向的条目必须是一个<code>CONSTANT_Class_info</code>类型。只有<code>Object</code>这个类本身的<code>class</code>文件，这个值是0。</p>
<h3 id="interfaces-count"><a href="#interfaces-count" class="headerlink" title="interfaces_count"></a>interfaces_count</h3><p>2个字节。表示当前<code>class</code>直接实现或者继承（对于接口class)的接口数量。</p>
<h3 id="interfaces-interfaces-count"><a href="#interfaces-interfaces-count" class="headerlink" title="interfaces[interfaces_count]"></a>interfaces[interfaces_count]</h3><p>2个字节为单位。每个值指向常量池内的一个条目，被指向的条目必须是一个<code>CONSTANT_Class_info</code>类型。</p>
<h3 id="fields-count"><a href="#fields-count" class="headerlink" title="fields_count"></a>fields_count</h3><p>2个字节。表示<code>field</code>的个数，<code>field</code>包括类的字段和实例的字段。</p>
<h3 id="fields-fields-count"><a href="#fields-fields-count" class="headerlink" title="fields[fields_count]"></a>fields[fields_count]</h3><p><code>field</code>集合。单个<code>field</code>的基本结构如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">field_info &#123;</span><br><span class="line">    u2             access_flags;</span><br><span class="line">    u2             name_index;</span><br><span class="line">    u2             descriptor_index;</span><br><span class="line">    u2             attributes_count;</span><br><span class="line">    attribute_info attributes[attributes_count];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>access_flags:跟前面描述的<a href="#access_flags">access_flags</a>区别就是<code>class</code>和<code>field</code>可以匹配的FLAG会有差别，比如<code>ACC_PROTECTED</code>(0x0004)在<code>class</code>中就没有。具体参考<a href="https://docs.oracle.com/javase/specs/jvms/se11/html/jvms-4.html#jvms-4.5">虚拟机规范</a>。</li>
<li> name_index：指向常量池的条目，被指向的条目类型必须是<code>CONSTANT_Utf8_info</code>，其值必须是一个有效的非全限定名（关于名字中不能使用一些特殊字符，<a href="https://docs.oracle.com/javase/specs/jvms/se11/html/jvms-4.html#jvms-4.2.2">参考这里</a>）</li>
<li>descriptor_index：指向常量池的条目，被指向的条目类型必须是<code>CONSTANT_Utf8_info</code>，其值是一个有效的<code>field descriptor</code>,内容表示<code>field</code>的type,分下列3种：  <ul>
<li>BaseType:对应8大原始类型，<code>B,C,D,F,I,J,S,Z</code>分别表示<code>byte,char,double,float,int,long,short,boolean</code>，除了<code>long，boolean</code>，其它的都是单词的首字母</li>
<li>ObjectType:对象类型，字母<code>L</code>开头,<code>;</code>结尾，格式：<code>LClassName;</code>如<code>Ljava/lang/String;</code></li>
<li>ArrayType:数组类型，<code>[</code>开头，格式<code>[ComponentType</code>，如<code>[Ljava/lang/String;</code></li>
</ul>
</li>
<li>attributes_count:属性的数量</li>
<li>attributes[attributes_count]：属性的具体内容。属性这个条目可能在类、方法、字段这些条目中出现，我们放到最后类的属性时再讲。<h3 id="methods-count"><a href="#methods-count" class="headerlink" title="methods_count"></a>methods_count</h3>2个字节。表示<code>method</code>的数量。<h3 id="methods-methods-count"><a href="#methods-methods-count" class="headerlink" title="methods[methods_count]"></a>methods[methods_count]</h3><code>method</code>集合。单个<code>method</code>的基本结构如下：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">method_info &#123;</span><br><span class="line">    u2             access_flags;</span><br><span class="line">    u2             name_index;</span><br><span class="line">    u2             descriptor_index;</span><br><span class="line">    u2             attributes_count;</span><br><span class="line">    attribute_info attributes[attributes_count];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><code>method</code>和<a href="#fields%5Bfields_count%5D">field</a>基本结构是一样的，只说不同点：<code>descriptor_index</code><strong>所指向的这个条目</strong>的结构不同，这里指向的是<code>method descriptor</code>，基本格式是<code>( &#123;ParameterDescriptor&#125; ) ReturnDescriptor</code>：<ul>
<li>{ParameterDescriptor}：表示0个-多个参数，类型是上面说的<code>field type</code>的3种类型之一，参数之间不用任何符号分隔</li>
<li>ReturnDescriptor：表示返回值，取值可以是<code>field type</code>的3种类型之一或者<code>V</code>(表示void)<br>举个例子：<code>Object m(int i, double d, Thread t) &#123;...&#125;</code>的<code>method descriptor</code>是：<code>(IDLjava/lang/Thread;)Ljava/lang/Object;</code><h3 id="attributes-count"><a href="#attributes-count" class="headerlink" title="attributes_count"></a>attributes_count</h3>2个字节。表示该<code>class</code>的<code>attribute</code>数量。<h3 id="attributes-attributes-count"><a href="#attributes-attributes-count" class="headerlink" title="attributes[attributes_count]"></a>attributes[attributes_count]</h3><code>attribute</code>集合。单个<code>attributes</code>的基本结构如下：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">attribute_info &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u1 info[attribute_length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
跟<a href="#constant_pool%5Bconstant_pool_count-1%5D">常量池</a>内的条目一样，前面两项<code>attribute_name_index</code>和<code>attribute_length</code>是所有<code>attribute</code>共有的，<code>info[attribute_length]</code>根据属性的不同使用不同的格式。<br>这里的<code>attribute_length</code>长度是指其后内容的字节数，跟内容的表示方式无关，有的属性长度值是固定的，有的是不固定的。<br>比如，<code>ConstantValue</code>，长度就是2：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ConstantValue_attribute &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u2 constantvalue_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
属性的内容非常多，也很复杂，展开的话超出了本文的范围，可以参考<a href="https://docs.oracle.com/javase/specs/jvms/se11/html/jvms-4.html#jvms-4.7">虚拟机规范</a>。</li>
</ul>
</li>
</ul>
<h2 id="2-还原一个class文件的内容"><a href="#2-还原一个class文件的内容" class="headerlink" title="2 还原一个class文件的内容"></a>2 还原一个class文件的内容</h2><p>根据上面的知识，我们按class文件的字节流顺序还原其内容。</p>
<p>首先，我们构造一个类，让它实现1个空的接口，继承1个空的父类，定义2个属性，1个方法，使用1个空的注解。<br>类<code>Test</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@MyAnnotation</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> <span class="keyword">extends</span> <span class="title">SuperTest</span> <span class="keyword">implements</span> <span class="title">IA</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> a=<span class="number">1</span>;</span><br><span class="line">    String s=<span class="string">&quot;hello world&quot;</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">sayHello</span><span class="params">(String aString,Integer anInt,<span class="keyword">double</span> aDouble)</span></span>&#123;</span><br><span class="line">        System.out.println(aString);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>空的接口<code>IA</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">IA</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>空的父类<code>SuperTest</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class SuperTest &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>空的注解<code>MyAnnotation</code>,保留至<code>CLASS</code>级别</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.annotation.Retention;</span><br><span class="line"><span class="keyword">import</span> java.lang.annotation.RetentionPolicy;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.CLASS)</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyAnnotation &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编译后，用16进制查看<code>Test.class</code>文件，内容如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CA FE BA BE 00 00 00 37 00 2B 0A 00 08 00 1B 09 </span><br><span class="line">00 07 00 1C 08 00 1D 09 00 07 00 1E 09 00 1F 00 </span><br><span class="line">20 0A 00 21 00 22 07 00 23 07 00 24 07 00 25 01 </span><br><span class="line">00 01 61 01 00 01 49 01 00 01 73 01 00 12 4C 6A </span><br><span class="line">61 76 61 2F 6C 61 6E 67 2F 53 74 72 69 6E 67 3B </span><br><span class="line">01 00 06 3C 69 6E 69 74 3E 01 00 03 28 29 56 01 </span><br><span class="line">00 04 43 6F 64 65 01 00 0F 4C 69 6E 65 4E 75 6D </span><br><span class="line">62 65 72 54 61 62 6C 65 01 00 12 4C 6F 63 61 6C </span><br><span class="line">56 61 72 69 61 62 6C 65 54 61 62 6C 65 01 00 04 </span><br><span class="line">74 68 69 73 01 00 06 4C 54 65 73 74 3B 01 00 08 </span><br><span class="line">73 61 79 48 65 6C 6C 6F 01 00 15 28 4C 6A 61 76 </span><br><span class="line">61 2F 6C 61 6E 67 2F 53 74 72 69 6E 67 3B 29 56 </span><br><span class="line">01 00 0A 53 6F 75 72 63 65 46 69 6C 65 01 00 09 </span><br><span class="line">54 65 73 74 2E 6A 61 76 61 01 00 1B 52 75 6E 74 </span><br><span class="line">69 6D 65 49 6E 76 69 73 69 62 6C 65 41 6E 6E 6F </span><br><span class="line">74 61 74 69 6F 6E 73 01 00 0E 4C 4D 79 41 6E 6E </span><br><span class="line">6F 74 61 74 69 6F 6E 3B 0C 00 0E 00 0F 0C 00 0A </span><br><span class="line">00 0B 01 00 0B 68 65 6C 6C 6F 20 77 6F 72 6C 64 </span><br><span class="line">0C 00 0C 00 0D 07 00 26 0C 00 27 00 28 07 00 29 </span><br><span class="line">0C 00 2A 00 16 01 00 04 54 65 73 74 01 00 09 53 </span><br><span class="line">75 70 65 72 54 65 73 74 01 00 02 49 41 01 00 10 </span><br><span class="line">6A 61 76 61 2F 6C 61 6E 67 2F 53 79 73 74 65 6D </span><br><span class="line">01 00 03 6F 75 74 01 00 15 4C 6A 61 76 61 2F 69 </span><br><span class="line">6F 2F 50 72 69 6E 74 53 74 72 65 61 6D 3B 01 00 </span><br><span class="line">13 6A 61 76 61 2F 69 6F 2F 50 72 69 6E 74 53 74 </span><br><span class="line">72 65 61 6D 01 00 07 70 72 69 6E 74 6C 6E 00 21 </span><br><span class="line">00 07 00 08 00 01 00 09 00 02 00 00 00 0A 00 0B </span><br><span class="line">00 00 00 00 00 0C 00 0D 00 00 00 02 00 01 00 0E </span><br><span class="line">00 0F 00 01 00 10 00 00 00 42 00 02 00 01 00 00 </span><br><span class="line">00 10 2A B7 00 01 2A 04 B5 00 02 2A 12 03 B5 00 </span><br><span class="line">04 B1 00 00 00 02 00 11 00 00 00 0E 00 03 00 00 </span><br><span class="line">00 02 00 04 00 03 00 09 00 04 00 12 00 00 00 0C </span><br><span class="line">00 01 00 00 00 10 00 13 00 14 00 00 00 01 00 15 </span><br><span class="line">00 16 00 01 00 10 00 00 00 40 00 02 00 02 00 00 </span><br><span class="line">00 08 B2 00 05 2B B6 00 06 B1 00 00 00 02 00 11 </span><br><span class="line">00 00 00 0A 00 02 00 00 00 06 00 07 00 07 00 12 </span><br><span class="line">00 00 00 16 00 02 00 00 00 08 00 13 00 14 00 00 </span><br><span class="line">00 00 00 08 00 0C 00 0D 00 01 00 02 00 17 00 00 </span><br><span class="line">00 02 00 18 00 19 00 00 00 06 00 01 00 1A 00 00</span><br></pre></td></tr></table></figure>
<p>下面我们按相同的顺序分析这些字节码（建议把字节码复制出来，一边分析一边删除）：</p>
<h3 id="magic-1"><a href="#magic-1" class="headerlink" title="magic"></a>magic</h3><p><code>CA FE BA BE</code>4个字节</p>
<h3 id="minor-version-1"><a href="#minor-version-1" class="headerlink" title="minor_version"></a>minor_version</h3><p><code>00 00</code>，次版本号，0</p>
<h3 id="major-version-1"><a href="#major-version-1" class="headerlink" title="major_version"></a>major_version</h3><p><code>00 37</code>,主版本号，3x16+7=55</p>
<h3 id="constant-pool-count-1"><a href="#constant-pool-count-1" class="headerlink" title="constant_pool_count"></a>constant_pool_count</h3><p><code>00 2B</code>，常量池数量，2x16+11=43，表示常量池有42个具体的条目</p>
<h3 id="constant-pool-constant-pool-count-1-1"><a href="#constant-pool-constant-pool-count-1-1" class="headerlink" title="constant_pool[constant_pool_count-1]"></a>constant_pool[constant_pool_count-1]</h3><p>分别列出42个条目（手动分析前面的条目时还不知道后面条目的内容，这种情况都是回头补充的）：</p>
<h4 id="1-CONSTANT-Methodref"><a href="#1-CONSTANT-Methodref" class="headerlink" title="1 CONSTANT_Methodref"></a>1 CONSTANT_Methodref</h4><p><code>0A</code>开头，说明是<code>CONSTANT_Methodref</code>类型，根据结构：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Methodref_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 class_index;</span><br><span class="line">    u2 name_and_type_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>class_index:<code>00 08</code>，第8个条目，<code>SuperTest</code><br>name_and_type_index:<code>00 1B</code>,第27个条目,<code>&lt;init&gt;:()V</code></p>
<h4 id="2-CONSTANT-Fieldref"><a href="#2-CONSTANT-Fieldref" class="headerlink" title="2 CONSTANT_Fieldref"></a>2 CONSTANT_Fieldref</h4><p><code>09</code>开头，说明是<code>CONSTANT_Fieldref</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Fieldref_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 class_index;</span><br><span class="line">    u2 name_and_type_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>class_index：<code>00 07</code>，第7个条目，<code>Test</code><br>name_and_type_index:<code>00 1C</code>,第28个条目，<code>a:I</code></p>
<h4 id="3-CONSTANT-String"><a href="#3-CONSTANT-String" class="headerlink" title="3 CONSTANT_String"></a>3 CONSTANT_String</h4><p><code>08</code>开头，说明是<code>CONSTANT_String</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_String_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 string_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>string_index：<code>00 1D</code>，第29个条目，<code>hello world</code></p>
<h4 id="4-CONSTANT-Fieldref"><a href="#4-CONSTANT-Fieldref" class="headerlink" title="4 CONSTANT_Fieldref"></a>4 CONSTANT_Fieldref</h4><p><code>09</code>开头，说明是<code>CONSTANT_Fieldref</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Fieldref_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 class_index;</span><br><span class="line">    u2 name_and_type_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>class_index：<code>00 07</code>，第7个条目，<code>Test</code><br>name_and_type_index：<code>00 1E</code>，第30个条目，<code>s:Ljava/lang/String;</code></p>
<h4 id="5-CONSTANT-Fieldref"><a href="#5-CONSTANT-Fieldref" class="headerlink" title="5 CONSTANT_Fieldref"></a>5 CONSTANT_Fieldref</h4><p><code>09</code>开头，说明是<code>CONSTANT_Fieldref</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Fieldref_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 class_index;</span><br><span class="line">    u2 name_and_type_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>class_index：<code>00 1F</code>，第31个条目,<code>java/lang/System</code><br>name_and_type_index：<code>00 20</code>第32个条目,<code>out:Ljava/io/PrintStream;</code></p>
<h5 id="6-CONSTANT-Methodref"><a href="#6-CONSTANT-Methodref" class="headerlink" title="6 CONSTANT_Methodref"></a>6 CONSTANT_Methodref</h5><p><code>0A</code>开头，说明是<code>CONSTANT_Methodref</code>类型，根据结构：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Methodref_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 class_index;</span><br><span class="line">    u2 name_and_type_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>class_index：<code>00 21</code>,第33个条目，<code>java/io/PrintStream</code><br>name_and_type_index：<code>00 22</code>，第34个条目，<code>println:(Ljava/lang/String;)V</code></p>
<h4 id="7-CONSTANT-Class"><a href="#7-CONSTANT-Class" class="headerlink" title="7 CONSTANT_Class"></a>7 CONSTANT_Class</h4><p><code>07</code>开头，说明是<code>CONSTANT_Class</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Class_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 23</code>,第35个条目，<code>Test</code></p>
<h4 id="8-CONSTANT-Class"><a href="#8-CONSTANT-Class" class="headerlink" title="8 CONSTANT_Class"></a>8 CONSTANT_Class</h4><p><code>07</code>开头，说明是<code>CONSTANT_Class</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Class_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 24</code>,第36个条目，<code>SuperTest</code></p>
<h4 id="9-CONSTANT-Class"><a href="#9-CONSTANT-Class" class="headerlink" title="9 CONSTANT_Class"></a>9 CONSTANT_Class</h4><p><code>07</code>开头，说明是<code>CONSTANT_Class</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Class_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 25</code>,第37个条目，<code>IA</code></p>
<h4 id="10-CONSTANT-Utf8"><a href="#10-CONSTANT-Utf8" class="headerlink" title="10 CONSTANT_Utf8"></a>10 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 01</code>,长度1个字节<br>bytes[1]:0x61=6x16+1=97,查ascii表得知是：<code>a</code></p>
<h4 id="11-CONSTANT-Utf8"><a href="#11-CONSTANT-Utf8" class="headerlink" title="11 CONSTANT_Utf8"></a>11 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 01</code>,长度1个字节<br>bytes[1]:0x49=4x16+9=73,查ascii表得知是：<code>I</code></p>
<h4 id="12-CONSTANT-Utf8"><a href="#12-CONSTANT-Utf8" class="headerlink" title="12 CONSTANT_Utf8"></a>12 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 01</code>,长度1个字节<br>bytes[1]:0x73=7x16+3=115,查ascii表得知是：<code>s</code></p>
<h4 id="13-CONSTANT-Utf8"><a href="#13-CONSTANT-Utf8" class="headerlink" title="13 CONSTANT_Utf8"></a>13 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 12</code>,长度0x12=1x16+2=18个字节<br>bytes[18]:<code>4C 6A 61 76 61 2F 6C 61 6E 67 2F 53 74 72 69 6E 67 3B</code>,对应的ascii值是<code>L j a v a / l a n g / S t r i n g ;</code></p>
<h4 id="14-CONSTANT-Utf8"><a href="#14-CONSTANT-Utf8" class="headerlink" title="14 CONSTANT_Utf8"></a>14 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 06</code>,长度6个字节<br>bytes[6]:<code>3C 69 6E 69 74 3E</code>,对应的ascii值是<code>&lt; i n i t &gt;</code></p>
<h4 id="15-CONSTANT-Utf8"><a href="#15-CONSTANT-Utf8" class="headerlink" title="15 CONSTANT_Utf8"></a>15 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 03</code>,长度3个字节<br>bytes[3]:<code>28 29 56</code>,对应的ascii值是<code>( ) V</code></p>
<h4 id="16-CONSTANT-Utf8"><a href="#16-CONSTANT-Utf8" class="headerlink" title="16 CONSTANT_Utf8"></a>16 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 04</code>,长度4个字节<br>bytes[4]:<code>43 6F 64 65</code>,对应的ascii值是<code>C o d e</code></p>
<h4 id="17-CONSTANT-Utf8"><a href="#17-CONSTANT-Utf8" class="headerlink" title="17 CONSTANT_Utf8"></a>17 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 0F</code>,长度15个字节<br>bytes[15]:<code>4C 69 6E 65 4E 75 6D 62 65 72 54 61 62 6C 65</code>,对应的ascii值是<code>L i n e N u m b e r T a b l e</code></p>
<h4 id="18-CONSTANT-Utf8"><a href="#18-CONSTANT-Utf8" class="headerlink" title="18 CONSTANT_Utf8"></a>18 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 12</code>,长度0x12=1x16+2=18个字节<br>bytes[18]:<code>4C 6F 63 61 6C 56 61 72 69 61 62 6C 65 54 61 62 6C 65</code>,对应的ascii值是<code>L o c a l V a r i a b l e T a b l e</code></p>
<h4 id="19-CONSTANT-Utf8"><a href="#19-CONSTANT-Utf8" class="headerlink" title="19 CONSTANT_Utf8"></a>19 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 04</code>,长度4个字节<br>bytes[4]:<code>74 68 69 73</code>,对应的ascii值是<code>t h i s</code></p>
<h4 id="20-CONSTANT-Utf8"><a href="#20-CONSTANT-Utf8" class="headerlink" title="20 CONSTANT_Utf8"></a>20 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 06</code>,长度6个字节<br>bytes[6]:<code>4C 54 65 73 74 3B</code>,对应的ascii值是<code>L T e s t ;</code></p>
<h4 id="21-CONSTANT-Utf8"><a href="#21-CONSTANT-Utf8" class="headerlink" title="21 CONSTANT_Utf8"></a>21 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 08</code>,长度8个字节<br>bytes[8]:<code>73 61 79 48 65 6C 6C 6F</code>,对应的ascii值是<code>s a y H e l l o</code></p>
<h4 id="22-CONSTANT-Utf8"><a href="#22-CONSTANT-Utf8" class="headerlink" title="22 CONSTANT_Utf8"></a>22 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 15</code>,长度0x15=1*16+5=21个字节<br>bytes[21]:<code>28 4C 6A 61 76 61 2F 6C 61 6E 67 2F 53 74 72 69 6E 67 3B 29 56</code>,对应的ascii值是<code>( L j a v a / l a n g / S t r i n g ; ) V</code></p>
<h4 id="23-CONSTANT-Utf8"><a href="#23-CONSTANT-Utf8" class="headerlink" title="23 CONSTANT_Utf8"></a>23 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 0A</code>,长度10个字节<br>bytes[10]:<code>53 6F 75 72 63 65 46 69 6C 65</code>,对应的ascii值是<code>S o u r c e F i l e</code></p>
<h4 id="24-CONSTANT-Utf8"><a href="#24-CONSTANT-Utf8" class="headerlink" title="24 CONSTANT_Utf8"></a>24 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 09</code>,长度9个字节<br>bytes[9]:<code>54 65 73 74 2E 6A 61 76 61</code>,对应的ascii值是<code>T e s t . j a v a</code></p>
<h4 id="25-CONSTANT-Utf8"><a href="#25-CONSTANT-Utf8" class="headerlink" title="25 CONSTANT_Utf8"></a>25 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 1B</code>,长度0x1B=1x16+11=27个字节<br>bytes[27]:<code>52 75 6E 74 69 6D 65 49 6E 76 69 73 69 62 6C 65 41 6E 6E 6F 74 61 74 69 6F 6E 73</code>,对应的ascii值是<code>R u n t i m e I n v i s i b l e A n n o t a t i o n s</code></p>
<h4 id="26-CONSTANT-Utf8"><a href="#26-CONSTANT-Utf8" class="headerlink" title="26 CONSTANT_Utf8"></a>26 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 0E</code>,长度14个字节<br>bytes[14]:<code>4C 4D 79 41 6E 6E 6F 74 61 74 69 6F 6E 3B</code>,对应的ascii值是<code>L M y A n n o t a t i o n ;</code></p>
<h4 id="27-CONSTANT-NameAndType"><a href="#27-CONSTANT-NameAndType" class="headerlink" title="27 CONSTANT_NameAndType"></a>27 CONSTANT_NameAndType</h4><p><code>0C</code>开头，说明是<code>CONSTANT_NameAndType</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_NameAndType_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">    u2 descriptor_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 0E</code>，第14个条目，<code>&lt;init&gt;</code><br>descriptor_index:<code>00 0F</code>,第15个条目，<code>()V</code></p>
<h4 id="28-CONSTANT-NameAndType"><a href="#28-CONSTANT-NameAndType" class="headerlink" title="28 CONSTANT_NameAndType"></a>28 CONSTANT_NameAndType</h4><p><code>0C</code>开头，说明是<code>CONSTANT_NameAndType</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_NameAndType_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">    u2 descriptor_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 0A</code>，第10个条目，<code>a</code><br>descriptor_index:<code>00 0B</code>,第11个条目，<code>I</code></p>
<h4 id="29-CONSTANT-Utf8"><a href="#29-CONSTANT-Utf8" class="headerlink" title="29 CONSTANT_Utf8"></a>29 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 0B</code>,长度11个字节<br>bytes[11]:<code>68 65 6C 6C 6F 20 77 6F 72 6C 64</code>,对应的ascii值是<code>h e l l o 空格 w o r l d</code></p>
<h4 id="30-CONSTANT-NameAndType"><a href="#30-CONSTANT-NameAndType" class="headerlink" title="30 CONSTANT_NameAndType"></a>30 CONSTANT_NameAndType</h4><p><code>0C</code>开头，说明是<code>CONSTANT_NameAndType</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_NameAndType_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">    u2 descriptor_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 0C</code>，第12个条目，<code>s</code><br>descriptor_index:<code>00 0D</code>,第13个条目，<code>Ljava/lang/String;</code></p>
<h4 id="31-CONSTANT-Class"><a href="#31-CONSTANT-Class" class="headerlink" title="31 CONSTANT_Class"></a>31 CONSTANT_Class</h4><p><code>07</code>开头，说明是<code>CONSTANT_Class</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Class_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 26</code>,第0x26=2x16+6=38个条目，<code>java/lang/System</code></p>
<h4 id="32-CONSTANT-NameAndType"><a href="#32-CONSTANT-NameAndType" class="headerlink" title="32 CONSTANT_NameAndType"></a>32 CONSTANT_NameAndType</h4><p><code>0C</code>开头，说明是<code>CONSTANT_NameAndType</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_NameAndType_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">    u2 descriptor_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 27</code>，第0x27=2x16+7=39个条目，<code>out</code><br>descriptor_index:<code>00 28</code>,第0x28=2x16+8=40个条目，<code>Ljava/io/PrintStream;</code></p>
<h4 id="33-CONSTANT-Class"><a href="#33-CONSTANT-Class" class="headerlink" title="33 CONSTANT_Class"></a>33 CONSTANT_Class</h4><p><code>07</code>开头，说明是<code>CONSTANT_Class</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Class_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 29</code>,第0x29=2x16+9=41个条目，<code>java/io/PrintStream</code></p>
<h4 id="34-CONSTANT-NameAndType"><a href="#34-CONSTANT-NameAndType" class="headerlink" title="34 CONSTANT_NameAndType"></a>34 CONSTANT_NameAndType</h4><p><code>0C</code>开头，说明是<code>CONSTANT_NameAndType</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_NameAndType_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 name_index;</span><br><span class="line">    u2 descriptor_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>name_index:<code>00 2A</code>，第0x2A=2x16+10=42个条目，<code>println</code><br>descriptor_index:<code>00 16</code>,第0x16=1x16+6=22个条目，<code>(Ljava/lang/String;)V</code></p>
<h4 id="35-CONSTANT-Utf8"><a href="#35-CONSTANT-Utf8" class="headerlink" title="35 CONSTANT_Utf8"></a>35 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 04</code>,长度4个字节<br>bytes[4]:<code>54 65 73 74</code>,对应的ascii值是<code>T e s t</code></p>
<h4 id="36-CONSTANT-Utf8"><a href="#36-CONSTANT-Utf8" class="headerlink" title="36 CONSTANT_Utf8"></a>36 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 09</code>,长度9个字节<br>bytes[9]:<code>53 75 70 65 72 54 65 73 74</code>,对应的ascii值是<code>S u p e r T e s t</code></p>
<h4 id="37-CONSTANT-Utf8"><a href="#37-CONSTANT-Utf8" class="headerlink" title="37 CONSTANT_Utf8"></a>37 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 02</code>,长度2个字节<br>bytes[2]:<code>49 41</code>,对应的ascii值是<code>IA</code></p>
<h4 id="38-CONSTANT-Utf8"><a href="#38-CONSTANT-Utf8" class="headerlink" title="38 CONSTANT_Utf8"></a>38 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 10</code>,长度0x10=1x16+0=16个字节<br>bytes[16]:<code>6A 61 76 61 2F 6C 61 6E 67 2F 53 79 73 74 65 6D</code>,对应的ascii值是<code>j a v a / l a n g / S y s t e m</code></p>
<h4 id="39-CONSTANT-Utf8"><a href="#39-CONSTANT-Utf8" class="headerlink" title="39 CONSTANT_Utf8"></a>39 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 03</code>,长度3个字节<br>bytes[3]:<code>6F 75 74</code>,对应的ascii值是<code>o u t</code></p>
<h4 id="40-CONSTANT-Utf8"><a href="#40-CONSTANT-Utf8" class="headerlink" title="40 CONSTANT_Utf8"></a>40 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 15</code>,长度0x15=1x16+5=21个字节<br>bytes[21]:<code>4C 6A 61 76 61 2F 69 6F 2F 50 72 69 6E 74 53 74 72 65 61 6D 3B</code>,对应的ascii值是<code>L j a v a / i o / P r i n t S t r e a m ;</code></p>
<h4 id="41-CONSTANT-Utf8"><a href="#41-CONSTANT-Utf8" class="headerlink" title="41 CONSTANT_Utf8"></a>41 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 13</code>,长度0x13=1x16+3=19个字节<br>bytes[19]:<code>6A 61 76 61 2F 69 6F 2F 50 72 69 6E 74 53 74 72 65 61 6D </code>,对应的ascii值是<code>j a v a / i o / P r i n t S t r e a m</code></p>
<h4 id="42-CONSTANT-Utf8"><a href="#42-CONSTANT-Utf8" class="headerlink" title="42 CONSTANT_Utf8"></a>42 CONSTANT_Utf8</h4><p><code>01</code>开头，说明是<code>CONSTANT_Utf8</code>类型，根据结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONSTANT_Utf8_info &#123;</span><br><span class="line">    u1 tag;</span><br><span class="line">    u2 length;</span><br><span class="line">    u1 bytes[length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>length:<code>00 07</code>,长度7个字节<br>bytes[7]:<code>70 72 69 6E 74 6C 6E</code>,对应的ascii值是<code>p r i n t l n</code></p>
<h3 id="access-flags-1"><a href="#access-flags-1" class="headerlink" title="access_flags"></a>access_flags</h3><p><code>00 21</code>,表示<code>0020+0001</code>，所以是<code>ACC_SUPER</code>,<code>ACC_PUBLIC</code>,这里的<code>ACC_SUPER</code>是为了兼容性存在的，java 8以后每个<code>class</code>文件都有，现在已经没有实际意义。</p>
<h3 id="this-class-1"><a href="#this-class-1" class="headerlink" title="this_class"></a>this_class</h3><p><code>00 07</code>，指向常量池第7个条目，即<code>Test</code></p>
<h3 id="super-class-1"><a href="#super-class-1" class="headerlink" title="super_class"></a>super_class</h3><p><code>00 08</code>，指向常量池第8个条目，即<code>SuperTest</code></p>
<h3 id="interfaces-count-1"><a href="#interfaces-count-1" class="headerlink" title="interfaces_count"></a>interfaces_count</h3><p><code>00 01</code>，接口数量，1个</p>
<h3 id="interfaces"><a href="#interfaces" class="headerlink" title="interfaces[]"></a>interfaces[]</h3><p>指向常量池的条目，因为这里只有1个接口，所以取2个字节，<code>00 09</code>，第9个条目，即<code>IA</code></p>
<h3 id="fields-count-1"><a href="#fields-count-1" class="headerlink" title="fields_count"></a>fields_count</h3><p><code>00 02</code>，<code>field</code>数量，2条。</p>
<h3 id="fields-fields-count-1"><a href="#fields-fields-count-1" class="headerlink" title="fields[fields_count]"></a>fields[fields_count]</h3><p>因为有2个<code>field</code>,根据下面的基本结构分析：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">field_info &#123;</span><br><span class="line">    u2             access_flags;</span><br><span class="line">    u2             name_index;</span><br><span class="line">    u2             descriptor_index;</span><br><span class="line">    u2             attributes_count;</span><br><span class="line">    attribute_info ;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="第1个field"><a href="#第1个field" class="headerlink" title="第1个field"></a>第1个field</h4><ul>
<li>access_flags:<code>00 02</code>,<code>ACC_PRIVATE</code></li>
<li>name_index:<code>00 0A</code>,指向第10个条目，即<code>a</code></li>
<li>descriptor_index:<code>00 0B</code>,指向第11个条目，即<code>I</code></li>
<li>attributes_count：<code>00 00</code>，0个</li>
<li>attributes[attributes_count]，因为是0个，所以这里没有。<h4 id="第2个field"><a href="#第2个field" class="headerlink" title="第2个field"></a>第2个field</h4></li>
<li>access_flags:<code>00 00</code>,没有FLAG</li>
<li>name_index:<code>00 0C</code>,指向第12个条目，即<code>s</code></li>
<li>descriptor_index:<code>00 0D</code>,指向第13个条目，即<code>Ljava/lang/String;</code></li>
<li>attributes_count：<code>00 00</code>，0个</li>
<li>attributes[attributes_count]，因为是0个，所以这里没有。</li>
</ul>
<h3 id="methods-count-1"><a href="#methods-count-1" class="headerlink" title="methods_count"></a>methods_count</h3><p><code>00 02</code>，方法数量，2个。</p>
<h3 id="methods-methods-count-1"><a href="#methods-methods-count-1" class="headerlink" title="methods[methods_count]"></a>methods[methods_count]</h3><p>因为有2个<code>method</code>,根据下面的基本结构分析：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">method_info &#123;</span><br><span class="line">    u2             access_flags;</span><br><span class="line">    u2             name_index;</span><br><span class="line">    u2             descriptor_index;</span><br><span class="line">    u2             attributes_count;</span><br><span class="line">    attribute_info attributes[attributes_count];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="第一个方法"><a href="#第一个方法" class="headerlink" title="第一个方法"></a>第一个方法</h4><ul>
<li><p>access_flags:<code>00 01</code>,即<code>ACC_PUBLIC</code></p>
</li>
<li><p>name_index：<code>00 0E</code>，指向第13个条目，即<code>Ljava/lang/String;</code></p>
</li>
<li><p>descriptor_index:<code>00 0F</code>,第向第14个条目，即<code>&lt;init&gt;</code></p>
</li>
<li><p>attributes_count：<code>00 01</code>，属性数量，1条</p>
</li>
<li><p>attributes[1]：属性内容，根据下面的结构展开：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">attribute_info &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u1 info[attribute_length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  attribute_name_index：<code>00 10</code>，第16个条目，即<code>Code</code>，说明我们的属性是<code>Code</code><br>  attribute_length：<code>00 00 00 42</code>，长度0x42=4x16+2=66个字节<br>  info[66]：根据<code>Code</code>属性的结构再展开：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Code_attribute &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u2 max_stack;</span><br><span class="line">    u2 max_locals;</span><br><span class="line">    u4 code_length;</span><br><span class="line">    u1 code[code_length];</span><br><span class="line">    u2 exception_table_length;</span><br><span class="line">    &#123;   u2 start_pc;</span><br><span class="line">        u2 end_pc;</span><br><span class="line">        u2 handler_pc;</span><br><span class="line">        u2 catch_type;</span><br><span class="line">    &#125; exception_table[exception_table_length];</span><br><span class="line">    u2 attributes_count;</span><br><span class="line">    attribute_info attributes[attributes_count];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  max_stack：<code>00 02</code>，栈的最大值，2<br>  max_locals：<code>00 01</code>，本地变量的最大值，1<br>  code_length：<code>00 00 00 10</code>，code长度，16字节<br>  code[16]：<code>2A B7 00 01 2A 04 B5 00 02 2A 12 03 B5 00 04 B1</code>，这些对应的是虚拟机指令，查规范可得分别是<code>aload_0 invokespecial indexbyte1=0 indexbyte2=1 aload_0 iconst_1 putfield indexbyte1=0 indexbyte2=2 aload_0 ldc index=3 putfield indexbyte1=0 indexbyte2=4 return</code>,其中<code>indexbyte</code>是用来给它前面的指令加载常量池用的地址，地址的值是<code>indexbyte1&lt;&lt;8 | indexbyte2</code>，就是两个字节表示的数，上面的指令整理后如下：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">aload_0</span><br><span class="line">invokespecial #1 SuperTest.&lt;init&gt;()V</span><br><span class="line">aload_0</span><br><span class="line">iconst_1 </span><br><span class="line">putfield #2 Test.a:I</span><br><span class="line">aload_0</span><br><span class="line">ldc #3 hello world</span><br><span class="line">putfield #4 Test.s:Ljava/lang/String;</span><br><span class="line">return</span><br></pre></td></tr></table></figure>
<p>  exception_table_length:<code>00 00</code>，长度0。<br>  exception_table[exception_table_length]:因为上面的长度是0，所以这里是空。<br>  attributes_count:<code>00 02</code>,属性数量，2个。<br>  attributes[2]：2个属性,分别展开：</p>
<ul>
<li><p>第一个属性：</p>
<ul>
<li><p>attribute_name_index：<code>00 11</code>，第0x11=1x16+1=17个条目，即<code>LineNumberTable</code>，其结构如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LineNumberTable_attribute &#123;</span><br><span class="line">u2 attribute_name_index;</span><br><span class="line">u4 attribute_length;</span><br><span class="line">u2 line_number_table_length;</span><br><span class="line">&#123;   u2 start_pc;</span><br><span class="line">    u2 line_number;	</span><br><span class="line">&#125; line_number_table[line_number_table_length];</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>attribute_length:<code>00 00 00 0E</code>,长度，14个字节</p>
</li>
<li><p>line_number_table_length：<code>00 03</code>表格长度，3</p>
</li>
<li><p>line_number_table[3]:属性长度14个字节，减去上面表示表格长度的2个字节，正好12字节，<code>00 00 00 02 00 04 00 03 00 09 00 04</code>分别对应当前的[0,2],[4,3],[9,4]</p>
</li>
</ul>
</li>
<li><p>第二个属性：</p>
<ul>
<li>attribute_name_index：<code>00 12</code>，第0x12=1x16+2=18个条目，即<code>LocalVariableTable</code>,其结构如下:  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LocalVariableTable_attribute &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u2 local_variable_table_length;</span><br><span class="line">    &#123;   u2 start_pc;</span><br><span class="line">        u2 length;</span><br><span class="line">        u2 name_index;</span><br><span class="line">        u2 descriptor_index;</span><br><span class="line">        u2 index;</span><br><span class="line">    &#125; local_variable_table[local_variable_table_length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>attribute_length:<code>00 00 00 0C</code>,属性长度，12个字节</li>
<li>local_variable_table_length:<code>00 01</code>，表格长度，1  </li>
<li>local_variable_table[1]:属性长度12个字节，减去上面表示表格长度的2个字节，正好10字节，<code>00 00 00 10 00 13 00 14 00 00</code>，对应里面的5个属性:</li>
<li>start_pc:<code>00 00</code>,0</li>
<li>length:<code>00 10</code>,16</li>
<li>name_index:<code>00 13</code>,第19个条目，<code>this</code></li>
<li>descriptor_index:<code>00 14</code>,第20个条目，<code>LTest</code></li>
<li>index:<code>00 00</code>,0</li>
</ul>
</li>
</ul>
<p>  回头看方法的<code>attribute</code>里的<code>info</code>是66个字节，把上面分析的内容加起来正好也是66字节。</p>
</li>
</ul>
<h4 id="第二个方法"><a href="#第二个方法" class="headerlink" title="第二个方法"></a>第二个方法</h4><p>再看一遍方法的结构:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">method_info &#123;</span><br><span class="line">    u2             access_flags;</span><br><span class="line">    u2             name_index;</span><br><span class="line">    u2             descriptor_index;</span><br><span class="line">    u2             attributes_count;</span><br><span class="line">    attribute_info attributes[attributes_count];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>access_flags:<code>00 01</code>,即<code>ACC_PUBLIC</code></p>
</li>
<li><p>name_index：<code>00 15</code>，指向第21个条目，即<code>sayHello</code></p>
</li>
<li><p>descriptor_index:<code>00 16</code>,第向第22个条目，即<code>(Ljava/lang/String;)V</code></p>
</li>
<li><p>attributes_count：<code>00 01</code>，属性数量，1条</p>
</li>
<li><p>attributes[1]：属性内容，先看名称<code>00 10</code>，指向第16个条目，<code>Code</code>，有如下结构：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Code_attribute &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u2 max_stack;</span><br><span class="line">    u2 max_locals;</span><br><span class="line">    u4 code_length;</span><br><span class="line">    u1 code[code_length];</span><br><span class="line">    u2 exception_table_length;</span><br><span class="line">    &#123;   u2 start_pc;</span><br><span class="line">        u2 end_pc;</span><br><span class="line">        u2 handler_pc;</span><br><span class="line">        u2 catch_type;</span><br><span class="line">    &#125; exception_table[exception_table_length];</span><br><span class="line">    u2 attributes_count;</span><br><span class="line">    attribute_info attributes[attributes_count];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>attribute_length:<code>00 00 00 40</code>,属性长度64字节</p>
</li>
<li><p>max_stack：<code>00 02</code>，2</p>
</li>
<li><p>max_locals：<code>00 02</code>，2</p>
</li>
<li><p>code_length：<code>00 00 00 08</code>，长度8</p>
</li>
<li><p>code[8]:<code>B2 00 05 2B B6 00 06 B1</code>,分别对应<code>getstatic indexbyte1 indexbyte2 aload_1 invokevirtual indexbyte1 indexbyte2 return</code>,整理得：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">getstatic #5 java/lang/System.out:Ljava/io/PrintStream;</span><br><span class="line">aload_1</span><br><span class="line">invokevirtual #6 java/io/PrintStream.println:(Ljava/lang/String;)V</span><br><span class="line">return</span><br></pre></td></tr></table></figure></li>
<li><p>exception_table_length:<code>00 00</code>  </p>
</li>
<li><p>exception_table[0]:空</p>
</li>
<li><p>attributes_count：<code>00 02</code>,属性数量2个  </p>
</li>
</ul>
<p>  第一个属性：</p>
<pre><code>  - attribute_name_index：`00 11`，第17个条目，即`LineNumberTable`,有如下结构：

      <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LineNumberTable_attribute &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u2 line_number_table_length;</span><br><span class="line">    &#123;   u2 start_pc;</span><br><span class="line">        u2 line_number;	</span><br><span class="line">    &#125; line_number_table[line_number_table_length];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
  - attribute_length:`00 00 00 0A`,属性长度10
  - line_number_table_length：`00 02`，表格长度，2
  - line_number_table[2]:`00 00 00 06 00 07 00 07 `,对应[0,6],[7,7]
</code></pre>
<p>  第二个属性：</p>
<pre><code>  - attribute_name_index：`00 12`，第18个条目，即`LocalVariabletable`,有如下结构：
      <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LocalVariableTable_attribute &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u2 local_variable_table_length;</span><br><span class="line">    &#123;   u2 start_pc;</span><br><span class="line">        u2 length;</span><br><span class="line">        u2 name_index;</span><br><span class="line">        u2 descriptor_index;</span><br><span class="line">        u2 index;</span><br><span class="line">    &#125; local_variable_table[local_variable_table_length];</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
  - attribute_length:`00 00 00 16`,属性长度0x16=1x16+6=22字节
  - local_variable_table_length：`00 02`表格长度，2
  - local_variable_table[2]:表格长度2，内容如下：  
      &#123;
          start_pc:`00 00`,0
          length：`00 08`，8
          name_index：`00 13`,第19个条目，`this`
          descriptor_index：`00 14`，第20个条目，`LTest`
          index:`00 00`,0
      &#125;,
      &#123;
          start_pc:`00 00`,0
          length：`00 08`，8
          name_index：`00 0C`,第12个条目，`s`
          descriptor_index：`00 0D`，第13个条目，`Ljava/lang/String;`
          index:`00 01`,1
      &#125;
</code></pre>
</li>
</ul>
<h3 id="attributes-count-1"><a href="#attributes-count-1" class="headerlink" title="attributes_count"></a>attributes_count</h3><p><code>00 02</code>,<code>class</code>的属性数量，2个</p>
<h3 id="attributes-attributes-count-1"><a href="#attributes-attributes-count-1" class="headerlink" title="attributes[attributes_count]"></a>attributes[attributes_count]</h3><p>2个属性的内容：</p>
<ul>
<li><p>第一个属性</p>
<ul>
<li>attribute_name_index:<code>00 17</code>,第23个条目，<code>SourceFile</code>,有如下结构：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SourceFile_attribute &#123;</span><br><span class="line">    u2 attribute_name_index;</span><br><span class="line">    u4 attribute_length;</span><br><span class="line">    u2 sourcefile_index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>attribute_length：<code>00 00 00 02</code>，属性长度，2，这是固定的，因为后面的内容固定为2字节</li>
<li>sourcefile_index:<code>00 18</code>,源文件，第24个条目，<code>Test.java</code></li>
</ul>
</li>
<li><p>第二个属性</p>
<ul>
<li>attribute_name_index:<code>00 19</code>,第25个条目，<code>RuntimeInvisibleAnnotations</code>,有如下结构:<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">RuntimeInvisibleAnnotations_attribute &#123;</span><br><span class="line">    u2         attribute_name_index;</span><br><span class="line">    u4         attribute_length;</span><br><span class="line">    u2         num_annotations;</span><br><span class="line">    annotation annotations[num_annotations];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>attribute_length:属性长度，<code>00 00 00 06</code>，6个字节</li>
<li>num_annotations：<code>00 01</code>，注解编号，1</li>
<li>annotations[1]:注解内容，annotation有如下结构：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">annotation &#123;</span><br><span class="line">    u2 type_index;</span><br><span class="line">    u2 num_element_value_pairs;</span><br><span class="line">    &#123;   u2            element_name_index;</span><br><span class="line">        element_value value;</span><br><span class="line">    &#125; element_value_pairs[num_element_value_pairs];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>type_index：<code>00 1A</code>,注解类型，第26个条目，<code>LMyAnnotation</code></li>
<li>num_element_value_pairs：<code>00 00</code>，注解内的value及其赋值的数量,0</li>
<li>element_value_pairs[0]：空</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>到这里文件内容就还原结束了，跟<code>javap -v Test.class</code>的内容可以进行对比，基本一致。</p>
<h2 id="3-JVM从加载class文件到Class初始化的过程"><a href="#3-JVM从加载class文件到Class初始化的过程" class="headerlink" title="3 JVM从加载class文件到Class初始化的过程"></a>3 JVM从加载class文件到Class初始化的过程</h2><p>按照虚拟机规范的分类，这个过程可以分成3个步骤，但是这3个步骤也不是简单的顺序执行，而是根据需要交织在一起。<br>先介绍一下这3个步骤，再用一个例子说明。<br><em>注：class文件可以表示类或接口，jdk9以后还可以表示模块，因为模块用的不多，下文没有特殊说明的地方都默认表示类或接口。</em></p>
<h3 id="3-1-创建和载入-Creation-and-Loading"><a href="#3-1-创建和载入-Creation-and-Loading" class="headerlink" title="3.1 创建和载入(Creation and Loading)"></a>3.1 创建和载入(Creation and Loading)</h3><p>准确的说应该是”载入和创建”(为了跟类”加载”区分出来，这里loading译成载入而不是加载）。这个过程用<code>classlader</code>把<code>class</code>文件（或者其它表示形式如二进制数据流等）载入到虚拟机中转化成<code>方法区</code>内部的表现形式（具体跟虚拟机实现有关），从而完成对应Class的创建。在载入时，虚拟机会执行格式检查(<a href="https://docs.oracle.com/javase/specs/jvms/se11/html/jvms-4.html#jvms-4.8">format checking</a>)，主要是检查class文件整体格式是否符合要求，常量池中的数据格式等，这只算是“表面”上的检查，因为还没有执行链接中的解析等操作，就算常量池中的引用类型对应的Class并不存在，在这个阶段也查不出来。在后面的链接阶段会执行更详细的检查。</p>
<p>在虚拟机的角度，只有两种<code>classloader</code>:</p>
<ul>
<li><code>Bootstrap Class Loader</code>,虚拟机内部实现的类加载器</li>
<li><code>User-defined Class Loader</code>,除了Bootstrap之外的其它类加载器，都是<code>ClassLoader</code>这个抽象类的子类</li>
</ul>
<p>在用类加载器<code>L</code>加载名字为<code>N</code>的Class（记为<code>C</code>）时，两种类加载器的表现有点区别：<br>第一步：两种类加载器都会首先判断当前类加载器<code>L</code>是否已经被记录为名字为<code>N</code>的类的初始化加载器（initiating loader)，如果是，说明想要的Class已经被加载过，直接返回结果<code>C</code><br>第二步：如果第一步没有找到对应的记录，那么：</p>
<ul>
<li><code>Bootstrap Class Loader</code>：JVM把名字<code>N</code>这个参数传递给当前加载器的一个方法，根据<code>N</code>去搜索<code>C</code>的一个”宣称合理但未经验证的表现形式”（purported representation)，通常是一个文件，比如文件名是<code>N</code>，找到后JVM会尝试用当前加载器从这个文件生成跟<code>N</code>对应的<code>C</code>。成功后，记<code>L</code>为<code>C</code>的<code>initiating loader</code><sup>注</sup>和<code>defining loader</code><sup>注</sup>。这里会有两种<strong>意外</strong>情况：<ul>
<li>“宣称合理但未经验证的表现形式”找不到：抛出<code>ClassNotFoundException</code>异常的一个实例。</li>
<li>“宣称合理但未经验证的表现形式”验证失败：有几种可能：文件结构不符合虚拟机规范、版本号不被当前虚拟机支持、文件所表示的类并不是<code>N</code>所表示的类（比如<code>class</code>文件内的<code>this_class</code>名字不是<code>N</code>），这些情况都会抛出对应的异常。</li>
</ul>
</li>
<li><code>User-defined Class Loader</code>：它跟<code>bootstrap</code>的区别在于，这个加载器可以把创建操作委托给自己的父加载器（只是委托层次上的父加载器，不是实际的父子类继承关系），当然也可以自己直接创建，如果直接创建，跟上面<code>bootstrap</code>的方式类拟。<blockquote>
<p><span id="defandinit">注</span>：<br>initiating loader:初始化类加载器，可以有多个，如果有委托层次L-&gt;LP，且L发起初始化，但委托给LP，LP是defining lader,完成创建，那么L和LP都是initiating loader<br>defining loader:从文件生成class的这个加载器，也就是实际完成创建的加载器，defining loader必定是initiating loader  </p>
</blockquote>
</li>
</ul>
<p>关于数组：<br>数组类型本身是由虚拟机直接创建的，但它包含的元素类型的加载仍然是用的上面的方式。如果元素类型是引用类型，那么数组<code>C</code>的<code>defining loader</code>记录就是其元素类型的<code>defining loader</code>，如果元素是原始类型，那么数组<code>C</code>的<code>defining loader</code>记录就是<code>bootstrap class loader</code>，当然也同时包含<code>initiating loader</code>的记录。</p>
<p><strong>载入约束（loading constraints)</strong><br>如果<code>C</code>(&lt;N1,L1&gt;确定，表示L1是<code>C</code>的<code>defining loader</code>）里面的字段（字段类型）或者方法（参数类型，返回值类型）引用了其它Class比如<code>D</code>(&lt;N2,L2&gt;),那么相应的字段或者方法的<code>descriptor</code>所涉及到的任意类型，由<code>L1,L2</code>作为初始类加载器加载的时候，其表示的结果应该是同一个类，即N<sup>L1</sup>=N<sup>L2</sup>。比如，<code>C</code>里面的1个方法的<code>descriptor</code>是<code>（LE,LF)LG</code>，那么<code>E,F,G</code>这3个名字分别用<code>L1,L2</code>加载的时候，表示的类应该是相同的。<br>载入约束会在记录类的初始加载器的时候进行检查，后面提到的链接阶段也会触发检查。</p>
<h3 id="3-2-链接（Linking"><a href="#3-2-链接（Linking" class="headerlink" title="3.2 链接（Linking)"></a>3.2 链接（Linking)</h3><p>链接过程可以细分为3个步骤：验证、准备、解析。虚拟机规范没有明确要求链接的操作什么时候执行，只要满足下面几个要求：</p>
<ul>
<li>class在被链接前必须完全被载入</li>
<li>class在初始化之前必须完成验证、准备这两个步骤</li>
<li>如果链接过程会报错，有class跟这个错误有关，那么异常抛出的时机必须是：应用程序执行一些操作，直接或间接的需要链接这个class的时候。这可以让异常的抛出对调用方来说更加的明显。</li>
<li>对一个动态计算的常量的解析操作，会延迟执行，直到满足下面两种情况之一：<ul>
<li><code>ldc,ldc_w,ldc2_w</code>这3个指令引用这个常量，并且执行</li>
<li><code>bootstrap</code>加载器的方法把这个常量作为一个静态参数调用<h4 id="3-2-1-验证-Verification"><a href="#3-2-1-验证-Verification" class="headerlink" title="3.2.1 验证(Verification)"></a>3.2.1 验证(Verification)</h4>这个阶段主要是检查<code>Code_Attribute</code>的格式，因为这个属性内包含了类中的所有方法执行的代码，都是用虚拟机指令表示，属性本身非常复杂，检查的内容也很多，具体看虚拟机规范<a href="https://docs.oracle.com/javase/specs/jvms/se11/html/jvms-4.html#jvms-4.9">4.9,4.10</a><h4 id="3-2-2-准备-Preparation"><a href="#3-2-2-准备-Preparation" class="headerlink" title="3.2.2 准备(Preparation)"></a>3.2.2 准备(Preparation)</h4>这个阶段创建class的静态字段并初始化为系统的默认值，这个值是虚拟机指定的，比如<code>int</code>类型是0，并不是代码中的初始化赋值。在这个阶段会执行上面提到的<strong>载入约束</strong>检查。</li>
</ul>
</li>
</ul>
<p>准备阶段可以在创建(creation)和初始化(initialization)之间的任意时间节点出现。</p>
<h4 id="3-2-3-解析-Resolution"><a href="#3-2-3-解析-Resolution" class="headerlink" title="3.2.3 解析(Resolution)"></a>3.2.3 解析(Resolution)</h4><p>这个阶段就是把常量池里面那些引用符号解析成具体的Class，在解析这些引用符号比如方法时，其中引用的类型会先完成解析。</p>
<p>这里的解析也包括父类和实现的接口等，这些被解析的Class重复上面的流程，先被<code>载入、创建</code>，然后<code>验证、准备、解析</code>。</p>
<h3 id="3-3-初始化（Initialization"><a href="#3-3-初始化（Initialization" class="headerlink" title="3.3 初始化（Initialization)"></a>3.3 初始化（Initialization)</h3><p>class的初始化就是执行其<code>&lt;clinit&gt;</code>方法，这个方法是被虚拟机直接执行的，这一步结束就代表类已经初始化，类中的静态字段和静态方法等已经可用。<br><span id="jvminit">只有以下情况之一才会触发初始化</span>：</p>
<ol>
<li>执行虚拟机的以下四个指令之一：<code>new, getstatic, putstatic,invokestatic</code>，new是初始化类的实例，显然需要先初始化类本身，后面3个就是对类的静态字段和方法的调用，也需要类本身。</li>
<li>以下四种<code>method handle</code>：<code>kind 2 (REF_getStatic), 4 (REF_putStatic), 6 (REF_invokeStatic), or 8 (REF_newInvokeSpecial)</code>，对其中任何一个的解析结果是一个<code>java.lang.invoke.MethodHandle</code>类型的实例，那么在第一次调用这个实例时会触发初始化。这条跟上面那条本质上是一样的，只不过把指令包装在<code>Methodhandle</code>内。</li>
<li>对一些反射方法的调用，比如<code>java.lang.reflect</code>包内的一些方法。</li>
<li>对<code>C</code>的子类进行初始化之前，要先初始化<code>C</code></li>
<li>如果<code>C</code>是一个接口，里面实现了一个<code>non-static,non-abstract</code>的方法(也就是提供了一个<code>default</code>方法时)，假设<code>D</code><strong>直接或间接</strong>实现了<code>C</code>,那么对<code>D</code>的初始化会触发<code>C</code>的初始化，这跟子类初始化触发父类差不多。</li>
<li>作为虚拟机的启动类，比如<code>java MyApp</code>，这里的<code>MyApp</code>这个类会要求初始化。</li>
</ol>
<p>上面的4，5两条，规定了一个类在初始化时，它的父类和提供了<code>default</code>方法的接口也会被初始化，那么为什么普通的接口就算引用了它的静态字段也不会被初始化呢？<br>先看一下<a href="https://docs.oracle.com/javase/specs/jls/se11/html/jls-12.html#jls-12.4.1">JLS12.4.1</a>从java语言使用层面的说明，一个class，记为T，只有下面4种情况之一会触发T的初始化：</p>
<ol>
<li>T是类，当T被实例化时</li>
<li>T中的一个静态方法被调用时</li>
<li>T中的一个静态字段被赋值时</li>
<li>T中的一个静态字段被使用且这个字段不是一个<code>constant variable</code>(<font color="red">俗称常量，被final修饰的原始类型或者String类型变量，且初始化值是一个常量表达式，如原始类型的值或者简单的四则运算或者String常量)</font></li>
<li>子类的初始化会触发父类的初始化以及提供了<code>default</code>方法的接口的初始化，接口本身的初始化不会触发它的父接口的初始化。</li>
<li>一些反射调用</li>
</ol>
<p>这里除了第4条以外，其它几条跟上面的虚拟机规范的内容都能呼应上,第4条意思就是对T的<strong>非常量字段</strong>的引用才会触发T的初始化，什么是常量，上面已经解释了，仔细阅读几遍，很多人都没有真的明白java中定义的常量是什么，举几个反例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static int a=0;//没有final修饰</span><br><span class="line">static final Integer a=0;//不是原始类型</span><br><span class="line">static final int=Integer.valueOf(0);//不符合常量表达式要求，返回的是Integer，还要拆箱才是int</span><br></pre></td></tr></table></figure>
<p>常量的定义比较严格，满足这些定义以后，对于编译器来说这个值就可以确定了。<br>还有一个问题，那些”普通的接口”是什么时候初始化的呢？比如只有几个方法签名的接口？<br>它们不需要初始化，跟常量一样，方法签名这些信息在编译时就确定了，没有必要初始化。</p>
<p><a href="https://docs.oracle.com/javase/specs/jls/se11/html/jls-12.html#jls-12.4.1">JLS12.4.1</a>中也提供了一些示例代码，大家也可以自己构造一些类来验证上面的内容。</p>
<p>PS：上面提到的对于常量字段的使用，在<code>class</code>文件中可以看到对应的访问指令是<code>iconst_n</code>这个系列，而如果使用非常量字段，对应的指令就是<code>getstatic</code>，符合<a href="#jvminit">前面虚拟机规范定义的情况</a>。</p>
<h2 id="4-双亲委派模型-parent-delegation-model"><a href="#4-双亲委派模型-parent-delegation-model" class="headerlink" title="4 双亲委派模型(parent-delegation model)"></a>4 双亲委派模型(parent-delegation model)</h2><p>这个翻译容易让人误解，叫“父类委派模型”或许更直观。<br>简单的说，就是一个<code>classloader</code>加载一个类时，会先把这个加载任务委派给自己的父加载器（只是委派模型中的父子关系，不是类的继承）,父加载器也会委派给自己的父加载器。。。直到<code>bootstrap</code>这个最基本的类加载器，它没有父加器，如果<code>bootstrap</code>无法加载，那么再回退给委派给它的那个类加载器，如果这个类加载器也无法加载，再回退。。。直到回到最初的那个类加载器，调用<code>findClass</code>方法，这个方法内容是由具体实现类重载的，如果这个加载器也无法加载，则会报错。<br>在<code>ClassLoader</code>这个抽象类中，<code>loadClass</code>方法包含了这个模型的逻辑：</p>
<details>
<summary>显示代码</summary>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> Class&lt;?&gt; loadClass(String name, <span class="keyword">boolean</span> resolve)</span><br><span class="line">    <span class="keyword">throws</span> ClassNotFoundException</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (getClassLoadingLock(name)) &#123;</span><br><span class="line">        <span class="comment">// First, check if the class has already been loaded</span></span><br><span class="line">        Class&lt;?&gt; c = findLoadedClass(name);</span><br><span class="line">        <span class="keyword">if</span> (c == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">long</span> t0 = System.nanoTime();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (parent != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    c = parent.loadClass(name, <span class="keyword">false</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    c = findBootstrapClassOrNull(name);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">                <span class="comment">// ClassNotFoundException thrown if class not found</span></span><br><span class="line">                <span class="comment">// from the non-null parent class loader</span></span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (c == <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="comment">// If still not found, then invoke findClass in order</span></span><br><span class="line">                <span class="comment">// to find the class.</span></span><br><span class="line">                <span class="keyword">long</span> t1 = System.nanoTime();</span><br><span class="line">                c = findClass(name);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// this is the defining class loader; record the stats</span></span><br><span class="line">                PerfCounter.getParentDelegationTime().addTime(t1 - t0);</span><br><span class="line">                PerfCounter.getFindClassTime().addElapsedTimeFrom(t1);</span><br><span class="line">                PerfCounter.getFindClasses().increment();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (resolve) &#123;</span><br><span class="line">            resolveClass(c);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> c;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</details>

<p>上面的<code>parent!=null</code>用来判断是否是<code>bootstrap</code>类加载器，<code>null</code>代表<code>bootstrap</code>加载器。</p>
<p><strong>Java9引入模块化以后，类加载器的部分也发生了变化，其中的<code>PlatformClassLoader</code>（取代了以前的<code>ExtClassLoader</code>），<code>AppClassLoader</code>都是继承自<code>BuiltinClassLoader</code>，而<code>BuiltinClassLoader</code>内的<code>loadClassOrNull</code>方法加入了先判断类是否属于某个模块的逻辑，然后再执行父类委派，某种程度上已经破坏了双亲委派，所以后面不针对JAVA11，只是对传统意义上的双亲委派进行优缺点分析</strong><br>下面这段代码可以查看jdk中所有模块对应的加载器，有兴趣的可以看一下模块化以后的类加载情况：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ClassNotFoundException </span>&#123;</span><br><span class="line">        <span class="keyword">for</span>(Module m:ModuleLayer.boot().modules())&#123;</span><br><span class="line">            System.out.println(m.getName()+<span class="string">&quot;====&gt;&quot;</span>+m.getClassLoader());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>双亲委派的优点：</p>
<ul>
<li>保证核心类的加载安全：我们用换一种方式来描述双亲委派机制的作用：越重要的类由越底层的类加载器加载，像<code>java.util</code>,<code>java.lang</code>这些常用的核心库文件都是由<code>bootstrap class loader</code>加载的，只有几个内置的加载器都不加载的类，才轮到自定义加载器加载。所以比如我们自定义了一个<code>Integer</code>类型，编译后放在当前项目的class目录内，然后<code>AppClassLoader</code>负责加载当前项目的类，假如没有双亲委派，那么自定义的这个<code>Integer</code>类就会在这个阶段被加载，这对于核心类来说太容易被破坏了，除非虚拟机在加载<code>class</code>时要不断的去判断这些class文件是否跟核心类库里的类冲突，就算是检测到冲突，也只能报运行时异常了。如果基于双亲委派模型，那么<code>AppClassLoader</code>就会委派给<code>ExtClassLoader</code>(jdk8以及前)或者<code>PlatformClassLoader</code>（jdk9及以后）,然后再委派给<code>bootstrap class loader</code>，最后由<code>bootstrap class loader</code>加载这个类，这就保证了所有用到<code>Integer</code>等核心类库里的类的地方，就算有冲突，最终都会加载的核心类库里的类。<blockquote>
<p>PS：所有类都是全限定名，比如<code>Integer</code>是<code>java.lang.Integer</code>，所以上面的测试也要把<code>Integer</code>放在<code>java.lang</code>这个包内，但是直接代码结构定义这个包的话会在编译时报错，提示包名跟核心类库冲突，所以我们只能先在别的路径先编译好一个<code>Integer.class</code>，然后在项目的class路径内创建<code>java.lang</code>这个包把<code>Integer.class</code>放进去</p>
</blockquote>
</li>
<li>避免类的重复加载：如果没有用双亲委派模型，就算没有上面说的安全问题，且一个自定义类加载器在当前目录找不到class文件的时候，会自动去查找核心类库里的class文件，然后成功加载，那么100个自定义的类加载器就会有100次一模一样的加载，并且都把自己记录为这个class的初始类加载器(理论上来说也是define class loader)。如果用了双亲委派模型，比如<code>Integer</code>这个类，虽然是最终由<code>bootstrap class loader</code>加载的，但是根据<a href="#defandinit">前面</a>虚拟机规范定义的规则，发起这个加载的classloader，比如叫<code>L1</code>,和<code>bootstrap class loader</code>都会被记录为这个类的初始化加载器（initiating loader）,后面如果<code>L2</code>也要加载这个类，最终委派给<code>bootstrap class loader</code>的时候，会直接返回结果，并把<code>L2</code>也记录为<code>initiating loader</code>,这样，同一个类，只需要由”最接近底层且符合要求”的类加载器加载一次，虽然这个加载器可能会被多次委派去加载这个类。</li>
</ul>
<p>双亲委派的缺点：<br>因为双亲委派只能由“子加载器”向“父加载器”委派，而不能反向，很多时候这都没什么影响，比如我们编写一个普通的程序，要么用“核心类库”里的功能（向上委派由<code>bootstarp class loader</code>或者<code>PlatfromClassLoader</code>加载），要么调用由我们自己代码实现的功能（委派后回退到<code>AppClassLoader</code>加载），但是在<a href="https://en.wikipedia.org/wiki/Service_provider_interface">SPI</a>或者一些框架的实现中，就不是这么简单。</p>
<p>比如，jdbc，只提供了一套接口，具体的实现是由不同的数据库厂商负责的，我们在编写代码的时候，都是面向java平台提供的接口编程，用一个例子说明问题：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> SQLException, ClassNotFoundException </span>&#123;</span><br><span class="line">        Connection conn= DriverManager.getConnection(<span class="string">&quot;jdbc:mysql://localhost:3306/testdb&quot;</span>,<span class="string">&quot;root&quot;</span>,<span class="string">&quot;123456&quot;</span>);</span><br><span class="line">        <span class="comment">//看一下这两个类的classloader</span></span><br><span class="line">        System.out.println(Connection.class.getClassLoader());</span><br><span class="line">        System.out.println(DriverManager.class.getClassLoader());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面的<code>Connection</code>,<code>DriverManager</code>都在<code>java.sql</code>这个包下面，打印结果显示它们都是由<code>PlatformClassLoader</code>加载，而第三方实现肯定是用<code>AppClassLoader</code>或者自定义类加载器才能加载，如果用双亲委派的模型，上面的<code>getConnection</code>方法在尝试加载具体实现类的时候，会尝试用自己的加载器(<code>PlatformClassLoader</code>)去加载第三方类，但是无法加载，然后报错。<br>解决方法就是生成一个<code>AppClassLoader</code>去加载第三方类，比如用<code>Thread.currentThread().getContextClassLoader()</code>获取当前线程的加载器。</p>
<p>最后用文字简单记一下通过<code>DriverManager</code>这个类为入口分析第三方实现类加载的过程：  </p>
<ul>
<li>首先<code>getConnection</code>调用内部的<code>getConnection（）</code>方法，这个方法内部有一个<code>ensureDriversInitialized</code>方法  </li>
<li><code>ensureDriversInitialized</code>方法内部有一个<code>AccessController.doPrivileged</code>方法，其中使用了<code>ServiceLoader</code>类  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ServiceLoader&lt;Driver&gt; loadedDrivers = ServiceLoader.load(Driver.class);</span><br><span class="line">Iterator&lt;Driver&gt; driversIterator = loadedDrivers.iterator();</span><br></pre></td></tr></table></figure>
  ServiceLoader.load方法会设置一个类加载器变量<code>ClassLoader cl = Thread.currentThread().getContextClassLoader()</code>，并在<code>ServiceLoader</code>类的构造方法中赋值给变量<code>loader</code><br>  关键在于这个<code>driversIterator</code>变量，它的操作会通过<code>newLookupIterator</code>类型的变量来完成，当后面的代码调用<code>driversIterator.hasNext()</code>的时候，触发懒加载机制，最终来到<code>ServiceLoader</code>的内部类<code>LazyClassPathLookupIterator</code>的<code>hasNextService()</code>方法</li>
<li><code>hasNextService()</code>方法里调用了<code>nextProviderClass()</code>方法，这个方法去<code>META-INF</code>下查找第三方实现类的信息，方法最终返回<code>return Class.forName(cn, false, loader)</code>,这一步才加载真正的实现类，用的上面的变量<code>loader</code>所定义的加载器</li>
</ul>
]]></content>
      <categories>
        <category>编程语言特性</category>
      </categories>
  </entry>
</search>
